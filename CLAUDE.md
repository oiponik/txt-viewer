# CLAUDE.md

이 파일은 이 저장소에서 작업하는 Claude Code(claude.ai/code)에게 가이드를 제공한다.

## 이 앱이 뭔지

Bookify — 로그인한 사용자가 `.txt` 파일을 업로드해서 페이지 넘기는 책 형태로 읽을 수 있는 한국어 싱글페이지 웹앱. 페이지 넘김(애니메이션 + "지금 몇 페이지가 보이는지" 상태 관리)은 전부 자체 구현이다(`js/portrait-flip.js` + `js/page-window.js`) — 예전엔 `page-flip` 서드파티 라이브러리를 자체 호스팅+패치해서 썼지만 2026-08-26에 완전히 걷어냈다(아래 진행 상황 메모 참고). Firebase 기반 폴더/서재 관리, 사용자별 읽기 진행상황·책갈피, 오프라인 지원까지 갖췄다. 빌드 과정 없음 — 순수 ES 모듈을 정적 파일로 그대로 서빙한다.

## 명령어

`package.json`도 빌드/린트/테스트 도구도 없다 — 순수 HTML/CSS/JS를 그대로 서빙하는 구조.

- **로컬 개발 서버**: `preview_start` 도구로 `.claude/launch.json`의 `"static-server"` 설정(`.claude/serve.ps1`, 작은 파워셸 정적 파일 서버, 8934 포트 — git에는 커밋 안 됨, `.gitignore` 참고)을 사용한다. 이 도구 없이 직접 실행하려면: `powershell -NoProfile -ExecutionPolicy Bypass -File .claude/serve.ps1`
- **개발자 모드 로그인** (아래 "개발자 모드 테스트" 참고): `localhost`/`127.0.0.1`에서 로그인 화면 이메일란에 `dev`를 입력하면(비밀번호는 아무거나/공란) Firebase Auth를 완전히 우회해서 프로덕션 데이터를 건드리지 않고 테스트할 수 있다.
- 배포는 그냥 정적 파일 호스팅이다(이 저장소 어디에도 서버 코드는 없음).

## 아키텍처

### 모듈 구조 (`js/`)

번들러 없이 ES 모듈 import/export로 연결된 8개 도메인 모듈, `index.html`이 `<script type="module" src="js/main.js">` 하나만으로 불러온다:

- `session.js` — `currentUser` 상태(이 모듈만 직접 재할당하고, 다른 곳은 전부 `setCurrentUser()`를 호출), 개발자모드/관리자 권한 체크(`isDevUser`, `isAdminUser`). 의존성 그래프의 최하단 — 이 프로젝트의 다른 어떤 것도 import하지 않는다.
- `firebase-init.js` — Firebase `app`/`db`/`storage`/`auth`를 초기화하는 유일한 곳. 다른 모든 모듈은 여기서 `db`/`storage`/`auth`를 import해서 쓰지, 다시 초기화하지 않는다.
- `ui-shared.js` — 화면 전환(`showLibraryScreen`/`showViewerScreen` — `showAuthScreen`은 인증 전용 상태에 의존하기 때문에 `auth.js`에 있다), Wake Lock, 하단 상태 토스트(`setStatus(text, progress?)` — `progress`(0~1)를 넘기면 진행률 바+퍼센트가 있는 카드로, 안 넘기면 기존처럼 텍스트 pill로), 범용 시트 열기/닫기(`openSheet`/`closeSheet` + `[data-close-panel]` 배선).
- `offline-cache.js` — 책 텍스트의 IndexedDB 캐시(아래 "오프라인 지원" 참고).
- `reader.js` — 뷰어 화면: 페이지분할/페이지 넘김(`js/page-window.js`로 상태 관리, `js/portrait-flip.js`로 애니메이션 — 둘 다 서드파티 라이브러리 없이 자체 구현), 검색, 책갈피, 리더 설정, 진행상황 동기화, 몰입모드, 밝기 스와이프. 가장 큰 모듈.
- `library.js` — "내 서재" 화면: 폴더, 파일 목록, 드래그앤드롭, 업로드, 최근 파일.
- `auth.js` — 로그인/회원가입, 개발자 로그인, `onAuthStateChanged` 게이트, 로그아웃. 다른 모든 도메인 모듈을 import하므로 사실상 앱의 진짜 진입 시퀀스 역할을 한다.
- `storage-stats.js` — 설정 메뉴의 "기기 저장공간 현황" 하위 화면: 오프라인 저장된 책 / 진행상황·책갈피 캐시 / 페이지 나누기 캐시 / 서재 구조·환경설정 / 앱 실행 파일 5개 카테고리의 용량을 보여주고 책 단위(또는 전체) 삭제를 지원한다. `session.js`/`offline-cache.js`/`ui-shared.js`를 import하고, `library.js`의 `openItemActionSheet`를 재사용한다 — 다만 캐시 키 형식(접두사)은 `reader.js`/`library.js`의 진짜 키 생성 함수와 별개로 자체적으로 알고 있으므로, **그쪽 키 형식이 바뀌면 여기도 같이 고쳐야 한다.**
- `main.js` — 실제 모듈 진입점. 위 모듈들을 그 부수효과(각 모듈이 로드 시점에 자기 이벤트 리스너를 배선함)를 위해 import하고, 서비스워커를 등록한다.

**`library.js`와 `reader.js`는 서로를 import한다** — 의도된 순환 의존성이다. `library.js`는 목록 행에서 책을 열기 위해 `reader.js`의 `loadFileFromStorage`/`loadDevTestFile`가 필요하고, `reader.js`는 열린 파일을 목록에서 강조 표시하기 위해 `library.js`의 `markActiveFileRow`가 필요하다. 모든 상호 호출이 이벤트 핸들러나 비동기 함수 안에서만 일어나고 모듈 최상위에서는 절대 일어나지 않기 때문에 ES 모듈 스펙상 안전하다.

모듈 간 공유 가변 상태는 반복되는 한 가지 패턴을 따른다: 소유 모듈이 `let someState`(다른 곳에서 import로 읽을 수 있음)와 `setSomeState()` 함수(다른 모듈이 값을 쓸 수 있는 *유일한* 방법 — ES 모듈은 import한 바인딩을 재할당할 수 없으므로)를 함께 export한다. `session.js`의 `currentUser`/`setCurrentUser`나 `reader.js`의 `currentFileName`/`setCurrentFileName`이 그 예.

### Firebase 데이터 모델

- **Auth**: 이메일/비밀번호, `browserLocalPersistence`(오프라인을 포함해 새로고침해도 로그인이 유지됨).
- **Storage**: 업로드된 `.txt` 파일은 전부 `books/` 아래 평평하게 저장된다 — *모든* 계정에 걸쳐 공유되고, 아직 계정별로 분리되지 않았다(아래 관리자 권한 제한 참고).
- **Firestore**:
  - `library/shared` — 앱 전체 서재 폴더 구조(`folders`, 파일명→폴더ID 맵인 `fileFolder`, 드래그 순서변경용 `itemOrder`) 하나를 담은 문서. `books/`와 같은 이유로 공유된다.
  - `users/{uid}/reading_progress/{fileId}`와 `users/{uid}/bookmarks/{fileId}` — 사용자별·파일별. `fileId`는 `fileDocId(fileName)`(`btoa(encodeURIComponent(fileName))`)이라서, 파일명을 바꾸면 이 문서들도 같이 마이그레이션해야 한다(`library.js`의 `migrateFileDocs`).
  - `users/{uid}/settings/readerPrefs` — `{mobile: {...}, pc: {...}}`, 파일이 아니라 기기 종류(뷰포트 < 900px = 모바일)로 구분해서 저장.
  - 보안 규칙은 이 저장소에 없다(Firebase 콘솔에서 관리) — `library.js`와 `session.js`에 콘솔에 있어야 할 정확한 규칙 텍스트가 주석으로 남아있다.

**관리자 권한 제한**: `books/`와 `library/shared`가 모든 계정에 공유되기 때문에, `kinopioo@naver.com`(`session.js`의 `isAdminUser`)만 폴더/파일을 생성·이름변경·이동·삭제할 수 있다 — 계정별 파일 범위 분리가 되기 전까지의 임시 조치. 이건 UI만 가리는 것이라, 맞는 Firestore/Storage 규칙 없이는 진짜 보안이 아니다.

### 개발자 모드 테스트

`localhost`/`127.0.0.1`에서만, 이메일 `dev`로 로그인하면(`auth.js`의 `loginAsDevUser`) Firebase Auth를 완전히 건너뛰고 고정된 로컬 사용자(`DEV_USER_UID`)를 쓴다. 서재에는 정적 파일 `dev-test-book.txt` 딱 하나만 보이고, 진행상황/책갈피/리더설정/서재상태가 전부 Firestore/Storage 대신 `localStorage`로 간다. 브라우저 기반 검증은 전부 이 경로를 우선으로 쓸 것 — 프로덕션 Firebase 데이터를 절대 건드리지 않는다. 다만 오프라인 책 텍스트 캐시(아래 참고)나 진짜 Firebase Auth 지속성은 검증하지 *못한다* — 설계상 둘 다 우회하기 때문.

### 읽기 파이프라인 (`reader.js`)

가장 큰 모듈이고, 페이지 렌더링이 가장 복잡한 부분이다:

1. 책 텍스트는 **DOM 높이 측정 + 갤로핑 탐색**으로 페이지 단위로 나뉜다 — 추정하지 않고 숨겨진 `.page` 요소에서 실제 렌더링된 높이를 측정하며, 대용량 문서에서도 빠르도록 갤로핑/이분탐색 하이브리드를 쓴다. `findForwardPageEnd`/`findBackwardPageStart`가 서로 대칭인 양방향 함수이고, `createMeasurementDom`이 공유되는 숨겨진 DOM 측정 장치를 만든다.
2. **캐시가 없을 때(cold-cache)의 분할은 "0부터 끝까지"가 아니라 양방향·점진적이다.** 캐시 미스 시, `buildInitialWindowSplit`이 먼저 이어보기 위치(`currentLastCharIndex`, 새 책이면 0)를 기준으로 창(≤ `PAGE_WINDOW_RADIUS*2+1`페이지) 분량만 그 원점에서 뒤로*와* 앞으로 나눠서, 첫 페이지가 책 크기와 무관하게 창 크기만큼의 시간 안에 뜨게 한다. `continuePaginationInBackground`가 그 뒤로 화면을 막지 않고 양쪽 방향으로 계속 바깥으로 나누면서, 살아있는 `pageStartIndices`/`allTextPages` 배열에 `prepend`/`append`한다(앞에 붙이는 건 이미 추적 중인 모든 인덱스 — `windowStartIndex`/`windowEndIndex`/`currentDisplayedGlobalPage` — 를 1씩 밀어서 보정해야 한다). `pendingBackwardDone`/`pendingForwardDone`(`isPaginationPending()` 참고)이 각 방향이 책의 진짜 시작/끝에 도달했는지 추적한다. 대기 중일 때는: 페이지 슬라이더가 비활성화되고 "계산 중..."을 보여주며(대략치 표시 안 함), 검색도 비활성화되고(`updateSearchAvailability`), 아직 안 알려진 영역으로 넘기려 하면 에러 대신 "아직 준비 중" 상태를 보여준다(`goToNextPage`/`jumpToPrevPage`의 경계 가드). *완전히* 끝난 분할(양방향 다 끝남)만 캐시에 저장된다 — 부분 결과를 캐싱하면 다음에 열 때 더 작은 책처럼 보이므로 절대 캐싱하지 않는다.
   - ⚠️ 양방향 분할은 같은 책을 순수하게 처음부터 끝까지 정방향으로 분할했을 가상의 결과와 **바이트 단위로 동일하지 않다** — 워드랩이 페이지가 정확히 어디서 시작하는지에 민감해서, 이어보기 원점 근처의 경계가 왼쪽에서 오른쪽으로 쭉 훑었을 때 골랐을 위치와 최대 몇십 자 정도 어긋날 수 있다. 둘 다 똑같이 *유효한* 분할이다(검증됨: 빈틈 없고 단조증가하며 모든 페이지가 `maxHeight`에 맞음) — 이건 의도된 것이지 버그가 아니고, 이 정도 외에는 사용자에게 보이지 않는다.
3. 현재 위치 근처의 **창** 분량 페이지(`PAGE_WINDOW_RADIUS = 15`)만 `#my-book`에 실제로 마운트된다(`computeWindowRange`/`maybeShiftPageWindow`, `js/page-window.js`의 `mountWindow()`) — 책의 나머지는 그냥 메모리에 문자열로만 남아있다. ⚠️ 2026-08-26 이전엔 이 창의 시작 인덱스가 항상 짝수여야 했다(당시 쓰던 서드파티 페이지 넘김 라이브러리가 좌/우 스프레드를 *로컬* 페이지 인덱스로 짝지어서, 창이 홀수로 시작하면 스프레드가 한 페이지씩 밀렸기 때문) — 라이브러리를 걷어내고 스프레드를 전역 인덱스로 직접 짝짓게 되면서 이 제약 자체가 없어졌다(아래 진행 상황 메모 참고).
4. 분할 결과는 두 번 캐싱된다: 메모리(`paginationCache`, 파일+크기+폰트로 키를 만듦)와 `localStorage`(`persistedPaginationKey`) — 페이지 텍스트가 아니라 `pageStartIndices`만 저장하는데, 원본 텍스트가 그 오프셋들로부터 다시 잘라낼 수 있기 때문이다. 크기는 (텍스트 측정용 `maxHeight`와 캐시 키에) 쓰이기 전에 **버킷화**된다(20px/40px 격자로 내림, `WIDTH_JITTER_THRESHOLD`/`HEIGHT_JITTER_THRESHOLD`의 절반) — 안 그러면 열 때마다 미묘하게 다른 뷰포트 차이(주소창 접힘, PWA 상태표시줄 상태 등)가 캐시를 미스시켜서, 같은 기기의 같은 파일이라도 매번 전체 재분할을 강제한다.
   - ⚠️ **실제로 화면에 렌더링하는 크기(`renderWidth`/`renderHeight`, `js/page-window.js`의 `showPage()`/`showSpread()`에 넘김)는 이 버킷값이 아니라 `#book-stage`의 정확한 실측 크기다** — 버킷값(`bookWidth`/`bookHeight`)은 텍스트 측정·캐시 키 용도로만 쓴다. 버킷값은 항상 실측 이하로 "내림"한 값이라 렌더링 크기가 측정 기준보다 작아질 일은 없다(텍스트가 잘릴 위험 없음). 원래는 렌더링 크기도 버킷값을 그대로 썼는데, `#book-stage`가 내용을 가운데 정렬해서 그 버킷값과 실제 무대 크기 사이 간극(최대 39px)이 책 주위에 여백으로 남았다 — 평소엔 안 보이다가 페이지 넘기기 애니메이션의 그림자 효과 때문에 드러났다. 커밋 `7b62bb2`.
5. 책갈피/진행상황은 페이지 번호가 아니라 **글자 인덱스**로 저장된다 — 페이지 번호는 폰트/크기/문단너비/화면크기가 바뀔 때마다 흔들리지만, 글자 오프셋은 그렇지 않다.
6. `buildGeneration`/`fileLoadGeneration` 카운터가, 사용자가 이전 비동기 분할/다운로드가 끝나기도 전에 리사이즈하거나 책을 전환할 때 생기는 경쟁 상태를 막는다 — 백그라운드 점진적 완료 루프도 매 yield 지점마다 `buildGeneration`을 확인해서, 리사이즈/책전환이 라이브 배열을 망가뜨리는 대신 깔끔하게 오래된 진행 중이던 분할을 포기하게 한다.

### 오프라인 지원 (`offline-cache.js` + `sw.js`)

- `sw.js`가 정적 앱 셸(HTML/CSS/JS/아이콘/매니페스트/폰트 + Firebase SDK CDN 스크립트)을 미리 캐싱해서 네트워크가 없어도 앱이 뜬다. **정적 파일 목록이 바뀔 때마다 `CACHE_VERSION`을 올릴 것** — 안 그러면 클라이언트가 계속 예전 파일을 서빙받는다. (예전엔 페이지 넘김 라이브러리도 여기 포함됐지만 2026-08-26에 완전히 걷어냈다 — 아래 진행 상황 메모 참고. 지금 남은 CDN은 Firebase SDK뿐.)
- **⚠️ 같은 출처의 파일이 import하거나 참조하는 외부 CDN URL은 전부 `sw.js`의 사전 캐싱 목록에 있어야 한다 — 이미 두 번의 실제 진단하기 어려운 프로덕션 버그를 냈다:**
  - `firebase-init.js`/`reader.js`/`library.js`/`auth.js`가 `www.gstatic.com/firebasejs/...`에서 Firebase SDK 모듈을 ES 모듈로 바로 import한다. 그 도메인이 사전 캐싱 목록에 통째로 빠져있어서, 실기기에서(온라인 상태로 설치 후 완전 오프라인으로 재실행) 그 import가 에러 하나 없이 영원히 멈췄다 — `firebase-init.js`가 모듈 그래프 맨 앞쪽에 있어서, 폴백으로 실행되어야 할 코드를 포함해 그 아래 *어떤* 것도 실행될 기회를 못 얻었다. 증상은 "파일 하나가 실패함"이 아니라 "앱 전체가 멈춤"처럼 보였다. 2026-08-16에 확인하고 고침.
  - `index.html`이 예전엔 구글 폰트의 CDN CSS를 직접 `<link>`로 불러왔다. 구글 폰트는 각 서체를 수백 개의 작은 유니코드 범위별 조각 파일로 쪼개서 실제로 렌더링된 것만 서빙(그리고 캐싱을 허용)한다 — 그래서 온라인 중에 폰트를 한 번도 수동으로 바꿔본 적 없는 사용자는 폰트 파일이 *하나도* 캐싱 안 돼 있었고, 오프라인에서 폰트를 바꾸면 조용히 아무 효과 없이(에러도 없이 시스템 폰트로 대체) 실패했다. 자체 호스팅으로 고침: 진짜 웹폰트 파일을 구식 `MSIE 6.0` User-Agent로 `curl`해서 받아오고(구글 폰트가 이 방식일 때는 최신 브라우저용 100개+ 조각 대신 서체당 통짜 파일 하나를 서빙함), `fonttools`로 TTF→WOFF2 변환(`pip install fonttools brotli`; `TTFont(...).flavor = 'woff2'; .save(...)`)해서 한글 전체 커버리지를 가진 파일 하나씩을 `fonts/` 아래 뒀고, `styles.css`의 자체 `@font-face` 규칙에서 참조하고, 다른 정적 자산처럼 `PRECACHE_URLS`에 추가했다. 이제 구글 폰트 의존성이 전혀 없다.
  - **주의해서 볼 패턴**: 앱 자기 출처가 아닌 다른 도메인을 가리키는 `<script src>`, `<link>`, ES 모듈 `import`는 전부 "같은 출처만 생각하는" 사각지대다 — 코드베이스 어디든 새로 하나 추가될 때마다 `sw.js`의 `PRECACHE_URLS`/`PRECACHE_CORS_URLS`와 런타임 `fetch` 핸들러의 출처 허용목록을 확인할 것.
  - PageFlip은 처음엔 위 패턴대로 `PRECACHE_NO_CORS_URLS`(CDN, no-cors)로 캐싱했었는데, 2026-08-20에 라이브러리 자체의 버그를 고치려고 아예 `js/vendor/page-flip.browser.js`로 자체 호스팅+패치하는 쪽으로 옮기면서 이 배열 자체가 없어졌다 — 아래 진행 상황 메모 참고. 지금 CDN으로 남아있는 건 Firebase SDK(`PRECACHE_CORS_URLS`)뿐이다.
- `offline-cache.js`는 책 *텍스트* 전용의 별도 IndexedDB 캐시로, 파일명으로 키를 만들고 (책 개수가 아니라) 전체 용량 기준(`OFFLINE_CACHE_BYTES_LIMIT`, 500MB)으로 LRU 제한한다 — 텍스트 파일은 충분히 작아서 개수 제한은 불필요하게 빡빡했다. `reader.js`의 `loadFileFromStorage`는 (온라인이든 오프라인이든) 이 캐시를 먼저 읽는다, 단 백그라운드 신선도 체크(`refreshStaleFlags`, "내 서재"가 로드될 때마다 한 번씩 Storage `getMetadata()`와 비교)가 캐시된 사본이 오래됐다고 표시한 경우는 예외다 — 오래됨 표시는 *다음* 열 때만 적용되고, 읽는 도중에는 절대 적용 안 된다.
- 읽기 진행상황, 책갈피, 서재 폴더 구조는 각각 자기만의 `localStorage` write-through 캐시를 가진다(온라인일 땐 Firestore/Storage가 항상 정답이고, 로컬 사본은 순전히 오프라인 폴백용) — 이건 개발자 모드가 이미 쓰던 패턴을 일반화한 것이지, Firestore 자체의 오프라인 지속성을 켠 게 아니다.
- 모든 서재 쓰기 동작(폴더/파일 생성·이름변경·이동·삭제, 업로드, 드래그 순서변경)은 `navigator.onLine`을 체크해서 오프라인이면 토스트로 거부한다 — 나중을 위해 큐에 쌓아두지 않는다.
- 파일 이름변경이나 삭제는 오프라인 캐시의 해당 항목도 갱신하거나 지워서, 재사용된 파일명 아래 오래된 캐시 항목이 절대 남아있지 않게 한다.
- `sw.js`의 런타임 `fetch` 핸들러는 캐시 히트 시 `respondWith()`와 `event.waitUntil()` 기반 백그라운드 갱신(stale-while-revalidate)을 의도적으로 **같이 쓰지 않는다** — 그 조합이 iOS Safari/WebKit에서 불안정하다는 보고가 있어서, 정적 콘텐츠는 설치 시점(`CACHE_VERSION` 올릴 때)에만 갱신된다. `auth.js`에도 `onAuthStateChanged`가 끝내 안 불릴 경우 강제로 로그인 화면을 보여주는 4초 타임아웃이 있다 — 위 Firebase SDK 사례 같은 미래의 조용한 멈춤에 대비한 최후의 안전장치(index.html의 화면들이 기본적으로 전부 `screen-hidden`이라, 이런 멈춤은 안 그러면 에러 하나 없이 영원히 빈 화면처럼 보인다).
- iOS 참고: iOS에서 서비스워커가 제대로 동작하는 진짜 standalone PWA는 Safari의 "홈 화면에 추가"로만 만들어진다 — iOS의 크롬/파이어폭스 등은 WebKit 래퍼라 "홈 화면에 추가"가 북마크 바로가기에 더 가깝고 오프라인이 안정적으로 안 된다. 이건 애플 플랫폼 제약이지 이 코드베이스에서 고칠 수 있는 게 아니다.

### PWA

`manifest.json` + `icons/`가 iOS/안드로이드에서 "홈 화면에 추가"를 지원하고, `sw.js`가 설치된 앱이 오프라인에서 동작하게 한다.

## 진행 상황 메모 (2026-08-18 기준 — 다른 컴퓨터에서 이어서 작업할 때 참고)

> ⚠️ **이 섹션은 커밋할 때마다 같이 업데이트한다** — 크든 작든, 기능/버그수정/포기한 시도/조사 전부. 커밋 하나가 이 메모 없이 지나가면 다음 세션(다른 컴퓨터일 수도 있음)이 git log만 보고 맥락을 다시 추론해야 한다. 항목 형식: ✅/❌/🔄 상태 + 커밋 해시 + 무엇을/왜/어떻게 + 검증 중 발견한 함정. 되돌리거나 포기한 시도도 이유와 함께 남긴다(예: 아래 iOS PWA 풀스크린 항목).

- ✅ **완료·커밋됨**: 로그인 직후 마지막으로 읽던 책을 자동으로 여는 로직 — 페이지 나누기가 지금 화면 크기 기준으로 캐시에 없어서 실제로 새로 돌아야 하는 상황이면, 로딩 중인 뷰어에 계속 머무는 대신 즉시 내 서재로 돌아간다 (`reader.js`의 `bailToLibraryIfPaginationNeeded`/`setBailToLibraryIfPaginationNeeded`, `auth.js`의 두 자동 로그인 경로에서 세팅). 커밋 `e393457`. 캐시 히트(빠른 경우)는 그대로 뷰어에 머무는 것까지 브라우저로 검증 완료.
- ❌ **논의 후 취소됨**: 폰 전용 세로모드 고정(가로모드 감지 시 전체화면 오버레이로 가리기, 태블릿은 제외). 설계·구현·브라우저 검증까지 마쳤으나 사용자가 최종적으로 "없던 걸로 하자"며 취소 — 관련 코드(`index.html`/`styles.css`/`js/ui-shared.js`/`sw.js`)는 전부 `git checkout`으로 원상복구했고 커밋되지 않았다. **다시 요청받기 전엔 재작업하지 않는다.**
- ✅ **완료·커밋됨**: 설정 패널(`index.html`의 `#settings-panel`)에 "스토리지 현황" 섹션 추가 — 새 모듈 `js/storage-stats.js`. 커밋 `e6acd93`.
  - **카테고리 5개**: 오프라인 저장된 책(IndexedDB, 책별 삭제+전체삭제) / 읽기 진행상황·책갈피 캐시(localStorage, 책별 삭제+전체초기화) / 페이지 나누기 캐시(localStorage, 책별 삭제+전체초기화) / 서재 구조·환경설정(전체초기화만) / 앱 실행 파일(Cache Storage, 읽기 전용) — 설계는 위 문단들과 동일하게 그대로 구현.
  - `offline-cache.js`에 `getAllCachedBooksInfo()`/`clearAllCachedBooks()` 추가. `library.js`의 `openItemActionSheet`를 `export`로 열어서 카테고리별 "관리" 시트(전체삭제 + 책별삭제 나열)에 그대로 재사용 — 새 시트 UI를 안 만들어도 됐다.
  - `reader.js`/`library.js`를 또 import해서 순환을 늘리는 대신, `storage-stats.js`가 캐시 키 형식(접두사)을 자체적으로 알고 있다 — **키 형식이 바뀌면 두 곳을 같이 고쳐야 한다.**
  - ⚠️ 브라우저 검증 중 실제로 잡은 버그: 페이지 나누기 캐시 키의 `fontKey`(`readerPrefs.fontId + ':' + fontSizeStep + ':' + paragraphWidthStep`, 예: `system:2:1`)가 그 자체로 `:`를 포함해서, 파일명을 뒤에서부터 떼어내는 정규식의 마지막 그룹을 `[^:]+`로 짰더니 폰트키에서 걸려 파일명이 잘못 파싱됐다. `(.+)$`(끝까지 전부)로 고쳐서 해결 — 폭/높이가 숫자라는 사실로 앞의 `::` 두 개 위치만 고정하면 나머지는 자연히 갈린다.
  - `main.js`에 `import './storage-stats.js'` 추가, `sw.js`의 `PRECACHE_URLS`에 새 파일 추가하고 `CACHE_VERSION`을 `v7`→`v8`로 올림.
  - 개발자(dev) 세션으로 브라우저에서 5개 카테고리 전부(관리 시트 열기, 책별 삭제, 카테고리 전체삭제/초기화) 실제 클릭까지 검증 완료. 카테고리 1(오프라인 저장된 책)은 dev 모드가 IndexedDB 캐시를 안 거치는 구조라 항상 0B로만 확인됨 — 실제 로그인 계정으로 한 번 더 확인할 가치가 있음(다음에 이어서 볼 사람 참고).
- ✅ **완료·커밋됨**: 큰 파일 페이지 나누기 성능 개선 3건 — 사용자가 "빈번하게 다시 나뉜다"고 신고한 문제.
  1. **캐시 키 버킷화**(`buildFlipBook`) — 폭/높이를 20px/40px 단위로 내림해서, 뷰포트가 열 때마다 미묘하게 달라지는 환경(PWA 상태표시줄 등)에서도 캐시가 계속 히트하게 함.
  2. **양방향(bidirectional) 점진적 페이지 나누기** — 캐시가 없을 때 책 전체(0부터 끝까지)를 다 나눠야 첫 페이지가 보이던 구조를, 이어보기 위치(원점)에서 앞/뒤로 창 분량만 먼저 나누고(`buildInitialWindowSplit`) 나머지는 화면을 안 막고 배경에서(`continuePaginationInBackground`) 채우는 방식으로 바꿈. `findBackwardPageStart`가 기존 `findForwardPageEnd`(리팩터링으로 분리)를 좌우로 뒤집은 대칭 알고리즘. 배경 완료 전엔 슬라이더 "계산 중..." 비활성화(대략치 표시 안 함), 아직 안 나뉜 쪽으로 넘기면 "준비 중" 안내, 검색 비활성화 — 전부 사용자가 직접 정한 사양.
  3. (3번 후보였던 갤로핑 시드 개선/이어보기 prewarm은 1·2만으로 충분해 보여 보류 — 필요해지면 나중에.)
  - ⚠️ 검증 중 발견: 이 저장소(`OneDrive\바탕 화면\txt-viewer`)를 `.claude/launch.json`의 static-server로 띄운 로컬 프리뷰에서, `http://localhost:8000`과 `http://127.0.0.1:8000` 둘 다 특정 정적 파일 URL(예: `js/reader.js`, `dev-test-book.txt`)을 **서버·서비스워커 재시작과도 무관하게** 계속 예전 내용으로 캐싱하는 프록시 계층이 있는 것으로 보임(서버 자체는 `curl`로 확인하면 항상 최신). `http://[::1]:8000`처럼 아직 안 써본 호스트명으로 새 탭을 열면 그 세션 한정으로는 새 캐시라 최신 내용이 나오지만, `session.js`의 `isLocalDevHost`가 `localhost`/`127.0.0.1`만 인식해서 `dev` 로그인 우회가 안 먹힘 — 이 프리뷰 환경에서 코드를 고친 직후 즉시 재검증해야 할 때는 이 캐싱을 염두에 둘 것(다음에 참고할 사람을 위해 남겨둠).
  - 알고리즘 정확성은 실제 앱 대신 페이지 안에서 직접 재구현해 원본(정방향 전용) 결과와 대조하는 방식으로 검증 — 두 방식 모두 완전하고(gap 없음) 단조증가하지만, 원점 근처 경계가 최대 26자 정도 다를 수 있음을 확인(워드랩 민감성 때문, 버그 아님 — CLAUDE.md의 "Reading pipeline" 절 참고).
  - `sw.js`의 `CACHE_VERSION`을 계속 올려가며 진행(이 작업 시점 기준 최신 `v19` — 이후 다른 작업들로 계속 올라갔으니 "최신"으로 읽지 말 것, 지금 실제 최신 버전은 `sw.js` 파일에서 직접 확인).
- ❌ **결론 내리고 포기함 (2026-08-18)**: iOS 홈화면 PWA 완전 풀스크린(상태바 뒤까지 콘텐츠 확장) — 맥 + Safari 원격 Web Inspector로 배포 왕복 없이 라이브 디버깅까지 동원해서 원인을 좁혔지만, **이 기기(다이나믹 아일랜드 모델, `screenHeight`/`screenWidth` = 852×393)에서는 홈화면 standalone 웹앱의 `window.innerHeight`가 무엇을 해도 항상 정확히 `793`(=852−59, 상태바 높이만큼 항상 모자람)으로 고정됐다.** 사용자 결정으로 **완전 포기, 코스메틱 처리로 전환** — `index.html`/`styles.css`/`sw.js`를 이 재시도 시작 전(`d0fe0e4`) 상태로 되돌렸다. `viewport-fit=cover`, `apple-mobile-web-app-capable`/`status-bar-style`, `#debug-version-badge` 전부 소스에서 다시 사라진 상태다. **이 항목은 다시 열지 말 것 — 아래 결정적 증거 때문에 재도전할 가치가 낮다.**
  - **결정적으로 확인된 것**: 매번 완전 삭제 → Safari 데이터 지우기 → 재설치까지 거쳐 캐시 문제를 배제한 뒤, 아래 조합을 각각 **단독으로** 실기기에 올려 `window.screen.height + ' vs ' + window.innerHeight`와 `env(safe-area-inset-top)` 실측값을 라이브 콘솔로 직접 측정했다 — **전부 `852 vs 793`, `safe-area-inset-top`은 항상 `0`으로 완전히 동일했다**:
    1. 아무것도 안 넣은 순정 상태
    2. `viewport-fit=cover`만
    3. `manifest.json`의 `"display": "fullscreen"`만 (W3C 스펙상 `standalone`은 상태바 공간을 예약하고 `fullscreen`은 안 하는데도 차이 없음)
    4. `apple-mobile-web-app-status-bar-style: black-translucent` + `capable`만 (body min-height 보정 없이, 이전 라운드처럼 다른 변경과 섞지 않고 단독으로)
    - 즉 `793`은 버그가 아니라 **이 기기/iOS 버전에서 홈화면 standalone 웹앱이 절대 넘지 못하는 렌더링 상한선**으로 보인다. 이전 라운드(1차·2차 시도, 위쪽 오래된 기록)에서 봤던 상단 겹침/하단 여백 증상은 이 793 자체가 원인이 아니라, 그 위에 얹었던 `body { min-height: calc(100% + env(safe-area-inset-top)) }` 보정 코드가 (실제로는 `safe-area-inset-top`이 항상 0이라 사실상 no-op이어야 함에도) 다른 요인과 겹쳐 부작용을 낸 것으로 추정 — 정확한 상호작용까지는 안 밝혀졌고, 이제 그 보정 코드 자체가 소스에 없다.
  - **채택한 대안(코스메틱)**: 완전 투명 확장은 포기하고, 분리된 불투명 상태바가 앱과 최대한 자연스럽게 이어지도록 색만 맞춘다.
    - `manifest.json`의 `background_color`(`#B7AC97`)가 이미 앱의 `--bg-color`와 정확히 일치해서, 서재/뷰어 화면에서는 이미 상태바 영역이 자연스럽게 이어져 보인다 (재설치 검증 스크린샷으로 확인됨) — 추가 조치 불필요.
    - 유일하게 안 맞는 곳은 로그인 화면(`#auth-screen`, 의도적으로 어두운 `#605549` 배경) — 상태바 경계가 그 화면에서만 두드러진다. **사소한 이슈로 남겨둠, 다시 요청받기 전엔 손대지 않는다.**
  - ⚠️ **iOS Safari 홈화면 PWA는 서비스워커 캐시가 지독하게 끈질기다** — 앱을 완전 종료 후 재실행은 물론, **홈 화면 아이콘을 삭제하고 재설치해도 예전 캐시가 안 지워지는 경우**를 실기기에서 직접 확인함. 확실히 지우려면 설정 → Safari → "방문 기록 및 웹 사이트 데이터 지우기"(전체 삭제, 다른 사이트 로그인도 다 풀림)까지 가야 한다. (다만 이번 라운드에서 `manifest.json`의 `display` 변경만은 이 방법으로도 매번 확실히 반영됐다 — `apple-mobile-web-app-*` 메타태그류는 재실행만으로도 반영될 가능성이 있었지만 확인 안 해봄.)
  - **맥 + Safari 원격 Web Inspector 디버깅 방법 자체는 유효하고 앞으로도 유용함** — 설정 → Safari → 고급 → "웹 속성 관리자" 켜기 → 맥 Safari 개발자용 메뉴 → 기기 선택 → 홈 화면 앱 탭 선택. Elements 패널의 요소 선택 도구로 실기기 화면을 직접 탭하면 `$0`에 정확한 DOM 요소가 잡혀서, PageFlip처럼 라이브러리가 동적으로 만드는 요소(예: `.page`가 32개 중 대부분 `display:none`, 진짜 보이는 것 하나만 `stf__item` 클래스 붙음)를 추측 없이 바로 찾을 수 있었다.
- ✅ **완료·커밋됨 (2026-08-18)**: PageFlip이 페이지를 그릴 때 버킷값이 아니라 무대(`#book-stage`)의 정확한 실측 크기로 렌더링하도록 수정. `#book-stage`가 내용을 가운데 정렬해서, 캐시 키/텍스트 측정용으로 쓰는 버킷값(20px/40px 단위로 내림, "Reading pipeline" 절 4번 참고)과 실제 무대 크기 사이 최대 39px 간극이 책 주위에 여백으로 남았었다 — 평소엔 그 여백 색이 `#book-stage` 배경과 같아서 안 보이다가, 페이지 넘기기 애니메이션의 그림자 효과 때문에 드러남. `bookWidth`/`bookHeight`(버킷값, 캐시 키·측정용으로 계속 사용)와 별도로 `renderWidth`/`renderHeight`(무대 실측값)를 추가해서 `new St.PageFlip(...)`에는 후자를 넘기도록 분리. 최초 커밋 `7b62bb2` — iOS 풀스크린 작업을 되돌리며(`js/reader.js`를 `1668ad8`로 되돌림) 이 무관한 수정까지 같이 쓸려나갔던 걸 `2b0490d`로 재적용. **iOS 풀스크린 문제와는 무관한, 독립적으로 유효한 버그 수정이었다.**
- ✅ **완료·커밋됨 (2026-08-18, `f2a7e49`)**: 로딩/페이지 나누기 상태 토스트(`ui-shared.js`의 `setStatus`)에 진행률 바 + 퍼센트 표시 추가.
  - `setStatus(text, progress)` — `progress`(0~1)를 새로 받는 선택 인자로 추가. 값이 있으면 토스트가 진행률 바 + 퍼센트가 있는 작은 카드 형태로 커지고, 없으면(기존 호출부 대부분 — 에러 메시지, "책갈피 저장됨" 등) 예전 그대로 텍스트 pill로 동작해서 다른 곳은 손댈 필요 없었다.
  - 연결한 곳 2군데: (1) 배경 페이지 나누기(`reader.js`의 `continuePaginationInBackground`) — 진행률은 페이지 수가 아니라 **커버된 글자 수**(`forwardCursor - backwardCursor` / `rawTextData.length`) 기준이라, 폰트·화면 크기와 무관하게 항상 정확히 100%에 도달한다. (2) 파일 다운로드(`loadFileFromStorage`) — Firebase Storage의 `getBytes()`는 진행률 콜백이 없어서, `getDownloadURL()`로 받은 URL을 직접 `fetch`해 응답 본문을 스트림으로 읽으면서 `Content-Length` 대비 받은 바이트 수로 진행률을 추적하는 `downloadWithProgress()`를 새로 만듦 — 스트리밍 fetch가 어떤 이유로든(CORS 등) 실패하면 진행률 없이 기존 `getBytes()` 경로로 조용히 폴백.
  - `CACHE_VERSION` `v25`→`v26`.
- ✅ **완료·커밋됨 (2026-08-20)**: 기기 간 읽기 진행상황 동기화 버그 수정 — 사용자 신고: PC로 읽다가 그대로 두고 폰으로 이어읽은 뒤 PC로 돌아와 새로고침해도 폰에서 더 읽은 진행상황이 사라짐.
  - **원인**: `reader.js`의 `flushProgressSave()`(탭이 `visibilitychange`로 hidden 되거나 `pagehide`될 때 호출)가 이 세션에서 실제로 뭔가 새로 읽었는지 전혀 안 따지고 그 순간 메모리에 있는 `currentLastCharIndex`를 무조건 새 타임스탬프로 다시 저장했다. PC 탭을 그냥 열어만 두고 자리를 비우면(페이지 안 넘김) `currentLastCharIndex`는 옛날 값 그대로인데, 그 사이 폰이 서버에 더 앞선 위치를 저장해놔도, PC로 돌아와 새로고침하는 순간(구 페이지가 사라지기 직전 `pagehide` 발동) PC의 옛 위치가 "방금 갱신된 최신 기록"인 것처럼 서버를 덮어써서 폰의 진행상황이 지워졌다 — 그 직후 새로고침된 새 페이지가 서버를 다시 읽어도 이미 뭉개진 값을 받아온다.
  - **수정**: `progressDirty` 플래그 추가([reader.js:112](js/reader.js:112)) — 사용자가 실제로 페이지를 넘긴 세 지점(`jumpToPrevPage`/`jumpToGlobalPage`/PageFlip `'flip'` 이벤트 핸들러)에서만 `true`로 세팅하고, 디바운스 저장이 실제로 끝나면 `false`로 리셋한다. `flushProgressSave()`는 이제 `progressDirty`가 `true`일 때만 저장을 시도한다 — 아무것도 안 읽은 채 배경으로 가거나 새로고침되는 탭은 서버에 손을 대지 않는다. 새 파일을 열 때(`loadFileFromStorage`/`loadDevTestFile`)는 `lastKnownProgressUpdatedAt`과 함께 `progressDirty`도 `false`로 초기화한다.
  - **보너스**: 탭이 한 번도 배경으로 안 가고(=`visibilitychange`가 안 뜨고) 화면에 계속 떠 있는 채로 다른 기기가 더 읽는 경우도 따라잡도록, 보이는 동안 1분마다 `syncProgressFromServer()`를 추가로 돌리는 `setInterval`을 붙였다([reader.js:2093](js/reader.js:2093) 근처).
  - **검증**: 개발자(dev) 세션(로컬스토리지 기반)에서, ① 아무것도 안 읽은 채 hidden→visible을 흉내내면 저장을 안 건드림, ② 그 상태에서 다른 기기가 저장한 것처럼 로컬스토리지 값을 더 앞선 위치로 바꿔놓고 hidden→visible을 흉내내면 그 값이 그대로 보존됨(수정 전이었다면 PC의 옛 값으로 덮어썼을 상황), ③ 실제로 페이지를 넘긴 직후 곧바로 hidden이 되면 여전히 즉시 플러시되어 새 위치가 정상 저장됨 — 세 가지 다 브라우저에서 직접 확인. 콘솔 에러 없음. 실제 두 기기(진짜 로그인 계정) 간 크로스디바이스 시나리오까지는 이 환경에서 확인 못 함(dev 세션은 Firestore를 안 씀) — 다음에 실기기로 한 번 더 확인할 가치 있음.
  - `CACHE_VERSION` `v26`→`v27`.
- ✅ **완료·커밋됨 (2026-08-20)**: "뒤로가기(이전 페이지)가 애니메이션 없이 즉시 전환돼서 페이지가 실제로 넘어갔는지 구분하기 어렵다"는 사용자 피드백 대응.
  - `jumpToPrevPage`가 왜 애니메이션 없이 즉시 전환하는지는 이미 코드 주석에 설명돼 있음(page-flip 라이브러리의 prev 애니메이션이 방향 비대칭이라 부자연스러움 — "Reading pipeline" 관련 함수 근처 참고) — 그 결정 자체는 유효하므로 되돌리지 않고, 대신 짧은 시각적 피드백만 추가.
  - `#page-turn-flash` — `#brightness-overlay`/`#book-loading-overlay`와 같은 패턴으로 `#book-stage` 밖(`#main-content` 바로 아래)에 둔 새 오버레이 div(`index.html`). `--reading-text`를 옅게(`opacity: 0.16`) 깔았다가 `0.22s ease-out`으로 빠지는 `flash` 애니메이션 클래스를 `styles.css`에 추가 — 라이트/다크 읽기 테마 양쪽에서 다 보이도록 고정색이 아니라 `--reading-text` 변수를 씀.
  - `reader.js`에 `flashPageTurn()` 추가, `jumpToPrevPage()`의 `turnToPage()` 직후에 호출. 연타 시에도 매번 다시 보이도록 `classList.remove` → 강제 리플로우(`el.offsetWidth`) → `classList.add`로 애니메이션을 재시작시킴.
  - 슬라이더 드래그로 넘기는 `jumpToGlobalPage`는 건드리지 않음 — 슬라이더 자체가 이미 시각적 피드백을 주고, 사용자 피드백은 "뒤로가기"(스와이프/탭/키보드 좌측 화살표)에 한정됐음.
  - `CACHE_VERSION` `v27`→`v28`.
  - ⚠️ **검증 한계**: 이 원격 실행 환경(컨테이너)은 프록시가 `www.gstatic.com`/`cdn.jsdelivr.net`(Firebase SDK, page-flip 라이브러리 CDN)을 403으로 막아서, dev 로그인으로 실제 뷰어를 여는 end-to-end 브라우저 검증을 못 했다(`curl`로도 같은 403 확인). 대신 같은 CSS 애니메이션 규칙 + `flashPageTurn()` 로직만 떼어낸 독립 HTML로 Playwright 검증: 트리거 직후 `opacity 0.16` → 애니메이션 도중 감쇠 → 완료 후 `0`, 그리고 애니메이션 도중 재트리거해도 다시 `0.16`으로 정상 복귀하는 것까지 확인. 실제 뷰어 화면에서 눈으로 보는 확인은 다음에 네트워크 제약 없는 환경(로컬 `preview_start` 등)에서 한 번 더 하면 좋음.
  - **이 항목은 아래 2026-08-20 후속 작업으로 대체됨** — 플래시 펄스는 "진짜 애니메이션을 넣어달라"는 후속 요청으로 `#page-turn-flash`/`flashPageTurn()` 자체가 통째로 제거되고 실제 플립 애니메이션으로 바뀌었다. 이 항목은 그 배경 설명(왜 처음엔 플래시로 시작했는지)으로만 남겨둔다.
- ✅ **완료·커밋됨 (2026-08-20)**: 위 플래시 펄스를 진짜 "이전 페이지 플립 애니메이션"으로 교체 — 사용자가 "가짜라도 좋으니 애니메이션 자체를 넣어달라"고 후속 요청.
  - **라이브러리 소스를 직접 확인**(`StPageFlip` GitHub, `raw.githubusercontent.com` — `cdn.jsdelivr.net`/`www.gstatic.com`만 프록시가 막고 `raw.githubusercontent.com`/`github.com`은 열려 있어서 WebFetch로 조회 가능했음)해서 버그 범위를 정확히 좁힘: `PageCollection.getFlippingPage`/`getBottomPage`가 **portrait(세로, 한 페이지) 모드의 backward 방향에서만** 둘 다 `pages[current-1]`을 반환해 2겹 합성이 물리적으로 불가능하다(`HTMLRender.drawBottomPage`도 이 방향엔 그리기 자체를 건너뜀). **landscape(가로, 두 페이지 스프레드) 모드는 이 버그가 없다** — `getFlippingPage`/`getBottomPage`가 스프레드의 서로 다른 두 페이지(`spread[0]`/`spread[1]`)를 반환해서 2겹 합성이 정상 성립.
  - **모드별로 다르게 처리**(`jumpToPrevPage`, `js/reader.js`):
    - `pageFlip.getOrientation() === 'landscape'`면 라이브러리의 진짜 `flipPrev()`를 그대로 호출 — 버그가 없는 모드라 정상적으로 자연스러운 커얼 애니메이션이 나온다. `goToNextPage`가 `flipNext()`를 그대로 쓰는 것과 대칭이 맞아서, `jumpToPrevPage`가 직접 하던 진행상황 저장/페이지 인디케이터 갱신/창 재정렬 로직도 이 경로에서는 필요 없어졌다 — 기존 `pageFlip.on('flip', ...)` 핸들러가 알아서 다 처리해준다.
    - portrait(대부분의 모바일 기기)에서는 라이브러리에 기대지 않고 새 함수 `playPrevPageFlipAnimation()`이 직접 "가짜 플립"을 만든다: 지금 화면에 보이는 `.page` DOM을 `cloneNode`로 복제 → `getBoundingClientRect()`로 잡은 정확한 위치에 `position: fixed`로 고정 → 그 직후(같은 호출 순서 안에서) 실제 콘텐츠는 기존처럼 `turnToPage()`로 즉시 바꿔치기 → 복제본만 CSS(`.page-turn-clone`/`.turning`, `styles.css`)로 왼쪽 모서리를 축으로 `rotateY` + 페이드아웃시켜 걷어낸다. 복제본이 걷히는 동안 그 밑에서 이미 바뀌어 있던 진짜 이전 페이지가 드러나는 것처럼 보이는 착시. `transitionend`(+ 400ms 타임아웃 안전장치)로 복제본을 정리한다.
    - 복제 직후 `clone.removeAttribute('style')`로 page-flip이 내부 레이아웃용으로 걸어둔 인라인 스타일(위치/transform 등)을 전부 버리고 `.page` 클래스 스타일(배경/글자색/폰트 등)만 다시 입힌 뒤 우리가 필요한 top/left/width/height만 인라인으로 채우는 게 핵심 — 안 그러면 라이브러리가 내부적으로 걸어둔 위치/transform이 그대로 남아 복제본이 엉뚱한 곳에 나타난다.
    - 라이브러리 내부에는 전혀 손대지 않는다(몬키패치 없음) — 이 앱이 `page-flip`을 버전 고정 없이 CDN에서 그냥 최신으로 받아 쓰기 때문에, 내부 구현에 의존하는 방식은 버전이 바뀌면 조용히 깨질 위험이 있어서 처음부터 후보에서 제외했다.
  - **제거된 것**: `#page-turn-flash`(`index.html`)와 그 CSS(`styles.css`), `flashPageTurn()`(`js/reader.js`) — 이제 양쪽 모드 다 진짜 애니메이션이 있어서 플래시가 불필요한 중복이 됨.
  - `CACHE_VERSION` `v28`→`v29`.
  - **검증**: 여기서도 CDN이 막혀 있어서 dev 로그인으로 실제 뷰어를 열어보는 end-to-end 검증은 못 했다. 대신 (1) 라이브러리 소스 자체를 직접 읽어서 버그의 정확한 조건(모드/방향)을 코드 레벨로 확인했고, (2) `playPrevPageFlipAnimation()`의 핵심 로직(복제 → 위치 고정 → 콘텐츠 즉시 교체 → 트랜지션 재생 → 정리)만 그대로 떼어낸 독립 HTML로 Playwright 검증: 트리거 직후 복제본이 정확히 생기고, 그 순간 이미 실제 DOM 콘텐츠는 "다음(revealed)" 상태로 바뀌어 있으며, 애니메이션 도중 opacity가 감쇠하고 transform이 non-identity로 바뀌다가, 완료 후 복제본이 깨끗이 제거되는 것까지 확인. **다만 이건 로직이 도는지 확인한 것이지, 실제로 봤을 때 자연스러워 보이는지(회전 각도/그림자/타이밍 등 튜닝)는 확인 못 했다** — 로컬 `preview_start` 같은 네트워크 제약 없는 환경에서 실제로 눈으로 보고 튜닝할 가치가 있음.
- ✅ **완료·커밋됨 (2026-08-20)**: 위 항목을 PR로 올린 뒤(Vercel 프리뷰 배포로) 사용자가 실제 기기에서 직접 보고 "다음 페이지 넘기기 애니메이션이랑 느낌이 다르다"고 피드백 — 튜닝.
  - **원인**: page-flip 라이브러리의 다음 페이지 애니메이션은 `Settings.flippingTime` 기본값인 **1000ms**로 도는데(이 앱이 오버라이드 안 함, `reader.js`의 `new St.PageFlip(...)` 호출부에 없음), `playPrevPageFlipAnimation()`의 `.page-turn-clone` 트랜지션은 처음에 **320ms**로 만들어서 3배 빨랐다 — 스타일(평평한 카드 회전 vs 진짜 커얼) 차이보다 이 속도차가 "다르다"는 인상의 더 큰 원인이었을 것으로 판단.
  - **수정**(`styles.css`): 지속시간을 1000ms로 맞추고, `transition` 기반에서 `@keyframes` 기반(`animation`)으로 바꿔서 접히는 지점(왼쪽 모서리)에서 짙어졌다 옅어지는 그림자 스윕(`::after` + `page-turn-shadow` 키프레임, 45% 지점에서 opacity 피크)을 추가 — 진짜 커얼의 fold shadow를 흉내낸 근사치. `js/reader.js`의 `playPrevPageFlipAnimation()`은 `transitionend` → `animationend`로, 안전장치 타임아웃도 400ms→1100ms로 맞춰 갱신.
  - **의도적으로 안 한 것**: 라이브러리의 진짜 커얼 렌더링 엔진을 그대로 재사용하는 몬키패치(내부 `PageCollection.getFlippingPage`/`getBottomPage` 오버라이드 등)는 여전히 배제 — `page-flip`을 버전 고정 없이 CDN 최신으로 받아 쓰는 구조라 다음 업데이트에 조용히 깨질 위험이 크다고 판단(사용자에게 트레이드오프 설명 후 진행). 그래서 완전히 동일한 알고리즘은 아니고, 속도·이징·그림자를 맞춘 "느낌이 비슷한 근사치"다.
  - `CACHE_VERSION` `v29`→`v30`.
  - **검증**: 이전 항목과 같은 이유로 이 환경에서 dev 로그인 브라우저 검증은 여전히 불가능. 독립 harness로 재검증: `animationstart`/`animationend` 타임스탬프를 직접 로깅해서 두 애니메이션(`page-turn-flip`, `page-turn-shadow`)이 동시에 시작해 정확히 ~1000ms 후 함께 끝나는 것, 중간 지점(~480ms)에 opacity가 감쇠 중이고 그림자가 45% 피크를 지나 내려가는 중인 것, 완료 후 정리되는 것까지 확인. **PR #2의 Vercel 프리뷰 배포 링크를 실제 기기에서 다시 확인하는 절차가 여전히 필요함** — 사용자가 다음 라운드에서 눈으로 보고 재판단 예정.
- ✅ **완료·커밋됨 (2026-08-20)**: 위 항목에서 사용자가 미리보기를 본 뒤 "완전히 동일한 애니메이션으로 만들 순 없냐, 라이브러리 소스 직접 수정하는 범위까지 알아봐달라"고 후속 요청 — `page-flip` 자체를 패치해서 진짜로 동일하게 만듦. 커스텀 가짜 애니메이션(`playPrevPageFlipAnimation`)과 `#page-turn-flash`류 우회는 전부 필요 없어져서 삭제.
  - **라이선스 확인**: `page-flip`(StPageFlip, `Nodlik/StPageFlip`)은 **MIT 라이선스** — 수정·재배포 다 허용(저작권 고지만 유지). `js/vendor/page-flip.LICENSE`로 원문을 같이 커밋해뒀다.
  - **패치 내용**: 정확히 2곳, 둘 다 위에서 여러 라운드에 걸쳐 원본 소스로 확인해뒀던 그 지점이다.
    1. `PageCollection.getFlippingPage()` — portrait 분기가 이제 방향과 무관하게 항상 `this.pages[current].newTemporaryCopy()`를 반환한다(원래는 BACK일 때 `this.pages[current-1]`, 즉 `getBottomPage()`가 반환하는 것과 같은 객체를 반환해서 2겹 합성이 물리적으로 불가능했음).
    2. `HTMLRender.drawBottomPage()` — `orientation===PORTRAIT && direction===BACK`일 때 그리기를 건너뛰던 조건을 제거, 다른 모든 방향/모드 조합과 동일하게 항상 그리도록 함.
  - **빌드 방법**: 이 원격 실행 환경은 `cdn.jsdelivr.net`/`unpkg.com`은 막혀 있지만 **`registry.npmjs.org`는 열려 있어서**, npm 패키지 tarball(`page-flip@2.0.7`, 소스+사전빌드된 `dist/` 둘 다 포함)을 직접 받을 수 있었다. 소스(`src/`)에 위 2곳을 수정한 뒤, 별도 작업 디렉터리에 `rollup`/`rollup-plugin-typescript2`/`rollup-plugin-postcss`/`rollup-plugin-terser`/`typescript@4.9.5`(주의: `typescript@latest`는 `rollup-plugin-typescript2`와 안 맞아서 `tsModule.createDocumentRegistry is not a function`로 빌드가 깨졌다 — 4.9.5로 고정하니 해결)를 설치해서 업스트림과 완전히 같은 `rollup -c`(업스트림 `rollup.config.js` 그대로) 빌드로 `dist/js/page-flip.browser.js`(UMD, `St` 전역)를 재생성했다. 패치 전/후 번들 크기가 44058→44026바이트로 근소한 차이라 다른 의도치 않은 변경이 안 섞였다는 정황 증거로도 확인.
  - **검증(강함)**: 이번엔 로직 흉내가 아니라 **패치된 실제 라이브러리 번들 자체**를 Playwright로 구동해서 확인했다 — (1) npm에서 받은 순정 소스와 diff해서 의도한 2곳 외엔 안 바뀐 것 확인, (2) 격리된 harness에서 `flipPrev()`를 직접 호출해 mid-flip DOM을 검사: 패치 전엔 활성 요소 2개(정적 페이지 + flippingPage, bottomPage는 그리기 자체가 생략됨)였는데 패치 후엔 활성 요소 **3개**(정적 페이지 + 진짜로 그려지는 bottomPage `zIndex:3` + 복사본 flippingPage `zIndex:5`) — forward flip과 완전히 같은 구조가 됐다. (3) forward flip도 여전히 같은 3-요소 구조로 정상 동작(회귀 없음), (4) next/prev를 3번 연속 왕복해도 최종 DOM이 깨끗함(찌꺼기 temp copy 없음), 콘솔 에러 없음. (5) **최종적으로 저장소에 실제로 커밋한 파일**(`js/vendor/page-flip.browser.js`)을 로컬 정적 서버로 직접 서빙해서 같은 테스트를 한 번 더 돌려 재확인 — scratchpad 사본이 아니라 커밋되는 바로 그 파일이 동작함을 확인.
  - **적용 범위 변경**: `reader.js`의 `jumpToPrevPage`가 이제 `goToNextPage`의 `flipNext()`와 완전히 대칭으로 `pageFlip.flipPrev()`를 그냥 호출한다 — landscape/portrait 모드 분기도, 커스텀 애니메이션 함수도 다 필요 없어져서 삭제. `'flip'` 이벤트 핸들러가 진행상황 저장/인디케이터/창 재정렬을 그대로 처리(다른 모든 flip과 동일 경로).
  - **CDN 의존성도 같이 해결**: `index.html`이 이제 `js/vendor/page-flip.browser.js`(같은 출처)를 로드한다 — `cdn.jsdelivr.net` 의존성 자체가 사라져서, "버전 고정 없이 CDN 최신을 받아 쓰던" 리스크(위 여러 항목에서 계속 몬키패치를 배제해온 이유였던 바로 그 리스크)도 부수적으로 해결됐다. `sw.js`에서 `PRECACHE_NO_CORS_URLS`(page-flip 전용이었음)를 통째로 제거하고 `PRECACHE_URLS`(같은 출처)에 새 파일을 추가 — 남은 CDN은 Firebase SDK(`PRECACHE_CORS_URLS`)뿐. 향후 업그레이드는 "새 버전 받기 → 패치 재적용 → 재검증" 명시적 단계가 된다(조용한 자동 업데이트 없음, 의도된 트레이드오프).
  - `js/vendor/page-flip.browser.js` 상단에 왜 자체 호스팅+패치인지, 정확히 뭘 고쳤는지 긴 주석으로 남겨뒀다 — 다음에 업그레이드할 사람을 위한 참고.
  - `CACHE_VERSION` `v30`→`v31`.
  - **남은 일**: PR #2에 이 커밋도 같이 올라감 — 여전히 이 환경에서 실제 브라우저로 최종 확인은 못 했으니, 사용자가 Vercel 프리뷰에서 다시 확인 필요.
- ✅ **완료·커밋됨 (2026-08-20)**: 위 항목을 Vercel 프리뷰로 실제 기기에서 확인한 사용자가 "애니메이션이 굉장히 어색하다, 한 페이지(portrait) 모드에서 확인해달라"고 후속 요청 — 지난 라운드의 패치(콘텐츠 합성)만으로는 부족했고, **완전히 별개의 두 번째 버그(기하학/좌표 계산)**가 남아있었다.
  - **증상 재현**: 이 환경에서도 여전히 실제 뷰어는 못 열지만, 이번엔 실제 벤더 파일을 로컬 정적 서버로 서빙하고 Playwright로 phone 크기(393×700) portrait 뷰포트에서 `flipPrev()`를 직접 호출해 30~50ms 간격으로 스크린샷을 찍어 프레임 단위로 직접 눈으로 비교했다. **~200ms 동안 화면에 아무 변화도 없다가 갑자기 다음 페이지로 툭 튀어넘어가는 것**을 확인 — forward(같은 방식으로 찍은 `flipNext()`)는 0ms부터 즉시 아름다운 접힘 애니메이션이 보이는 것과 뚜렷이 대비됨.
  - **원인 추적**: 스크린샷만으로는 이유를 알 수 없어서, 애니메이션 중인 페이지 요소의 실제 인라인 `style`(`transform: translate3d(...)`)을 매 프레임 직접 읽어 forward/backward를 수치로 비교했다. **backward flip 중인 페이지가 `translate3d(-450px, ...)` 근방 — 즉 뷰포트(393px 폭) 밖 수백 px 지점에 그려지고 있었다** — `#book-stage`의 `overflow:hidden`에 의해 완전히 잘려서 안 보였을 뿐, 애니메이션 자체는 정상적으로 돌고 있었던 것. 애니메이션이 끝나면 `turnToPrevPage()`가 최종 상태를 다시 그리면서 갑자기 나타나는 것처럼 보인 것이었다.
  - **근본 원인 2곳**(라이브러리 소스를 더 깊이 추적):
    1. `Flip.flipPrev()` — 합성 시작 좌표가 `x:10`으로, `flipNext()`가 `this.render.getRect().left`를 더하는 것과 짝이 안 맞았다. landscape는 `rect.left`가 대략 0이라 안 드러났지만, portrait(`size:"fixed"`)는 `Render.calculateBoundsRect()`가 `rect.left`를 깊은 음수로 잡아서(왼쪽 페이지를 화면 밖으로 미리 밀어두는 좌표계라서) 문제가 된다. `rect.left + (portrait ? pageWidth : 0)`을 더하도록 고쳤다 — 다만 실측해보니 이건 시각적 증상의 주된 원인은 아니었다(경계 사례에서만 차이 남).
    2. **`Render.convertToGlobal()` — 진짜 주범.** BACK 방향일 때 local x를 `boundsRect` 중심 기준으로 좌우 반전시켜 "boundsRect의 왼쪽 절반"에 매핑하는데, landscape에선 왼쪽 절반도 화면에 보여서 맞지만, portrait에선 그 왼쪽 절반이 정확히 위 1번에서 언급한 "화면 밖으로 밀어둔 숨겨진 영역"이다 — 그래서 반전 공식을 그대로 쓰면 애니메이션 중인 페이지가 항상 화면 밖에 그려진다. portrait에서는 FORWARD와 같은 "보이는 절반" 매핑을 쓰되, `FlipCalculation.getActiveCorner()`가 BACK일 때 반환하는 `rect.topRight`가 자기 정의상 이미 `pageWidth`만큼의 오프셋을 내장하고 있어서 FORWARD의 "+pageWidth"를 또 더하면 중복이 되므로, portrait+BACK은 `pos.x + rect.left`(FORWARD의 `pos.x + rect.left + pageWidth`에서 pageWidth 하나를 뺀 형태)로 고쳤다.
    3. (참고: `HTMLPage.drawSoft()`의 클립 폴리곤 반전 공식도 같은 종류 아닌가 의심해서 한 번 손댔었는데, 실측해보니 오히려 폴리곤 좌표가 훨씬 더 깨져서(수백 px 마이너스) 바로 되돌렸다 — `state.position`이 이미 `topRight` 기준이라 이 공식까지 바꾸면 이중 보정이 되는 것으로 보임. 이 부분은 건드리지 않는 게 맞았다.)
  - **검증**: 패치 전/후 실제 벤더 파일에서 `flippingPage`의 `translate3d` x값을 30ms 간격으로 직접 로깅해서, 패치 후 backward가 forward와 비슷한 범위(대략 ±150~300px, 뷰포트와 계속 겹치는 범위)로 스윕하는 것을 수치로 확인. 스크린샷도 재확인: 이제 t50~t200ms 사이에 실제로 두 페이지 텍스트가 겹쳐 보이는(커얼 전환 중 흔한 모습) 연속적인 프레임들이 나오고, forward의 동급 프레임에도 비슷한 텍스트 겹침 아티팩트가 있어 backward만의 문제가 아님을 확인(단순 텍스트라 진짜 커얼 그래픽처럼 안 보일 뿐, 둘 다 같은 종류의 겹침). next→next→prev→prev 4회 왕복 스트레스 테스트, 그리고 landscape 모드(넓은 뷰포트)에서 flipPrev/flipNext 재검증 — 둘 다 회귀 없음, DOM 깨끗함, 콘솔 에러 없음. 소스 diff로 원본 npm 패키지 대비 정확히 의도한 4곳(`PageCollection.ts`, `Render/HTMLRender.ts`, `Flip/Flip.ts`, `Render/Render.ts`)만 바뀌었음을 재확인.
  - `js/vendor/page-flip.browser.js` 상단 주석을 4개 패치 전부 설명하도록 다시 씀.
  - `CACHE_VERSION` `v31`→`v32`.
  - **여전히 남은 한계**: 이번에도 이 환경에서 dev 로그인으로 실제 뷰어를 열어 눈으로 보는 end-to-end 검증은 못 했다(CDN 차단은 여전함) — 이번엔 실제로 커밋되는 정확히 그 파일을 로컬 서버로 서빙하고 실제 phone 크기 뷰포트로 프레임 단위 스크린샷+좌표 실측까지 했으니 이전 라운드들보다 훨씬 강한 근거가 있지만, 그래도 사용자가 Vercel 프리뷰에서 실기기로 최종 확인해야 한다.
- ✅ **완료·커밋됨 (2026-08-21)**: 위 항목을 Vercel 프리뷰로 확인한 사용자가 다시 "애니메이션이 굉장히 어색하다, 한 페이지 모드에서 확인해달라"고 재요청 — 지난 라운드의 좌표 수정으로 페이지 자체는 화면 안으로 들어왔지만, **그림자 요소는 여전히 화면 밖에 있었고, 페이지 내용도 깔끔한 접힘이 아니라 두 페이지 텍스트가 뒤섞여 보이는 상태**였다. 원인을 한 곳 더 깊이 추적해서 세 번째 라운드로 고쳤다.
  - **재현·진단**: `.stf__outerShadow`/`.stf__innerShadow`(그림자 DOM 요소)의 실제 `transform: translate3d(...)`를 직접 읽어보니 backward일 때 x가 음수(-108~-185px 근방)로 뷰포트 밖 — 지난 라운드에서 페이지 자체는 고쳤지만 그림자는 별도 코드 경로라 안 고쳐져 있었다.
  - **근본 원인 재정리**: `Render.convertToGlobal()`의 BACK 분기(좌우 반전 공식)를 지난 라운드에 "portrait+BACK이면 pageWidth를 한 번만 반영"하도록 고쳤었는데, 이 보정치가 **페이지 위치용 좌표(`FlipCalculation.getActiveCorner()`/`getBottomPagePosition()`, BACK일 때 `rect.topRight`/`{pageWidth,0}` — 자기 정의 자체에 pageWidth 오프셋이 내장됨)에만 필요**했지, **그림자 시작점(`getShadowStartPoint()`, 페이지 자신의 [0,pageWidth] 범위 안의 교차점이라 애초에 그 오프셋이 없음)에는 불필요**했다 — `convertToGlobal`은 이 두 종류의 점을 구분 못 하고 똑같이 취급해서, 페이지는 맞았지만 그림자는 여전히 틀렸다.
  - **수정**: 관심사를 분리했다. `Render.convertToGlobal()`은 다시 단순하게 되돌려서 "portrait면 방향과 무관하게 항상 FORWARD와 같은 슬롯에 매핑"만 하도록(어떤 종류의 점인지 몰라도 되게) 만들고, pageWidth 오프셋 보정은 그 오프셋을 실제로 갖고 있는 값을 만들어내는 **`Flip.do()`**(매 프레임 `getActiveCorner()`/`getBottomPagePosition()`을 `setPosition()`에 넘기는 바로 그 지점)로 옮겨서 portrait+BACK일 때만 x에서 pageWidth를 빼도록 했다. 이러면 페이지 위치(변환된 값 사용)와 페이지 클립 폴리곤(변환 안 된 원본 `state.position`을 직접 쓰는 `drawSoft()`) 둘 다 자동으로 일관되게 보정되고, 그림자는 애초에 이 함수를 안 거치니 손대지 않아도 저절로 맞다.
  - **검증**: 그림자 요소의 `translate3d` x가 이제 forward와 비슷한 범위(200~290px 대)로 나오는 것을 확인했고, 프레임별 스크린샷에서 실제로 대각선 접힘선 + 그림자 그라데이션이 있는 **깨끗한 커얼 모양**(이전의 뒤섞인 텍스트가 아니라)이 나오는 것까지 확인했다. landscape 재검증(4개 요소, 정상 스프레드 인덱스 이동), next/prev 왕복 스트레스 테스트(깨끗한 DOM) 모두 통과. 원본 npm 소스 대비 정확히 의도한 4개 파일(`PageCollection.ts`, `HTMLRender.ts`, `Flip.ts`, `Render.ts`)만 바뀌었음을 diff로 재확인 — `HTMLPage.ts`는 이번에도 다음에도 안 건드림(중간에 한 번 잘못된 시도로 손댔다가 바로 되돌림).
  - `js/vendor/page-flip.browser.js` 상단 주석을 5개 변경사항 전부(3라운드에 걸친 조사 과정 요약 포함)로 다시 씀.
  - `CACHE_VERSION` `v32`→`v33`.
  - **여전히 남은 한계**: 동일 — 이 환경에서 dev 로그인 브라우저 검증은 여전히 불가능. 실제로 커밋되는 파일을 로컬 서버로 서빙해서 그림자 위치·클립 폴리곤 수치·프레임별 스크린샷까지 확인했지만, 최종 판단은 사용자가 Vercel 프리뷰에서 실기기로 해야 한다.
- ✅ **완료·커밋됨 (2026-08-21)**: 사용자가 실제 기기에서 녹화한 화면 녹화 영상(`.mp4`)을 처음으로 받아서 분석 — 지금까지는 스크린샷/DOM 수치로만 검증해왔는데, 이번엔 진짜 사용 장면을 프레임 단위로 볼 수 있었다. **좋은 소식과 새로 발견한 문제가 둘 다 있었다.**
  - **영상 분석 방법**: 이 환경엔 `ffmpeg`가 기본 설치돼 있지 않아서 `apt-get install -y --no-install-recommends ffmpeg`로 설치(일부 무관한 패키지가 미러 문제로 실패했지만 ffmpeg 자체는 정상 설치됨). `ffmpeg -vf fps=N`으로 프레임을 PNG로 뽑고, `tile=` 필터로 콘택트시트도 만들어서 전체 흐름을 빠르게 훑은 뒤, 의심스러운 구간만 30fps로 다시 뽑아 프레임 단위로 확인하는 방식으로 진행했다.
  - **좋은 소식**: 영상에 담긴 두 번의 실제 "이전 페이지" 전환을 프레임 단위로 뜯어본 결과, **둘 다 진짜로 부드러운 ~1000ms 커얼 애니메이션이 재생되고 있었다** — 대각선 접힘선과 그림자 그라데이션이 정상적으로 스윕하고, 정확한 페이지에 정확히 안착했다. 처음엔 한쪽 전환이 "200ms간 정지 후 순간이동"처럼 보여서 진짜 버그인가 했는데, 6fps 콘택트시트로 전체 구간을 다시 보니 그냥 내가 너무 늦은 시점부터 샘플링해서 애니메이션 후반부만 보고 "정지 상태"라고 오판한 것이었다 — 실제로는 처음부터 끝까지 정상 재생되고 있었다.
  - **새로 발견한 문제**: 다만 그 정상적으로 보였던 전환 중 하나에서, 애니메이션 시작 후 처음 ~100~150ms 동안 **회전된 글자 조각들이 화면 여기저기 흩뿌려져 보이는 글리치**를 프레임 단위로 확인했다(예: "이러면 이열치열", "살수의 얼굴이" 같은 현재 페이지 자신의 문장 파편들이 기울어진 채 왼쪽 영역에 겹쳐 보임). 이후 프레임에서는 깨끗해지고 정상적으로 커얼이 진행됨.
  - **원인 추정과 시도한 재현**: `HTMLPage.newTemporaryCopy()`가 `this.temporaryCopy`가 `null`일 때만 새로 복제하고, 아니면 **기존 복사본(과 그 인라인 스타일)을 그대로 재사용**한다는 걸 소스에서 확인했다 — 그런데 `Flip.flip()`은 `start()`로 flippingPage/bottomPage를 지정한 뒤 바로 `animateFlippingTo()`(requestAnimationFrame 기반)를 호출할 뿐, 그 사이에 `do()`(매 프레임 area/position/angle을 계산해서 실제로 그리는 함수)를 한 번도 동기 호출하지 않는다 — 즉 "첫 실제 프레임이 그려지기 전"에 오래된 상태가 잠깐 노출될 수 있는 진짜 빈틈이 있다. 이 가설로 Playwright에서 여러 각도로 재현을 시도했다: ① 같은 페이지에서 앞으로→뒤로→다시 앞으로 넘겨서 임시 복사본을 재사용시키기, ② 세션의 진짜 첫 번째 플립(재사용 자체가 불가능한 경우)까지 — **둘 다 격리된 harness에서는 재현되지 않았다.** 실제 앱은 자체 호스팅 웹폰트(고운돋움/노토 산스·세리프 KR)를 쓰는데 내 harness는 시스템 폰트를 쓴다는 등, 재현 안 된 이유는 여러 가지 있을 수 있다.
  - **적용한 조치**: 재현하진 못했지만, 위에서 확인한 "진짜 빈틈"(do()가 첫 프레임 전엔 한 번도 안 불림) 자체는 실재하는 문제라 방어적으로 고쳤다 — `Flip.flip()`에서 `animateFlippingTo()` 호출 직전에 애니메이션의 시작 지점으로 `do()`를 동기 호출 한 번 추가. orientation/방향 조건 없이 모든 경우에 적용(정당한 일반 버그 수정이라 portrait+BACK에 한정할 이유가 없음). **다만 이건 재현으로 검증된 근본 수정이 아니라, 확인된 빈틈을 방어적으로 닫은 것** — 실제 배포 환경에서 이 글리치의 진짜 트리거는 여전히 불확실하다.
  - **검증**: 이 수정 적용 전/후로 A/B 비교(첫 페인트 시점의 flippingPage 클립 패스 값)를 했으나 내 harness에선 차이가 감지되지 않았다(둘 다 이미 정상 값) — 즉 이 harness로는 수정의 효과를 직접 증명하지 못했다. 대신 기존 회귀 스위트(왕복 스트레스 테스트, landscape 재검증, 그림자 위치 확인)는 전부 그대로 통과 확인. 소스 diff로 원본 대비 정확히 의도한 4개 파일(`PageCollection.ts`, `HTMLRender.ts`, `Flip.ts`, `Render.ts`)만 바뀌었음을 재확인 — `HTMLPage.ts`는 여전히 안 건드림.
  - `js/vendor/page-flip.browser.js` 상단 주석에 이번 6번째 변경사항(재현 실패 사실까지 솔직하게) 추가.
  - `CACHE_VERSION` `v33`→`v34`.
  - **다음에 이어서 볼 사람에게**: 이 글리치가 매번 재현되는지 가끔인지, 어떤 조건(연속 클릭, 특정 페이지, 첫 플립 여부 등)에서 나오는지 사용자에게 더 물어봐서 좁혀볼 가치가 있다. 실제 웹폰트 로딩 타이밍이나 실기기 성능 차이가 관련 있을 가능성도 열어두고 있음.
- ✅ **완료·커밋됨 (2026-08-21)**: 위 글리치의 **진짜 근본 원인을 찾아 고쳤다** — 바로 위 항목(6번째 변경, 방어적 `do()` 동기 호출)은 재현조차 못한 채 나간 미봉책이었는데, 이번엔 사용자가 "다음장>이전장>다음장>이전장" 시나리오를 녹화한 두 번째 화면 녹화 영상을 보내면서 "다음장과 완전히 동일한 애니메이션에 방향만 다른 걸 원한다"고 구체적으로 요청 — 이걸 ffmpeg로 프레임 단위 분석해서 **처음으로 이 글리치를 이 환경에서 직접 재현하는 데 성공**했다.
  - **분석 과정**: `ffmpeg -vf fps=4/8/20`으로 저해상도 콘택트시트(전체 흐름 파악) → 의심 구간을 고해상도(책 패널만 크롭, `crop=in_w*0.42:in_h:0:0`)로 재추출해서 프레임별 정밀 비교. 처음엔 저해상도 200~300px 썸네일에서 "이전 페이지 전환 중간에 완전히 다른(훨씬 짧은) 페이지 내용이 잠깐 나타났다가 원래 페이지로 되돌아간다"는 걸로 잘못 읽었는데(픽셀 단위 컬럼-밝기 분석이 이동 중인 그림자 위치를 콘텐츠 변화로 오인), 고해상도로 다시 보니 그건 저해상도 OCR성 오독이었고 실제로는 **회전되고 흩어진 텍스트 조각들이 애니메이션 시작 직후 첫 100~150ms 동안 화면 여기저기 나타나는 글리치**(바로 위 5번째 항목에서 사용자가 신고했던 것과 동일 종류)였다. 그리고 이번엔 next→prev→next→prev 네 번의 전환 전부를 대칭적으로 비교해서, **이게 가끔이 아니라 portrait에서 매번, 100% 재현되는 backward flip 전용 버그**라는 걸 확실히 확인했다(forward는 첫 번째든 두 번째든 항상 깨끗했고, backward는 첫 번째든 두 번째든 항상 글리치가 있었다 — "직전에 어떤 역할을 했던 페이지인지"와 무관).
  - **이 환경에서 처음으로 실제 재현 성공**: 실제 커밋된 `js/vendor/page-flip.browser.js`를 로컬 정적 서버로 서빙하고, Playwright로 `flipPrev()` 호출 직후 8ms 간격으로 모든 활성 `.page` 요소의 인라인 스타일(`translate3d`/`rotate`/`clip-path`)을 직접 덤프해서 forward와 나란히 비교 — flippingPage(위에서 말리는 임시 복사본)의 clip-path 폴리곤 좌표가 첫 여러 프레임 동안 수백 px 범위(양쪽 부호 다)로 나오는 반면 bottomPage(밑에서 드러나는 실제 이전 페이지)는 깨끗한 것을 수치로 확인 — 실기기 영상에서 본 "회전된 조각들"과 정확히 일치하는 증거.
  - **진짜 원인**: 5번째 항목(`Flip.do()`, portrait+BACK일 때 `getActiveCorner()`/`getBottomPagePosition()`의 x에서 pageWidth를 빼는 보정)이 **절반만 맞았다**. `getBottomPagePosition()`의 BACK 값 `{pageWidth,0}`은 페이지-로컬 좌표라 pageWidth를 빼면 정확히 FORWARD의 `{0,0}`과 같아져서 우연히 딱 맞았지만, `getActiveCorner()`의 BACK 값(`rect.topRight`)은 그렇지 않았다 — `rect`는 폴딩 중인 페이지의 *회전된* 경계 사각형이라, `topRight`와 `topLeft`는 x만 pageWidth만큼 다른 게 아니라 y까지 완전히 다른(실측: 수백 px 차이) 별개의 꼭짓점이었다. x만 보정하면 `state.position`(flippingPage의 배치 위치)이 자기 자신의 clip-path 폴리곤(`getFlippingClipArea()`, 방향과 무관하게 항상 `rect.topLeft`를 기준점으로 삼음)과 서로 다른 기준점을 쓰게 되고, 그 결과 그려지는 클립 도형이 완전히 어긋났다. **별개로 하나 더**: `HTMLPage.drawSoft()`(클립-패스 폴리곤 문자열을 실제로 만드는 함수)도 BACK이면 항상 x를 미러링하는 자체 공식(`-p.x + position.x`)을 갖고 있었는데, `Render.convertToGlobal()`(5번째 항목에서 고쳤던 것)과는 별개의 코드 경로라 그때 안 고쳐졌던 채로 남아있었다 — portrait에서 이 미러링은 클립 좌표를 전부 `-pageWidth` 근방(보이는 0~pageWidth 범위 밖)으로 보내버렸다.
  - **수정**: 두 곳.
    1. `HTMLPage.drawSoft()` — `Render.convertToGlobal()`과 같은 원칙: portrait면 방향과 무관하게 항상 FORWARD 공식(미러링 없음)을 쓰도록 미러링 조건에 `orientation !== PORTRAIT`를 추가.
    2. `Flip.do()` — `getActiveCorner()`/`getBottomPagePosition()`의 BACK 값을 수치 보정(pageWidth 빼기)하는 대신, portrait에서는 이 값들 자체를 아예 안 믿고 FORWARD가 쓰는 것과 정확히 같은 기준점(flippingPage는 `rect.topLeft`, bottomPage는 `{0,0}`)을 직접 대입하도록 바꿨다 — `state.position`과 클립 폴리곤이 항상 같은 기준점을 공유하게 되어 다시 정확히 들어맞는다.
  - **검증**: 실제 커밋되는 `js/vendor/page-flip.browser.js`를 대상으로, (1) 같은 8ms 간격 스타일 덤프에서 flippingPage의 clip-path가 이제 forward와 비슷한 작은 범위 값으로 나오는 것을 수치 확인, (2) 16ms 간격 스크린샷으로 전체 backward 애니메이션을 다시 훑어서 회전/흩어짐 없이 작은 모서리 삼각형이 매끄럽게 전체 스윕으로 자라나는 깨끗한 커얼을 확인, (3) 6회 연속 next/prev 왕복 스트레스 테스트 — 모든 프레임에서 clip-path 좌표가 1000px 넘는 경우 없음, 최종 DOM 깨끗함(보이는 페이지 1개, 임시 복사본 누수 없음), (4) landscape 모드 재검증 — 모든 수정이 portrait 전용 분기라 영향 없음, forward/backward 둘 다 여전히 3개 활성 요소, 회귀 없음. 소스 diff로 원본 npm 패키지 대비 정확히 의도한 5개 파일(`PageCollection.ts`, `HTMLRender.ts`, `Flip.ts`, `Render.ts`, 그리고 이번에 새로 추가된 `HTMLPage.ts`)만 바뀌었음을 재확인.
  - `js/vendor/page-flip.browser.js` 상단 주석을 8개 변경사항(a~h)으로 다시 정리 — 5번째 항목(구 `Flip.do()` 보정)이 절반만 맞았던 경위와, 이번 h) 항목이 그 나머지 절반을 어떻게 대체했는지까지 명시.
  - `CACHE_VERSION` `v34`→`v35`.
  - **여전히 남은 한계**: 이번에도 dev 로그인으로 실제 뷰어를 여는 end-to-end 브라우저 검증은 이 환경에서 불가능(CDN 차단 여전함). 하지만 이번엔 이전 라운드들과 달리 **실제 사용자 녹화 영상에서 시작해서 그 안의 정확한 시각적 증상(회전된 텍스트 조각)을 프레임 단위로 확인하고, 그 증상을 이 환경에서 실제로 재현한 뒤, 재현된 증상이 수정 후 사라지는 것까지 확인**했다는 점에서 지금까지 중 가장 강한 근거를 갖고 있다. 그래도 최종 판단은 사용자가 실기기에서 직접 확인해야 한다 — next/prev를 빠르게 여러 번 연달아 눌러보면서 이번엔 다음/이전이 정말 "완전히 동일한 느낌"인지 봐달라고 요청할 것.
- 🔄 **작업 중 — `portrait-flip-rewrite` 브랜치 (2026-08-24)**: 위 항목까지 총 9라운드에 걸쳐 `js/vendor/page-flip.browser.js`를 패치했는데도, 사용자가 새로 보내온 화면 녹화(`Bookify 2026-08-24 11-45-12.mp4`)에서 **글자 겹침 글리치가 여전히 남아있는 것을 재확인**했다. 이번엔 원인을 한 곳 더 파는 대신, 사용자가 먼저 제안한 방향("한 라이브러리를 세로/가로 공용으로 억지로 쓰지 말고, 한 페이지용과 두 페이지용을 아예 분리하면 되지 않냐")을 받아들여 **세로(portrait, 모바일 한 페이지) 모드를 StPageFlip에서 완전히 분리**했다.
  - **왜 라이브러리 패치를 더 안 파고 분리로 갔는지**: 이번 라운드에서 CSS 컴포지팅 힌트(`will-change: transform`, `backface-visibility: hidden`)를 실험적으로 제거해봤지만 — 실기기 녹화로 재확인한 결과 **효과 없음**(styles.css에 실험 경위와 "원인이 아니었다"는 결론을 주석으로 남기고 원상복구, 별도 커밋 없이 같은 브랜치에서 revert). 지금까지 검증한 모든 것(클립 영역 기하학, 실측 화면 위치, 브라우저가 실제로 파싱한 clip-path, 매 프레임 DOM 요소 전체 목록)이 다 정확하다고 나왔는데도 실제 페인트만 깨지는 것까지 확인된 상태라 — **JS/DOM 레벨에서 관찰 가능한 어떤 값도 문제가 없는데 결과만 깨지는, 이 이상 패치로는 못 잡을 가능성이 높은 결함**이라는 결론을 내렸다. 근본적으로 StPageFlip이 세로 모드를 "2페이지 스프레드의 왼쪽 페이지를 화면 밖으로 숨겨서 1페이지처럼 보이게" 만드는 구조 자체가 원흉이라고 보고, 그 구조를 아예 안 타는 별도 구현으로 우회하는 쪽을 선택했다.
  - **백업**: 재작업 시작 직전 커밋에 주석 달린 태그 `pre-portrait-flip-rewrite`를 남기고, `main`은 전혀 건드리지 않은 채 별도 브랜치 `portrait-flip-rewrite`에서만 작업했다(`git push -u origin portrait-flip-rewrite`) — 사용자가 명시적으로 요구한 "되돌릴 수 있게 백업 확실히" 조건.
  - **새 아키텍처**: 새 파일 `js/portrait-flip.js` — 세로 모드 전용 페이지 넘기기 애니메이션. StPageFlip을 아예 안 쓰고, **`translate3d`(이동)만 쓰는 "밀어내기" 방식**으로 완전히 새로 구현했다:
    - 아래층(`revealing`, 새로 드러날 페이지)은 처음부터 제자리에 고정, 위층(`leaving`, 지금 보이던 페이지)만 `sign`(다음=-1, 이전=+1) 방향으로 `translate3d`로 미끄러져 나간다. `clip-path`나 `rotate`를 전혀 안 써서 — 지금까지 9라운드 내내 문제의 공통분모였던 "매 프레임 바뀌는 clip-path + rotate 조합" 자체가 구조적으로 존재하지 않는다.
    - 앞/뒤 방향이 `sign` 부호 하나만 다른 완전히 같은 코드라, "다음 페이지랑 느낌이 다르다"는 게 애초에 불가능하다.
    - `activeAnimation`으로 겹쳐 눌림 방지(`isPortraitFlipAnimating()`), `requestAnimationFrame` 실패/정지에 대비한 `setTimeout` 안전장치(탭이 백그라운드로 가서 rAF가 멈춰도 애니메이션이 끝까지 진행되게), 그리고 `buildFlipBook`이 리사이즈 등으로 `#book-stage`를 통째로 갈아끼우기 직전에 진행 중이던 애니메이션의 rAF/타이머만 조용히 정리하는 `cancelPortraitFlip()`(onDone은 호출 안 함 — 새 pageFlip이 만들어지며 현재 페이지를 어차피 다시 그려주므로).
  - **가로(PC, 2페이지 스프레드) 모드는 전혀 안 건드림** — 원래부터 문제없던 StPageFlip의 `flipNext()`/`flipPrev()`를 그대로 쓴다. `js/reader.js`의 `jumpToPrevPage()`/`goToNextPage()`가 `isSinglePageMode`(새 모듈 상태, `buildFlipBook()`에서 세팅) 값으로 분기해서 세로/가로 각각 다른 엔진을 부른다.
  - **`reader.js` 연동**: `finishManualPageTurn(targetGlobal)` 신설 — `pageFlip.on('flip', ...)` 핸들러가 라이브러리 자체 애니메이션 뒤에 하던 일(진행상황 저장, 페이지 인디케이터 갱신, 창 재정렬, 화면-항상-켜짐 타이머 리셋)을 애니메이션 완료 콜백(`onDone`)에서 그대로 재현 — `isShiftingWindow`로 감싸서 `pageFlip.turnToPage()`가 쏘는 합성 `'flip'` 이벤트가 중복 실행되지 않게 막는다(`maybeShiftPageWindow`가 이미 쓰던 것과 같은 패턴). `pageFlip.destroy()`가 일어나는 두 지점(`resetReaderSession()`, `buildFlipBook()`) 모두에서 그 직전에 `cancelPortraitFlip()`을 호출하도록 배선했다.
  - **검증(이 환경 한정, 실기기 아님)**: 로컬 정적 서버 + `127.0.0.1` 프록시 캐싱 우회(사용법은 아래 환경 메모 참고) + dev 로그인으로, phone 크기(393×700) 뷰포트에서 실제 뷰어를 열어 직접 확인했다 — 이번엔 처음으로 **진짜 뷰어 화면에서** 검증할 수 있었다(CDN 차단 문제가 이번엔 없었음, 정확한 이유는 불명). (1) forward/backward 각각 단발 실행 시 오버레이가 정확히 하나 생겼다가 완료 후 깨끗이 제거되고 페이지 내용이 정확히 바뀜, (2) mid-animation에 `leaving`/`revealing` 두 레이어의 `transform`/`boxShadow`를 직접 읽어 방향별 부호가 대칭인 것 수치 확인, (3) 빠른 연속 탭(다음×2, 이전×2, 다음, 이전) 6연타 스트레스 테스트 — 매번 오버레이 정확히 1개, 최종 DOM 깨끗함(`.page` 18개 그대로, 누수 없음), (4) 애니메이션 도중 3연타(같은 방향) — `isPortraitFlipAnimating()` 가드가 정확히 작동해 오버레이가 항상 1개로 유지되고 딱 1페이지만 넘어감, (5) 애니메이션 도중 강제 `resize` 이벤트 발생시켜 `buildFlipBook` 재빌드를 유도 — `cancelPortraitFlip()` 배선 덕에 콘솔 에러 없이 깨끗하게 정리됨, (6) 데스크톱 크기(1280×800)로 리사이즈해 가로 모드 재검증 — `.portrait-flip-overlay`가 전혀 안 생기고(StPageFlip 자체 `flipNext()` 경로를 그대로 탐) 페이지 내용도 정상적으로 바뀜, 회귀 없음. 콘솔 에러 전 구간 없음.
  - `sw.js`의 `PRECACHE_URLS`에 `./js/portrait-flip.js` 추가, `CACHE_VERSION` `v42`→`v43`.
  - **아직 안 한 것 / 남은 일**: 이번엔 이 환경에서 실제 뷰어로 검증까지 했지만, 그래도 **이 환경의 Browser pane 자동화는 실기기와 근본적으로 다른 렌더링 환경**이라 최종 판단은 여전히 사용자 몫이다. 아직 `main`에 머지 안 함 — `portrait-flip-rewrite` 브랜치에만 커밋/푸시했고, 사용자가 실기기(Vercel 프리뷰 등)에서 다음/이전 넘기기가 실제로 깔끔하고 대칭적으로 보이는지 확인한 뒤에 머지할 것.
- 🔄 **작업 중 (2026-08-24)**: `portrait-flip-rewrite`가 이후 `main`에 머지됐지만(PR #3), 사용자가 그 결과물의 **밀기(translate3d slide) 방식 자체가 원하던 게 아니었다**고 피드백 — "기본 페이지 플립(커얼) 방식을 가져가고 싶다"고 명시적으로 요청. 대화로 근본 원인을 다시 짚어보니, StPageFlip이 세로 모드를 "2페이지 스프레드의 왼쪽 페이지를 화면 밖으로 숨겨서 1페이지처럼 보이게" 만드는 구조 자체는 맞지만, **9라운드 패치로도 못 잡은 마지막 겹침 버그는 좌표가 다 검증된 뒤에도 남아있었다는 점에서, 꼭 그 "숨긴 절반" 구조 때문이라는 보장은 없었다**(한 번도 진짜 1페이지 컨테이너로 시도해본 적이 없었음) — 사용자가 "정말 1페이지만 보이게 구현하면 해결되지 않냐"고 제안, 시도해보기로 결정.
  - **조사**: 원본 `StPageFlip` v2.0.7 TypeScript 소스(`FlipCalculation.ts`/`Helper.ts`/`HTMLPage.ts`/`HTMLRender.ts`/`Flip.ts`/`Render.ts`, GitHub에서 직접 확인)를 읽어보니 핵심 발견 — **커얼 기하학 계산(`FlipCalculation`) 자체는 원래도 완전히 "페이지 한 장짜리" 로컬 좌표계(0..pageWidth, 0..pageHeight) 안에서만 동작**했다. "숨긴 절반" 문제는 오직 그 로컬 좌표를 2배 너비 `boundsRect`(`rect.width = pageWidth*2`, 항상 이 값) 안의 올바른(숨겨지지 않은) 절반에 매핑하는 상위 레이어(`Render.convertToGlobal()`, `HTMLRender`의 그림자 함수들)에서만 생겼다. 즉 진짜 1페이지짜리 컨테이너를 새로 만들면 그 상위 매핑 자체가 필요 없어져서, 지금까지 5라운드 넘게 잡아온 좌표 버그 클래스가 구조적으로 원천 차단된다는 근거를 확보.
  - **구현**: `js/portrait-flip.js`를 다시 갈아엎어서, `FlipCalculation`의 각도/클립영역 계산과 `HTMLPage.drawSoft()`의 clip-path+rotate 조합을 이식했다(코너는 항상 TOP만 — `pageFlip.flipNext()`/`flipPrev()`를 코너 인자 없이 부르면 기본값이 TOP이라 이 앱이 실제 쓰던 것과 동일, BOTTOM 분기는 안 씀). BACK 방향의 원본 미러링 공식도 임시방편("항상 FORWARD처럼 취급") 없이 그대로 이식 — 진짜 1페이지 컨테이너에는 "잘못된 절반"이 아예 없으니 원본 그대로도 안전하다는 판단. 그림자는 원본의 정교한 다각형 계산 대신, `flippingLayer`와 정확히 같은 clip-path/transform을 재사용하는 단순 그라데이션 오버레이로 근사(버그가 있던 곳은 항상 "페이지 내용물"의 클립/좌표였지 그림자가 아니었으므로, 위험이 큰 부분엔 원본 수학을 충실히 이식하고 위험이 적은 장식은 단순화).
  - ⚠️ **검증 중 실제로 잡은 버그**: 초기 구현에서 안전망 배경 레이어(`background`, 동적 클립 두 장 사이에 빈틈이 생겨도 뭔가 보이게 까는 정적 레이어)에 **드러날(revealing) 페이지 내용을 깔았는데, 기하 계산을 직접 돌려보니 애니메이션 초반(t≈0.02)엔 동적 클립 두 장이 전체 페이지의 2.4%만 덮고 나머지 97.6%는 이 배경이 보여준다는 걸 발견** — 그 시점엔 사실상 아무것도 안 넘어갔으니 그 큰 영역은 원래 넘어가는(leaving) 페이지 내용이어야 정상인데 반대로 깔려 있었다(원본 StPageFlip의 `drawRightPage()`가 항상 "아직 안 넘어간 현재 페이지"를 보여주는 고정 배경 역할을 하는 것과 같은 이치). `leaving` 내용으로 수정.
  - **검증 방법(스크린샷 불가 환경 — 아래 참고)**: (1) t=0.01~0.99 사이 98개 지점 × 양방향으로 `flipArea`/`botArea` 다각형의 겹침 비율을 Sutherland-Hodgman 클리핑으로 직접 계산 — 최대 겹침 비율 6.4e-16(부동소수점 잡음 수준), NaN/실패 0건. (2) 같은 지점들을 point-in-polygon으로 문자 그리드(ASCII 아트)에 래스터화해서 실제로 "봄" — t=0.15에서 작은 모서리 삼각형, t=0.5에서 대각선으로 깔끔히 반씩, t=0.85에서 대부분 새 페이지 + 작은 커얼 조각으로, 어느 지점에서도 겹침 문자 없이 물리적으로 말이 되는 모양 확인. (3) **가장 중요한 검증**: 이 자동화 브라우저는 여전히 `document.hidden=true`라 진짜 `requestAnimationFrame`이 멈춰있어서(과거 라운드들과 동일한 환경 제약, 위쪽 "환경 메모" 참고) 실제 애니메이션이 화면에 페인트되는 걸 눈으로 볼 수 없었다 — 대신 `requestAnimationFrame`을 `setTimeout(fn,0)`으로 임시 패치해서 **실제 실행 중인 `playPortraitPageTurn()` 코드**(재구현이 아니라 진짜 배포되는 코드)가 만들어내는 매 프레임 `clip-path`/`transform` 값을 `MutationObserver`로 743개 프레임 그대로 수집 — forward/backward 둘 다 NaN·Infinity·빈 clip-path 0건, 페이지 인디케이터 정상 이동. (4) 연타 스트레스 테스트(다음×2/이전×2/다음/이전, 애니메이션 도중 겹쳐 누르기 포함) — `isPortraitFlipAnimating()` 가드 정상 동작, 콘솔 에러 0건. **실제 눈으로 보는 화면 검증은 이 환경의 rAF 제약 때문에 못 했다 — 사용자가 실기기에서 최종 확인 필요.**
  - `sw.js`의 `CACHE_VERSION`을 `v43`→`v44`로 올림. `main` 브랜치에 직접 커밋(별도 브랜치 안 씀 — 이미 머지된 `portrait-flip.js`를 그 자리에서 교체하는 작업이라).
  - **환경 메모(다음에 참고)**: 이 세션에서 재확인한 것 — 로컬 정적 서버 프리뷰의 캐싱-프록시 문제는 `127.0.0.1`/`localhost` 자체가 항상 stale인 게 아니라, **특정 브라우저 탭(tabId)이 예전에 열렸던 상태를 물고 있을 때만** stale이었다 — 새 탭을 열고 `127.0.0.1`로 다시 이동하니 즉시 fresh한 콘텐츠가 나왔다(굳이 안 써본 새 호스트명으로 갈 필요는 없었음, 매번 그럴지는 불확실). 또한 이 세션에서 `computer`(클릭) 도구가 반복적으로 30초 타임아웃했지만 — `javascript_tool`로 `element.click()`이나 `dispatchEvent(new MouseEvent(...))`를 직접 실행하는 방식은 즉시 정상 동작했다. `computer` 클릭이 막히면 이 우회로 계속 진행할 것.
- ✅ **완료·커밋됨 (2026-08-24)**: 사용자가 위 커얼 재구현을 실기기에서 확인한 뒤 **"똑같은 문제가 있다"고 재확인** — 즉 StPageFlip 패치(9라운드) → 순수 translate3d 밀기 → 원본 커얼 수학을 진짜 1페이지 컨테이너에 이식, **서로 다른 세 가지 구현 전부가 이 환경에서는 "검증됨"으로 나왔는데 실기기에서는 매번 같은 종류의 글리치가 재발**하는 패턴이 확정됐다. 사용자 결정: 세로 모드의 페이지 넘김 애니메이션 자체를 완전히 포기하고, 애니메이션 없이 즉시 페이지가 바뀌는 대신 짧은 명암 깜빡임(flash)으로만 피드백을 준다 — 앞/뒤 방향 둘 다 동일하게. (2026-08-20에 딱 이 방식으로 시작했었다가 "진짜 애니메이션을 원한다"는 요청으로 이후 한 달 가까이 이어진 커얼 시도가 시작됐던 것과 정반대 방향으로 되돌아온 것.)
  - **`js/portrait-flip.js`를 통째로 비웠다**: `FlipCalculation` 이식, clip-path/rotate 조합, 그림자 근사, `translate3d` 밀기 등 지금까지의 애니메이션 코드를 전부 삭제하고 `flashPageTurn()` 함수 하나만 남겼다 — `#page-turn-flash`(새 DOM 엘리먼트, `#brightness-overlay`/`#book-loading-overlay`와 같은 패턴으로 `#book-stage` 밖에 위치)에 `.flash` 클래스를 뗐다 붙여서(강제 리플로우로 재생 재시작 보장) `--reading-text` 색의 0.16→0 opacity 펄스(`0.22s ease-out`)를 재생할 뿐, 페이지 콘텐츠 자체에는 어떤 `clip-path`/`rotate`/`transform`도 걸지 않는다 — 3번의 재구현 내내 문제의 공통분모였던 "매 프레임 바뀌는 시각적 변형" 자체를 다시는 안 쓰기로 한 것.
  - **`js/reader.js` 단순화**: `jumpToPrevPage()`/`goToNextPage()`의 `isSinglePageMode` 분기가 이제 `finishManualPageTurn(targetGlobal)`(진행상황 저장/인디케이터 갱신/창 재정렬 등, 기존 그대로) → `flashPageTurn()` 두 줄로 끝난다 — 비동기 콜백(`onDone`)도, 겹쳐 눌림 방지용 `isPortraitFlipAnimating()` 가드도, `cancelPortraitFlip()`(구 `resetReaderSession()`/`buildFlipBook()`의 정리 호출 2곳)도 전부 필요 없어져서 삭제됐다 — 페이지 전환이 이제 완전히 동기적이라 "애니메이션 도중 상태"라는 게 아예 존재하지 않는다. **부수 효과**: 연타해도 이제 한 번도 안 놓치고 전부 처리된다(예전엔 애니메이션 재생 중 들어온 탭은 조용히 무시됐음) — 스트레스 테스트로 확인(다음×3/이전×2/다음×1/이전×2 = 7번 입력 → 정확히 순입력 결과로 페이지 이동).
  - **가로(PC) 모드는 전혀 안 건드림** — `pageFlip.flipNext()`/`flipPrev()`를 여전히 그대로 쓴다.
  - `index.html`에 `#page-turn-flash` 엘리먼트 추가, `styles.css`에 그 스타일 + `@keyframes page-turn-flash-pulse` 추가.
  - **검증**: dev 로그인 + phone 뷰포트(375×812)로 실제 뷰어를 열어 (1) 다음/이전 각각 단발로 페이지 인디케이터가 **같은 호출 안에서 동기적으로** 즉시 바뀌는 것과 `.flash` 클래스가 동시에 붙는 것 확인, (2) 7연타 스트레스 테스트 — 입력 순서 그대로 정확히 반영, 콘솔 에러 0건, (3) 데스크톱 크기(1280×800)로 뷰포트를 새로고침 전에 미리 잡고 새로 로그인해서 진짜 가로(2페이지 스프레드, `#book-spine` 보임) 모드로 빌드되게 한 뒤 다음 페이지 이동 — `.flash` 클래스가 전혀 안 붙는 것(=세로 전용 코드가 가로에 영향 없음) 확인, 콘솔 에러 없음. ⚠️ 이 환경에서 뷰포트 리사이즈 자체는 실제 CSS 레이아웃엔 반영되지만, 앱의 `ResizeObserver` 기반 재빌드 로직(`scheduleFlipbookRebuild`, `js/reader.js`)이 안 뒤따라와서 `isSinglePageMode`가 옛 빌드 크기에 고정된 채 안 바뀌는 걸 발견했다 — 이건 내가 건드린 코드가 아니라 이 자동화 환경에서 `ResizeObserver`가 안 미덥게 도는 것으로 보이는 기존 동작(사용자에게는 실제 브라우저 리사이즈가 정상 작동하는지 별개로 확인 가치 있음) — 그래서 가로 모드 검증은 "리사이즈"가 아니라 "처음부터 데스크톱 크기로 새로고침·재로그인"으로 우회했다.
  - `sw.js`의 `CACHE_VERSION`을 `v44`→`v45`로 올림.
  - **여전히 남은 한계**: 이번에도 실제 화면을 눈으로 보는 최종 확인은 사용자 몫이다 — 다만 이번엔 애니메이션 자체가 없어서 이전 라운드들처럼 "수치는 맞는데 페인트만 깨지는" 클래스의 버그가 원천적으로 재발할 표면적이 없다.
  - **이 항목은 아래 2026-08-24 후속 작업으로 대체됨** — 사용자가 "PC와 같은 진짜 애니메이션을 원한다"고 재요청해서 flash를 버리고 다시 커얼(clip-path 기반) 시도로 넘어갔다가, 그것도 실기기에서 또 깨져서 최종적으로 CSS `rotateY` 카드 뒤집기로 정착했다. 아래 항목들 참고.
- ✅ **완료·커밋됨 (2026-08-24) — 📌 페이지 넘김 효과 1차 완료**: 사용자가 실기기에서 최종 확인 — "의도한 대로 작업이 된 것 같다"고 승인. 아래 CSS 3D `rotateY` 카드 뒤집기가 지금까지 9라운드+3번의 재구현을 거친 세로 모드 페이지 넘김 애니메이션 문제의 **실질적인 종착점**이다. 다시 이 부분을 만질 일이 있으면(느낌을 더 다듬거나 PC 커얼에 더 가깝게 만들고 싶어지는 등) 아래 트레이드오프(평평한 카드 뒤집기 vs 대각선 커얼)부터 다시 논의할 것 — clip-path 기반 재시도는 이미 3번 실패했으니 재도전하지 않는다.
  사용자가 "PC와 같은 애니메이션을 모바일에도 문제없이 넣고 싶다"고 재요청 — flash로는 만족스럽지 않았고, 원래 목표(진짜 페이지 넘김 애니메이션)로 돌아가고 싶어했다. 그래서 위 flash 항목을 대체하고, 커얼(clip-path+rotate) 재구현을 다시 시도했다가(아래 세부 내역) 또 실기기에서 같은 종류의 문제가 재발해서, **결국 clip-path 기법 자체를 완전히 버리고 CSS 3D `rotateY` 카드 뒤집기로 처음부터 다시 만들었다.** 이 과정에서 중요한 소통 문제도 있었다 — "검증됨"이라고 여러 번 말했지만 실제로는 논리/좌표 검증이었을 뿐 실기기 페인트 확인이 아니었다는 걸 사용자가 지적했고, 이후로는 그 차이를 명확히 구분해서 전달하기로 했다.
  - **1차 재시도(커얼)**: git 이력(`72d4f7f`)에서 이전 커얼 구현을 되살려 코드 리뷰만으로 실제 포팅 버그 후보를 하나 찾았다 — 원본 라이브러리의 `Flip.flip()`은 `animateFlippingTo()`(rAF 루프) 시작 *직전에* `this.do(startPoint)`를 동기 호출해서 첫 프레임이 그려지기 전에 이미 올바른 스타일이 걸려있게 하는데(과거 9라운드 사가의 패치 (f)), 이 포트는 그 동기 호출 없이 곧장 `requestAnimationFrame(frame)`만 걸어서 — DOM 삽입 시점과 첫 rAF 콜백 사이에 새로 만든 레이어들이 클립/회전 없는 "날것" 상태로 잠깐 노출될 수 있는 진짜 초기화 틈이 있었다. 추가로 이 포트는 매 애니메이션마다 5개 레이어 DOM을 통째로 새로 만드는데(원본은 첫 플립 이후 임시 복사본을 재사용), 새 DOM의 첫 페인트가 폰트 로딩 등과 얽혀 불안정할 가능성도 있었다. **다만 사용자가 이 두 가설을 검증하기도 전에 "이전 작업을 참고하지 말고 완전히 새로운 방식으로 하라"고 명확히 요청**해서, 이 진단은 실제로 코드에 반영되지 않았다 — 기록만 남긴다.
  - **최종 결정 — 기법 자체 교체**: `clip-path`+`rotate`를 매 프레임 JS로 갱신하는 방식은 이제 서로 다른 세 가지 구현(라이브러리 9라운드 패치 → 진짜 1페이지 커얼 이식)에서 전부 실기기 글리치가 재발했다 — 코드를 더 고치는 게 아니라 기법 자체를 바꾸기로 결정. `js/portrait-flip.js`를 처음부터 다시 작성: **CSS 3D `perspective`+`rotateY` 카드 뒤집기**로, 이전 세 구현의 코드를 전혀 재사용하지 않았다.
    - `clip-path`를 아예 안 쓴다 — 지금까지 문제의 공통분모였던 요소 자체가 없다.
    - 매 프레임 JS가 스타일을 쓰지 않는다 — `leaf.style.transform`을 시작/목표 값 두 번만 설정하고 나머지는 CSS `transition`이 브라우저 자체 보간으로 처리한다(JS 구동 rAF 루프 자체가 없음, `requestAnimationFrame` 호출이 코드에 없다).
    - 실제 DOM(`.page` 요소, 기존 `createPageElements`와 같은 구조)을 그대로 쓴다 — 캔버스나 이미지 스냅샷이 아니라서 폰트/렌더링이 항상 정확하다.
    - 구조: 아래층 `base`(드러날 페이지, 고정) + 위층 `leaf`(넘어가는 페이지, `backface-visibility:hidden`으로 90도를 넘으면 시각적으로 사라짐) 두 장뿐. 방향은 `transform-origin`(왼쪽/오른쪽 가장자리)과 회전 부호만 다른 완전히 같은 코드.
    - 겹쳐 눌림 방지(`isPortraitFlipAnimating`)와 정리(`cancelPortraitFlip`)는 이전 커얼 버전과 같은 패턴 유지, 다만 `transitionend` 이벤트 + `setTimeout` 안전장치 조합으로 완료를 감지(rAF 대신).
    - ⚠️ **트레이드오프를 사용자에게 명시**: 이건 종이가 대각선으로 말리는 PC의 커얼과 완전히 같은 모양이 아니라, 세로축 중심으로 평평하게 도는 카드 뒤집기다. 슬라이드보다는 "진짜 애니메이션"에 훨씬 가깝지만 PC와 동일한 룩은 아니라는 점을 사용자가 인지하고 진행.
  - **검증**: dev 로그인 + phone 뷰포트(393×700)로 실제 뷰어에서 (1) 다음/이전 각각 트리거 시 `.portrait-flip-stage`가 생기고 `leaf`의 목표 `transform`이 방향에 맞게 정확히 설정되는 것 확인, (2) 애니메이션 도중 겹쳐 누르기 — 스테이지가 정확히 1개만 유지되고 최종적으로 페이지가 1번만 넘어감, (3) 완료 후 스테이지 DOM이 깨끗이 제거되고 페이지 인디케이터가 정확한 값으로 갱신됨, (4) 데스크톱 크기(1280×800)로 새로고침·재로그인해서 진짜 가로 모드(`#book-spine` 보임)로 확인 — `.portrait-flip-stage`가 전혀 안 생기고 콘솔 에러 없음. **다만 이 환경은 `document.hidden=true`라 CSS 트랜지션도 rAF와 마찬가지로 멈춰있는 것으로 실측 확인됨**(`transitionend`가 몇 초를 기다려도 전혀 안 뜨고, 매번 `setTimeout` 안전장치가 대신 완료를 처리함) — 즉 이 환경은 CSS 트랜지션 기반 애니메이션도 실제 재생을 볼 방법이 구조적으로 없다. 기능적 정확성(상태 갱신, 가드, 정리)은 이 환경에서 확인됐고, **시각적 매끄러움은 사용자가 실기기에서 최종 확인 완료** — "의도한 대로 작업이 된 것 같다"고 승인받음(2026-08-24). 이 항목으로 세로 모드 페이지 넘김 애니메이션 작업 1차 마무리.
  - `index.html`/`styles.css`에서 이제 안 쓰는 `#page-turn-flash` 엘리먼트와 CSS를 제거했다.
  - `sw.js`의 `CACHE_VERSION`을 `v45`→`v46`으로 올림.
- ✅ **완료·커밋됨 (2026-08-24)**: 위 rotateY 카드 뒤집기를 승인한 사용자가 참고 삼아 DearFlip 데모(`js.dearflip.com`, WebGL/Three.js 기반 렌더링)를 보여주며 "이게 더 자연스러워 보인다"고 언급 — **기법(WebGL/캔버스) 자체를 가져오라는 게 아니라, 애니메이션의 느낌(가속·감속 곡선/입체감/휘어지는 모양)만 참고해서 지금 구현에 반영해달라는 것**이었다(캔버스 기반 재작업 여부를 먼저 물어봤으나 사용자가 그건 아니라고 명확히 함 — WebGL/Three.js는 이 앱엔 과한 의존성이고, 텍스트를 이미지로 캡처하는 방식은 iOS Safari의 SVG foreignObject 폰트 버그 같은 새 위험을 또 만들 수 있어서, 애초에 채택 안 하는 쪽으로 조언했음).
  - `js/portrait-flip.js`의 leaf 애니메이션을 `transition`(시작값→목표값 두 지점)에서 `styles.css`의 `@keyframes portrait-flip-leaf-next`/`-prev`(0%/45%/100% 세 지점)로 바꿨다 — 여전히 clip-path 없음, 여전히 JS 매 프레임 갱신 없음(선언적 CSS 애니메이션 그대로).
    - **가속·감속**: `ease-in-out` → `cubic-bezier(0.22, 1, 0.36, 1)`(빠르게 시작해서 부드럽게 착지, "easeOutQuint" 계열) — 실제로 페이지를 손으로 넘길 때의 느낌(휙 넘기고 사뿐히 안착)에 더 가깝게.
    - **휘어지는 모양**: 45% 지점(대략 rotateY 80도 근처 — 90도를 넘으면 `backface-visibility:hidden`으로 안 보이니 그 전까지만 의미 있음)에서 `scaleX`를 0.86까지 줄였다가 100%에서 1로 복귀 — 접히는 쪽(pivot 반대편 가장자리)이 안으로 눌리는 것처럼 보여서 종이가 살짝 휘는 착시를 준다. 진짜 메시 변형이 아니라 순전히 시각적 눌림 효과지만, 리스크(멀티 세그먼트 체인 트랜스폼 등)를 안 지는 선에서 가장 단순한 방법으로 선택.
    - **입체감**: 같은 45% 지점에서 `translateZ(24px)`로 살짝 들어올리고, `box-shadow`도 그 순간 더 크고 짙게(`-14px 0 30px rgba(0,0,0,0.3)` → `-40px 0 60px rgba(0,0,0,0.5)`) — 정적 그림자보다 입체감이 뚜렷해짐.
  - `js/portrait-flip.js`의 완료 감지를 `transitionend`→`animationend`로, `cancelPortraitFlip()`의 리스너 해제도 맞춰서 변경. `transition` 특유의 "강제 리플로우 후 값 변경" 트릭은 `animation`에서는 필요 없어서 제거(애니메이션 이름을 설정하는 순간 항상 처음부터 재생 시작).
  - **검증**: dev 로그인으로 phone(393×700)/desktop(1280×800, 진짜 landscape 확인) 뷰포트에서 다음/이전 각각 트리거 시 `leaf.style.animation`이 올바른 keyframe 이름·이징으로 설정되는 것, `document.styleSheets`를 직접 순회해서 `@keyframes portrait-flip-leaf-next`의 0%/45%/100% 규칙이 의도한 그대로 파싱된 것(브라우저가 실제로 이해하는 값), 연타 스트레스 테스트(겹쳐 누르기 시 스테이지 1개만 유지), 완료 후 DOM 정리, 가로 모드에서 `.portrait-flip-stage`가 전혀 안 생기는 것까지 확인. 콘솔에 실제 에러는 없었지만, **가로 모드 테스트 중 `js/vendor/page-flip.browser.js`에 남아있던 예전 `[PFDEBUG ...]` 디버그 로그(9라운드 사가 때 넣었던 임시 로깅, 그 버그는 이미 해결됨 — 세로 모드가 이 라이브러리를 아예 안 쓰게 됐으므로)가 여전히 매 플립마다 콘솔에 찍히는 걸 발견** — 오늘 작업과 무관한 기존 잔재라 별도 백그라운드 작업으로 분리해서 정리하도록 남겨뒀다(`task_f938e402`).
  - 이번에도 이 환경은 `document.hidden=true`라 실제 애니메이션 재생(가속·감속 곡선이 실제로 어떻게 보이는지, scaleX 눌림이 자연스러운지)은 여전히 못 봤다 — 코드가 의도한 값을 정확히 설정한다는 것만 확인됐고, 시각적 판단은 사용자 몫.
- ✅ **완료·커밋됨 (2026-08-24)**: 사용자 피드백 두 가지를 이어서 반영.
  1. **`task_f938e402`(위에서 백그라운드로 분리해뒀던 PFDEBUG 로그 정리)를 직접 처리** — `js/vendor/page-flip.browser.js`에 남아있던 4곳의 `console.log("[PFDEBUG ...]")` 호출부(`PageCollection.getFlippingPage()`/`getBottomPage()`, `Flip.do()`, `HTMLPage.drawSoft()`)와 그 배경을 설명하던 "TEMPORARY DEBUG LOGGING" 헤더 주석 블록을 삭제하고, 대신 "왜 없어졌는지·문제가 어떻게 최종 해결됐는지"를 짧게 남겼다. 미니파이된 한 줄짜리 코드라 Edit 도구로 직접 매칭이 안 돼서, Python으로 정확한 바이트 위치를 찾아 각 호출부의 정확한 경계(`if(...)try{...}catch(err){}`, 콤마 연산자로 묶인 IIFE 등)를 확인한 뒤 제거했다. ⚠️ **검증 중 실수 하나 발견·수정**: 처음엔 헤더 주석만 지우고 실제 4개 호출부는 깜빡 잊고 그대로 뒀었다 — 브라우저에서 가로 모드로 다시 확인하다가 로그가 여전히 찍히는 걸 보고 알아챘고, 그제서야 4곳을 마저 지웠다.
  2. **세로 모드 애니메이션 타이밍 재조정** — 사용자 피드백: "한 페이지(모바일)는 착지가 안 보이니 그거 생각해서 속도 다시 조절해줘, 지금은 너무 빠르게 보이네." 원인: `rotateY`가 90도를 넘으면 `backface-visibility:hidden`으로 leaf가 완전히 안 보이는데, 감속 곡선을 애니메이션 전체(0~100%, 즉 rotateY 0~180도)에 걸어놨더니 **실제로 보이는 구간(대략 0~60%)엔 곡선의 빠른 초반부만 노출되고, 정작 "부드럽게 착지"하는 감속 부분은 안 보이는 뒤쪽 절반에서 낭비되고 있었다.** 키프레임별로 `animation-timing-function`을 따로 걸 수 있다는 걸 이용해 수정: `@keyframes`의 0% 규칙에만 감속 곡선(`cubic-bezier(0.16, 1, 0.3, 1)`, easeOutExpo 계열로 이전보다 더 뚜렷하게)을 걸고, 그게 다음 키프레임(사라지기 직전인 62%, 기존 45%에서 늦춤 — 보이는 구간 자체도 늘려서 여유를 줌)까지만 적용되게 했다. 62%~100%(이미 안 보이는 구간)는 타이밍 함수 지정 안 함(JS의 전체 애니메이션 기본값인 `linear`로 처리, 어차피 안 보이니 상관없음). 전체 `duration`도 600→650ms로 살짝 늘려 여유를 더 줬다.
  - `js/portrait-flip.js`의 `leaf.style.animation` 설정을 `cubic-bezier(...)` 전체 지정에서 `linear`로 바꿨다(실제 감속은 이제 styles.css의 키프레임별 `animation-timing-function`이 담당).
  - **검증**: 이 환경 특유의 캐싱-프록시 문제(같은 탭을 계속 재사용하면 예전 파일을 계속 서빙하는 것으로 보임 — CLAUDE.md에 이미 기록된 증상)를 이번에 직접 겪었다 — 기존 탭에서 PFDEBUG 로그가 여전히 찍혀서 처음엔 "코드 삭제가 실패했나?" 헷갈렸는데, 새 탭을 열어 다시 확인하니 깨끗했다. 최종적으로 새 탭에서: 로그 완전히 사라짐 확인, `leaf.style.animation`이 `"650ms linear ... portrait-flip-leaf-next"`로 설정됨, `document.styleSheets`에서 `@keyframes portrait-flip-leaf-next`의 0% 규칙에 `animation-timing-function: cubic-bezier(0.16, 1, 0.3, 1)`이 정확히 걸려있고 62%/100% 값도 의도한 그대로임을 확인, 가로 모드 재검증(landscape 정상, PFDEBUG 없음), 콘솔에 앱 관련 실제 에러 없음(리소스 400 에러 하나는 검증 중 내가 직접 시도한 `127.0.0.1` 접속 실패였을 뿐, 앱과 무관).
  - `sw.js`의 `CACHE_VERSION`을 `v47`→`v48`로 올림.
  - 여전히 이 환경에서 감속 곡선이 실제로 자연스럽게 느껴지는지는 확인 불가 — 사용자가 실기기에서 "착지가 이제 보이는지, 속도가 적당한지" 다시 판단해야 한다.
- ✅ **완료·커밋됨 (2026-08-25)**: 위 타이밍 튜닝 이후에도 사용자가 실기기에서 "페이지 넘어가는 속도가 너무 빠르게 느껴진다"고 피드백 — 세로 모드 애니메이션 전체 길이만 늘림.
  - `js/portrait-flip.js`의 `playPortraitPageTurn()` 기본 `duration`을 `650`→`850`ms로 늘렸다(호출부인 `js/reader.js`의 `jumpToPrevPage`/`goToNextPage`가 `duration`을 안 넘기므로 이 기본값이 그대로 적용됨). `safetyTimer`(`duration + 500`)는 이미 `duration` 변수를 참조해서 자동으로 같이 늘어남 — 별도 수정 불필요.
  - 키프레임 자체(`styles.css`의 `portrait-flip-leaf-next`/`-prev`, 감속 곡선이 걸리는 0~62% 구간 비율, scaleX 눌림 정도)는 안 건드림 — "속도"에 대한 피드백이라 전체 길이만 조정, 곡선 모양은 그대로 유지.
  - `sw.js`의 `CACHE_VERSION`을 `v48`→`v49`로 올림.
  - **검증 한계**: 이 세션도 브라우저 자동화로 실제 애니메이션 재생 속도(체감)를 확인할 방법이 없다(과거 라운드들과 동일한 rAF/CSS transition 정지 제약) — 순수 상수 변경이라 로직 리스크는 낮지만, 최종 판단(650→850ms가 충분한지, 더 늘려야 하는지)은 사용자가 실기기에서 확인해야 한다.
- ✅ **완료·커밋됨 (2026-08-25)**: 850ms도 사용자가 "조금만 더 느리게" 요청 — `duration` 기본값을 `850`→`1000`ms로 한 번 더 늘림. 다른 건 동일(곡선 모양 그대로, `safetyTimer`는 `duration` 참조라 자동 반영). `CACHE_VERSION` `v49`→`v50`. 검증 한계도 동일(이 환경에서 체감 속도 확인 불가) — 사용자가 실기기에서 재확인 필요.
- ✅ **완료·커밋됨 (2026-08-25)**: 사용자가 두 가지를 동시에 지적 — (1) "화면에 보이는 건 90도 넘어가면 안 보이는데 실제로는 180도까지 도는 거 아니냐"는 질문에 코드로 확인해서 그렇다고 답했고, (2) "안 보이는 쪽은 아예 없애는 게 맞을 것 같고, 애니메이션이 끝나고 나서 페이지 번호가 움직이는 현상이 있어 확인해달라"고 요청. 조사해보니 **이 둘이 사실 같은 원인이었다**: 키프레임의 100%가 rotateY(-180deg/180deg)까지 도는데 leaf는 90도를 넘는 순간 이미 `backface-visibility:hidden`으로 안 보이니, 유저 눈엔 페이지가 이미 넘어간 것처럼 보이는 시점(구 62% 부근)과 실제 `animationend`가 발동해서 `finishManualPageTurn()`(페이지 인디케이터 갱신 등)이 실행되는 시점(100%) 사이에 duration의 상당 부분(약 38%)만큼 눈에 띄는 간극이 있었다.
  - **수정**: `styles.css`의 `@keyframes portrait-flip-leaf-next`/`-prev`를 0%/62%/100% 3단에서 **0%/100% 2단**으로 단순화 — 100% 지점을 구 62% 키프레임 값(rotateY 대략 88deg 부근)에서 딱 화면에서 사라지는 지점(rotateY 92deg, 90을 살짝 넘겨 확실히 안 보이게)으로 옮겼다. 즉 duration 전체가 이제 "보이는 구간"이라, `animationend`(→페이지 인디케이터 갱신)가 유저가 페이지 전환을 보는 시점과 사실상 같아진다.
  - 감속 곡선도 더는 "0% 키프레임에만 걸어서 특정 %까지만 적용" 트릭이 필요 없어져서, `js/portrait-flip.js`의 `leaf.style.animation`에 `linear` 대신 `cubic-bezier(0.16, 1, 0.3, 1)`을 통째로 걸도록 단순화(스타일시트 쪽 `animation-timing-function` 트릭 코드 제거).
  - **부수 효과**: 이제 duration(1000ms) 전체가 보이는 구간이 되므로, 같은 duration 값이라도 체감 회전 속도는 이전보다 느려진다(구 설정은 duration의 62%만 보이는 구간이었음) — 별도로 duration 값을 더 줄여달라는 요청은 없었으니 1000ms는 그대로 뒀지만, 이 부수효과로 사용자가 보기에 다시 느리다/빠르다고 느낄 수 있어 유의.
  - `js/portrait-flip.js` 상단 히스토리 주석과 `styles.css`의 키프레임 위 주석에 이번 변경 경위를 남김.
  - `sw.js`의 `CACHE_VERSION`을 `v50`→`v51`로 올림.
  - **검증**: 이번엔 dev 로그인 + phone 뷰포트(393×700)로 실제 뷰어를 열어 (1) `document.styleSheets`를 직접 순회해서 두 키프레임이 의도한 대로 0%/100% 2단(100%가 ±92deg)으로 파싱된 것 확인, (2) 넘기기 트리거 직후 `leaf.style.animation`이 `"1000ms cubic-bezier(0.16, 1, 0.3, 1) ... portrait-flip-leaf-prev"`(linear 아님, 전체 duration에 감속 곡선)로 정확히 설정되는 것 확인, (3) 이 환경은 `document.hidden`이라 `animationend`가 전혀 안 뜨고 매번 `safetyTimer`(`duration+500`)가 대신 완료를 처리하는 것도 재확인(과거 라운드와 동일 제약) — 완료 시점에 오버레이 제거와 페이지 인디케이터 갱신(`1/53`→`2/53`, 슬라이더 값도 동기화)이 여전히 같은 동기 호출 안에서 함께 일어나는 것 확인, 콘솔 에러 없음, (4) 가로(데스크톱, 1280×800) 모드 재검증 — `.portrait-flip-stage`가 전혀 안 생기고 정상 동작, 회귀 없음.
  - **여전히 남은 한계**: 이 환경은 `document.hidden`이라 실제 애니메이션이 재생되는 걸 눈으로 볼 방법이 구조적으로 없다(과거 라운드들과 동일) — 논리적으로 "보이는 구간=animationend 시점"이 되도록 고쳤다는 것까지는 이번에도 코드/키프레임 수치로 확인했지만, 실기기에서 정말로 "페이지 번호가 즉각 같이 바뀌는 것처럼 느껴지는지"와 "회전 속도가 부수효과로 인해 다시 느려/빨라 보이는지" 둘 다 사용자가 확인해야 한다.
  - **이 항목은 아래 2026-08-25 두 번째 라운드로 대체(보완)됨** — 위 수정만으로는 부족했다. 아래 항목 참고.
- ✅ **완료·커밋됨 (2026-08-25, 같은 날 2차 수정)**: 사용자가 실기기에서 위 수정을 재확인한 뒤 "여전히 한 박자 쉬고 페이지 번호(위치)가 바뀐다, 180도까지 다 돌고 나서 움직이는 것 같다"고 재신고 — 이번엔 **Web Animations API로 실제 재생 각도를 프레임 단위로 실측**해서 정확한 원인을 찾았다(이전엔 키프레임/논리만 확인하고 "될 것"이라고 추정했었는데, 이번엔 진짜 숫자로 검증).
  - **실측 방법**: `element.animate()`(또는 실제 `leaf.getAnimations()[0]`)를 `pause()`한 뒤 `anim.currentTime`을 20ms 간격으로 강제로 설정해가며 그때마다 `getComputedStyle(leaf).transform`의 `matrix3d(...)`를 파싱해 실제 rotateY 각도를 역산 — 이 방법은 `document.hidden`이라 rAF/실제 재생이 멈춰있는 이 환경에서도 "각 시점에 브라우저가 실제로 계산하는 보간값"을 정확히 읽을 수 있다(재생 자체가 아니라 특정 시점의 스냅샷을 강제로 만드는 것이므로 hidden 여부와 무관).
  - **찾아낸 진짜 원인**: 직전 수정(100%를 rotateY 92deg로 당김)은 방향은 맞았지만, 그때 같이 쓰던 감속 곡선 `cubic-bezier(0.16, 1, 0.3, 1)`(easeOutExpo 계열 — 아주 급격하게 감속)이 목표각의 대부분을 실제로는 duration 초반에 이미 다 돌아버린다는 걸 놓쳤다. 실측 결과: duration 1000ms 중 **540ms(54%) 시점에 이미 rotateY가 90도를 넘어 화면에서 사라졌는데, `animationend`(→페이지 인디케이터 갱신)는 여전히 1000ms(100%)에야 발동** — 결국 100%를 "사라지는 지점"에 맞춰놔도, 급격한 감속 곡선이 그 지점 도달 시점 자체를 duration 중반으로 앞당겨버리면서 정확히 같은 종류의 문제(사용자가 "180도까지 다 돌고 나서"라고 표현한 그 간극)가 그대로 남아있었다.
  - **수정**: `styles.css` 키프레임의 목표각을 92deg→**91deg**로 살짝 낮추고, `js/portrait-flip.js`에서 `leaf.style.animation`에 걸던 감속 곡선을 `cubic-bezier(0.16, 1, 0.3, 1)`→**`ease-out`**(훨씬 완만한 표준 곡선)으로 교체. 같은 실측 방법으로 재검증한 결과 **duration의 94%(940ms) 지점에야 90도를 넘는 것**으로 확인됐다 — 남은 무의미한 여백은 duration의 약 6%(~60ms)뿐이라 사람이 인지하기 어려운 수준.
  - `js/portrait-flip.js` 상단 히스토리 주석과 `styles.css` 키프레임 위 주석에 "1차 수정으로는 부족했던 이유"까지 포함해서 남김 — 다음에 이 부분을 또 만질 사람이 "왜 이미 한 번 고친 걸 또 고쳤는지" 헷갈리지 않도록.
  - `sw.js`의 `CACHE_VERSION`을 `v51`→`v52`로 올림.
  - **검증**: 이번엔 실측이 핵심 검증이었다(위 방법론 참고) — dev 로그인 + phone 뷰포트(393×700)로 실제 뷰어를 열어 (1) 실제 `leaf.getAnimations()[0]`을 대상으로 20ms 간격 94개 프레임을 찍어 90도 교차 시점이 94%로 나온 것 확인, (2) `document.styleSheets`로 키프레임이 의도한 91deg/2단 구조로 파싱된 것 재확인, (3) `leaf.style.animation` 문자열이 `"1000ms ease-out ... portrait-flip-leaf-next"`로 정확히 설정되는 것 확인, (4) 완료 후 오버레이 제거+페이지 인디케이터 갱신이 여전히 정상 작동, 콘솔 에러 없음. ⚠️ **이 세션에서 이 환경 특유의 캐싱 문제를 한 번 더 겪었다** — 새 탭을 열어도 이전 세션(같은 대화 안에서 먼저 연 다른 탭)이 등록해둔 서비스워커/Cache Storage가 여전히 남아있어서 처음엔 수정 전 코드(cubic-bezier, 92deg)가 그대로 서빙됐다 — `navigator.serviceWorker.getRegistrations()`로 등록 해제 + `caches.delete()`로 전부 지우고 나서야 새 코드가 반영됐다. **다음에 이 환경에서 JS/CSS를 고친 직후 바로 재검증할 때는, 새 탭을 여는 것만으로는 부족할 수 있고 서비스워커/캐시를 명시적으로 지워야 할 수 있다는 걸 기억할 것.**
  - **여전히 남은 한계**: 동일 — 이 환경은 `document.hidden`이라 실제 재생을 눈으로 볼 방법이 없다. 다만 이번엔 "논리적으로 맞다"가 아니라 **실제 브라우저가 계산하는 각 시점의 각도를 숫자로 실측**했다는 점에서 이전보다 훨씬 신뢰도가 높다. 그래도 최종 판단(정말 "한 박자"가 없어졌는지, `ease-out` 곡선의 느낌이 이전 곡선과 비교해 괜찮은지)은 사용자가 실기기에서 확인해야 한다.
  - **이 항목도 아래 2026-08-25 3차 수정으로 대체(보완)됨** — 위 두 라운드는 모두 CSS 키프레임/이징 자체가 문제라고 가정했는데, 사용자가 또 재신고하면서 **완전히 다른 원인**을 찾아 고쳤다. 아래 항목 참고.
- ✅ **완료·커밋됨 (2026-08-25, 같은 날 3차 수정)**: 2차 수정(94% 지점까지 밀어냄) 이후에도 사용자가 "여전히 페이지 표시 위치가 바뀌는 문제가 있다"고 재신고 — 이번엔 CSS 키프레임 자체가 아니라 **완료를 감지하는 JS 쪽 타이머 설계**에서 진짜 원인을 찾았다.
  - **다시 짚은 사실**: 이 환경(자동화 브라우저, `document.hidden=true`)은 앞선 두 라운드 모두에서 `animationend`가 단 한 번도 발동한 적이 없고, 매번 `safetyTimer`(당시 `duration + 500`)가 대신 완료를 처리해왔다 — 이는 이미 파일 히스토리에 여러 번 기록돼 있던 사실이다. 이걸 재조명하니: **만약 실기기(특히 iOS Safari — backface-visibility로 사라지는 3D 회전 요소는 `animationend`가 안정적으로 안 뜨는 사례가 알려져 있다)에서도 같은 이유로 `animationend`가 안 뜬다면, 그동안 CSS 쪽을 아무리 정교하게 맞춰도 실제 완료 처리는 항상 `duration + 500`(예: 1000+500=1500ms) 뒤에야 일어난다.** 이건 시각적으로는 대략 `duration` 근처(2차 수정 기준 940ms쯤)에 이미 다 끝나 보이는데, 코드가 그보다 한참 뒤(1500ms)에야 반응한다는 뜻 — 정확히 "한 박자 쉬고 바뀐다"는 신고와 크기·성격이 들어맞는다. 즉 지금까지 두 라운드는 **틀린 원인(키프레임 타이밍)을 고치고 있었을 가능성이 높고, 진짜 병목은 이 500ms 여유분 자체**였을 수 있다.
  - **수정**: `js/portrait-flip.js`의 `safetyTimer`를 `setTimeout(finish, duration + 500)`→`setTimeout(finish, duration + 80)`으로 줄였다 — 500ms는 "탭이 백그라운드로 가는 등 드문 경우에만 쓰일 여유"로 설계됐던 값인데, `animationend`가 아예 안 뜨는 기기에서는 이게 *매번* 적용되는 사실상의 기본 지연이 되어버린다. 80ms는 이벤트 디스패치/스케줄링 지터를 흡수할 정도로만 남겨서, `animationend`가 뜨든 안 뜨든 완료가 `duration` 근처에서 최대한 못박히게 했다.
  - **이 수정의 강점**: 앞선 두 라운드와 달리 이번엔 실제 재생을 볼 필요 없이 **"트리거부터 완료까지 실제 벽시계 시간이 얼마나 걸리는지"를 `performance.now()`로 직접 잴 수 있어서**, 이 환경의 `document.hidden` 제약과 무관하게 수정 효과를 직접 실측 검증할 수 있었다(아래 검증 참고) — 앞선 두 라운드가 "논리적으로 맞다"에 머물렀던 것과 다르다.
  - `sw.js`의 `CACHE_VERSION`을 `v52`→`v53`으로 올림.
  - **검증**: dev 로그인 + phone 뷰포트(393×700)로 실제 뷰어에서, 페이지 넘김 트리거 직전/직후 `performance.now()`를 찍고 `.portrait-flip-stage`가 DOM에서 사라지는 시점을 5ms 간격 폴링으로 감지해서 실제 경과 시간을 측정 — **1085ms**로 나왔다(`duration(1000) + safetyTimer 여유(80)` = 1080ms와 거의 정확히 일치, 나머지는 폴링 간격 오차). 수정 전이었다면(이 환경은 매번 safetyTimer 경로를 타므로) 1500ms 근처였을 것 — 실측으로 확인된 감소폭은 약 400ms. 콘솔 에러 없음. ⚠️ 이번에도 이 환경의 서비스워커/Cache Storage 캐싱 문제를 먼저 겪어서, 검증 전에 `navigator.serviceWorker.getRegistrations()`로 등록 해제 + `caches.delete()`로 지우고 나서야 최신 코드가 반영됐다(반복되는 패턴 — 위 항목의 메모 그대로).
  - **여전히 남은 한계 / 불확실성**: (1) 이 환경은 `document.hidden`이라 매번 `safetyTimer` 경로만 타므로, 이번 수정으로 "이 환경의" 완료 지연이 줄어든 건 확실히 실측했지만, **실기기에서 정말 `animationend`가 안 뜨는지는 여전히 추정일 뿐 직접 확인하지 못했다** — 만약 실기기에서는 `animationend`가 잘 뜨고 있었다면(즉 이번 가설이 틀렸다면) 이 수정은 무해하지만 무효과일 수 있다. (2) `safetyTimer` 여유를 80ms로 좁힌 것 자체의 트레이드오프: 만약 실제 CSS 애니메이션이 어떤 이유로든(저사양 기기의 렌더링 부하 등) `duration`보다 오래 걸린다면, 이 타이머가 애니메이션이 시각적으로 다 끝나기 *전에* 강제로 오버레이를 제거해서 살짝 끊기는 느낌을 줄 이론적 위험이 있다 — CSS 3D transform 애니메이션은 보통 컴포지터 스레드에서 돌아 메인스레드 부하와 비교적 무관하게 제 시간에 끝나는 편이라 위험은 낮다고 판단했지만, 실기기에서 완전히 배제된 가설은 아니다. 사용자가 "한 박자"가 사라졌는지, 대신 끝에 살짝 끊기는 느낌이 새로 생겼는지 둘 다 확인해줄 필요가 있다.
- 🔄 **작업 중 (2026-08-25)**: 위 safetyTimer 수정 이후에도 사용자가 "여전히 페이지 표시 위치가 바뀌는 문제가 있다"고 재신고 — 이번엔 사용자가 실기기 화면 녹화(`Bookify 2026-08-25 08-04-23.mp4`, 데스크톱 앱, 창 너비 720px로 세로/단일페이지 모드)를 보내줘서 `ffmpeg`(프레임 추출) + Python(`Pillow`/`numpy`, 이번에 새로 설치)로 페이지 footer("- N / 3143 -")의 실제 픽셀 Y좌표를 프레임별로 측정 — **처음으로 "애니메이션 타이밍"이 아니라 완전히 다른 종류의, 명확히 측정된 버그를 찾았다.**
  - **측정된 사실**: 영상에 담긴 페이지 넘김 3번 전부에서 동일한 패턴 — 넘긴 직후 footer가 y≈1086px에 나타나서 **약 300~350ms 그대로 유지되다가, 정확히 한 프레임 만에 y≈1047px로 39px 튀어 올라가서** 그 뒤로 고정된다. 페이지를 넘기기 전(오래 안정된 상태)도 항상 y≈1047px이라, 1047이 "맞는" 위치이고 1086이 넘긴 직후 잠깐 나타나는 "틀린" 위치로 보인다.
  - **배제한 후보들**(코드 확인, 재현 안 됨/타이밍 안 맞음): `#sidebar`/`#bottom-bar`의 0.35s slide transition(`position:fixed`라 레이아웃에 영향 없음), 몰입모드 자동숨김 타이머(10초, 크기 안 맞음), `saveProgress`의 300ms 디바운스(DOM을 전혀 안 건드리는 순수 저장 로직), `maybeShiftPageWindow`의 창 재정렬(이 페이지 구간은 창 가장자리 근처가 아니라 계산상 이 3번 다 안 걸렸을 것), Wake Lock 재요청(세션당 한 번만 실제로 발동, 매 페이지마다가 아님).
  - **유력하지만 미확인 가설**: `portrait-flip.js`의 임시 애니메이션 카드는 `buildFlipBook()` 시점에 딱 한 번 측정해 고정한 정확한 픽셀 값(`currentRenderWidth`/`currentRenderHeight`, 이후 절대 안 바뀜)으로 높이를 주는 반면, 진짜 PageFlip 페이지는 `.page{height:100%}` + 라이브러리 내부의 **퍼센트 기반 종횡비 트릭**(`.stf__wrapper`의 `padding-bottom: N%`, 자기 자신의 *현재* 너비 기준으로 계산됨)으로 높이가 정해진다 — 이 둘이 서로 다른 계산 방식이라 어긋날 수 있는 구조라는 것까지는 코드로 확인했지만, **왜 하필 넘긴 직후 잠깐 어긋났다가 정확히 ~300~350ms 뒤에 저절로 교정되는지는 정적 코드 분석만으로 확정 못 했다.**
  - **다음 단계로 택한 것**: 또 확신 없이 코드를 고치는 대신(지난 세 라운드가 전부 "논리적으로 맞음"에서 끝나 사용자 재신고로 이어진 패턴 반복 방지), `js/reader.js`에 **임시 진단 로깅**을 추가했다 — `logFooterDiagnostics(label)`(트리거 시점/`finishManualPageTurn` 종료 시점에 `#book-stage`/`.stf__wrapper`/활성 `.page`/`.page-footer`의 실측 rect + `performance.now()`를 `console.log('[FOOTERDEBUG] ...')`로 찍음), `pollFooterDiagnostics()`(그 뒤 2초간 50ms 간격으로 같은 스냅샷 반복 기록해서 "정확히 언제, 무엇이" 바뀌는지 놓치지 않게 함), 그리고 `scheduleFlipbookRebuild()` 안에도 매 호출마다(ResizeObserver/window resize/orientationchange 전부 포함) widthDelta/heightDelta/재빌드 여부를 로그하는 줄을 추가했다.
  - **⚠️ 이 로깅은 임시다 — 원인이 확인되면 전부 제거할 것**: 대상은 `js/reader.js`의 `logFooterDiagnostics`/`pollFooterDiagnostics` 두 함수, `finishManualPageTurn` 끝의 호출 2줄, `jumpToPrevPage`/`goToNextPage`의 트리거 시점 호출 2줄, `scheduleFlipbookRebuild` 안의 로그 한 블록 — 전부 `⚠️ 임시 디버그 로깅 (2026-08-25)` 주석으로 표시해뒀다. 예전 `js/vendor/page-flip.browser.js`의 `[PFDEBUG]` 라운드와 같은 패턴(그때도 결국 원인 확인 후 통째로 제거했다).
  - `sw.js`의 `CACHE_VERSION`을 `v53`→`v54`로 올림.
  - **검증**: dev 세션(phone 뷰포트)에서 로깅 자체가 예외 없이 정상 작동하는 것만 확인 — trigger/finish/poll(2초, ~46~50ms 간격 40회) 로그가 전부 콘솔에 찍히고 콘솔 에러 없음. ⚠️ 이 환경은 PageFlip의 실제 `.page` `display:none↔block` 전환이 (아마도 `document.hidden`으로 인한 rAF 정지 때문에) 전혀 동작하지 않는다는 걸 이번에 처음 발견했다 — `#my-book .page` 중 `display!=='none'`인 걸 찾는 필터가 이 환경에서는 매번 0개를 반환해서 `pageH`/`footerY`가 항상 `null`로 찍혔다. 즉 **이 환경에서는 이 로깅의 진짜 값(실기기의 footer Y 좌표 변화)을 검증할 수 없다** — 실기기에서 사용자가 페이지를 넘기고 콘솔 로그를 캡처해줘야 다음 단계로 진행 가능하다.
  - **다음에 이어서 볼 사람에게**: 사용자가 실기기(또는 데스크톱 앱)에서 개발자 도구 콘솔을 열고 페이지를 한 번 넘긴 뒤 `[FOOTERDEBUG]` 로그들을 복사해서 주면, `footerY`가 `trigger`→`finish`→`poll+Nms` 시퀀스에서 정확히 몇 ms 지점에 1086→1047로 바뀌는지, 그 순간 `wrapperPaddingBottom`/`stageW`/`stageH`가 같이 바뀌는지(바뀐다면 어느 쪽이 원인이고 어느 쪽이 결과인지), `scheduleFlipbookRebuild` 로그가 그 근처에 끼어 있는지(`willRebuild`가 `false`인데도 발동 자체는 하는지)를 보고 정확한 트리거를 특정할 수 있을 것이다.
  - **이 항목은 아래 2026-08-25 후속 라운드로 이어짐** — 실제로 사용자가 실기기에서 로그를 캡처해줬는데, 그 결과가 뜻밖의 방향으로 원인을 완전히 좁혀줬다. 아래 항목 참고.
- ✅ **완료·커밋됨 (2026-08-25, 같은 날 진단 로그 분석 라운드)**: 사용자가 실기기(데스크톱 Bookify 앱, 영상과 같은 창)에서 `[FOOTERDEBUG]` 로그를 두 번 캡처해서 보내줬다 — 한 번은 '이전'(prev, 467→466), 한 번은 '다음'(next, 466→467) 방향으로.
  - **뜻밖의 결과**: 두 로그 전부 `footerY`가 **트리거 시점부터 2초 폴링 끝까지 999에서 단 1px도 안 움직였다.** `stageW`/`stageH`/`wrapperPaddingBottom`/`pageH`도 전부 완전히 고정. 바뀌는 건 `footerText`(페이지 번호 자체)뿐이었고, 그마저도 `finish` 시점(트리거+~1000ms, 우리가 튜닝해둔 duration과 정확히 일치)에 이미 정확한 값으로 바뀌어 있었다. 그런데 사용자에게 "이 로그를 캡처하는 그 순간 화면에서 실제로 위치가 한 번 더 튀는 게 보였냐"고 물으니 **"이번에도 같이 보였다"**는 답변 — 즉 **눈으로는 분명히 튀는 게 보이는데, 우리가 측정하고 있던 DOM 요소(`#my-book .page-footer`, 진짜 PageFlip 페이지)는 단 1px도 안 움직였다.**
  - **이 모순이 알려준 것**: `logFooterDiagnostics`가 `#my-book .page-footer`만 쿼리해서(`document.querySelectorAll('#my-book .page')`) — **애니메이션 도중 실제로 화면에 보이는 임시 카드(`js/portrait-flip.js`의 `.portrait-flip-stage` 안 leaf/base, `#my-book` 밖의 완전히 별개 DOM)의 footer는 애초에 한 번도 측정한 적이 없었다.** 즉 유저가 보는 "튀는 위치"는 임시 카드의 footer 위치 → 진짜 페이지의 footer 위치(=1047/999, 항상 안정적인 그 값) 사이의 불일치였는데, 우리 로깅이 처음부터 후자만 재고 있었으니 "안 움직인다"는 결과가 나온 것도 당연했다.
  - **직접 검증**: 이 환경(헤드리스, PageFlip의 `display:none↔block` 전환이 안 되는 것으로 이미 확인됨)에서도 `.portrait-flip-stage`는 PageFlip과 무관한 순수 DOM/CSS라 정상적으로 측정 가능했다 — `#my-book`이 `#book-stage`와 완전히 같은 rect(오프셋 0)라는 것과, 임시 카드의 footer가 `buildPageElement()`가 만드는 `position:absolute; top:0; left:0` 구조로 스테이지 바닥에서 정확한 거리를 두고 그려진다는 것까지 확인했다. 실기기 로그(`stageH:1069`, `footerY:999` → 바닥에서 50px)와 우리 환경에서 측정한 임시 카드 footer(스테이지 바닥에서 10.8px)의 "바닥으로부터의 거리"가 서로 크게 다르다(둘 다 다른 책·다른 환경이라 직접 비교는 아니지만, 그 차이의 크기 자체가 영상에서 실측한 39px와 같은 자릿수라는 게 정황상 들어맞는다) — **임시 카드와 진짜 PageFlip 페이지가 서로 다른 크기 계산 방식을 쓰고 있고, 그 결과 footer가 서로 다른 위치에 그려진다**는 게 이제 가장 유력하다.
  - **수정**: `js/portrait-flip.js`의 임시 카드는 `buildFlipBook()` 시점에 딱 한 번 측정해 그 뒤로 절대 안 바뀌는 고정값(`currentRenderWidth`/`currentRenderHeight`)으로 크기를 잡아왔는데, 이걸 **애니메이션을 시작하는 바로 그 순간 실제로 화면에 보이고 있는 진짜 PageFlip 페이지의 실측 크기**로 바꿨다. `js/reader.js`에 `getActiveRealPageSize()` 신설 — `#my-book .page` 중 `display!=='none'`인 걸 찾아 그 `getBoundingClientRect()`의 width/height를 반환(측정 실패 시 기존 고정값으로 안전하게 폴백). `jumpToPrevPage`/`goToNextPage`가 `playPortraitPageTurn()`에 넘기던 `width`/`height`를 이 함수의 결과로 교체했다. 이러면 라이브러리 내부 래퍼가 정확히 몇 px를 차지하든(그리고 나중에 라이브러리 버전이 바뀌어 그 값이 달라져도) 임시 카드가 항상 "지금 실제로 보이는 페이지"와 픽셀 단위로 똑같은 크기가 된다 — 두 계산 방식이 서로 얼마나 다른지 몰라도 되는, 원인 불문 구조적으로 견고한 수정.
  - **부가 진단 로그**: `activeSize`(새로 측정한 값)와 `currentRenderWidth`/`currentRenderHeight`(기존 고정값)의 차이(`deltaW`/`deltaH`)를 트리거 시점마다 `[FOOTERDEBUG] activeSize vs frozen render size`로 찍도록 추가 — 다음 번 실기기 로그 캡처에서 이 델타가 실제로 0이 아닌 값(예상: 세로로 약 39px 안팎)으로 나오면 가설이 최종 확인되는 것이고, 수정 후에도 여전히 위치가 튄다면 이 델타 값이 다음 단서가 된다.
  - `sw.js`의 `CACHE_VERSION`을 `v54`→`v55`로 올림.
  - **검증**: dev 세션에서 코드가 예외 없이 도는 것만 확인(headless 환경은 여전히 `#my-book .page`가 항상 `display:none`이라 `getActiveRealPageSize()`가 매번 폴백 경로를 타서 `deltaW`/`deltaH`가 0으로 찍힘 — 이 환경에서는 이 수정의 실효성 자체를 검증할 수 없다). 콘솔 에러 없음.
  - **다음에 이어서 볼 사람에게**: 여전히 남은 `[FOOTERDEBUG]` 임시 로깅(`logFooterDiagnostics`/`pollFooterDiagnostics`/`scheduleFlipbookRebuild` 안 로그/이번에 추가한 `activeSize vs frozen render size`)은 **이 수정이 실기기에서 확인되면 전부 제거할 것.** 사용자가 다시 페이지를 넘겨보고 (1) 화면에서 위치가 튀는 게 사라졌는지, (2) `[FOOTERDEBUG] activeSize vs frozen render size` 로그의 `deltaW`/`deltaH`가 실제로 0이 아니었는지(가설이 맞았다는 증거) 확인해줘야 한다.
  - **이 항목은 아래 후속 라운드에서 반증됨** — 실기기 재캡처에서 `deltaW`/`deltaH`가 둘 다 0으로 나왔다(임시 카드와 진짜 페이지 크기가 이미 완전히 같았음). 그런데도 사용자는 "이번에도 위치가 튀는 게 보였다"고 확인 — 즉 **크기 불일치 가설 자체가 틀렸다.** 아래 항목 참고.
- 🔄 **작업 중 (2026-08-25, 같은 날 4차 라운드)**: 크기 불일치 가설이 실기기 재검증으로 반증됨(`deltaW:0, deltaH:0`인데도 여전히 튐, `footerY`는 트리거~2초 폴링 내내 999로 고정). 진짜 PageFlip 페이지(`#my-book`)는 확실히 안 움직이는데 유저 눈엔 여전히 튄다는 뜻이라, **애초에 우리가 한 번도 직접 측정한 적 없는 대상 — 임시 애니메이션 카드(`portrait-flip.js`의 `.portrait-flip-stage` 안 `base`) 자신의 footer가 화면 어디에 실제로 그려지는지** — 를 처음으로 직접 계측하는 로그를 추가했다.
  - `js/portrait-flip.js`의 `playPortraitPageTurn()`에 두 지점 추가: 애니메이션 시작 직후(카드가 DOM에 붙자마자) `[FOOTERDEBUG] overlay base footer @ start`, 그리고 `finish()`에서 `perspectiveStage.remove()` 직전(유저가 마지막으로 보는 임시 카드의 상태) `[FOOTERDEBUG] overlay base footer @ removal` — 둘 다 `base`(드러날 새 페이지, 정적)의 `.page-footer` 실측 `getBoundingClientRect().top`과 `stage`(`#book-stage`)의 뷰포트 기준 절대 top/bottom을 함께 찍는다.
  - `js/reader.js`의 `logFooterDiagnostics()`에도 `#my-book`이 `#book-stage` 안에서 (flex 가운데 정렬 등으로) 오프셋이 있는지 확인하는 `myBookOffsetTop`/`myBookOffsetLeft`와, 절대 좌표 비교를 위한 `stageTop`/`stageBottom`(기존엔 상대 W/H만 있었음)을 추가했다 — 이러면 두 파일의 로그를 절대 좌표 기준으로 직접 겹쳐볼 수 있다(`overlay base footer`의 `baseFooterY`와 `logFooterDiagnostics`의 `footerY`를 같은 좌표계에서 직접 비교 가능).
  - `sw.js`의 `CACHE_VERSION`을 `v55`→`v56`으로 올림.
  - **검증**: dev 세션에서 새 로그 두 종류(`overlay base footer @ start`/`@ removal`, `myBookOffsetTop`/`Left`)가 정상적으로 찍히는 것과 콘솔 에러 없음만 확인 — 이 환경에서 실제 값의 의미(진짜로 어긋나는지)는 검증 불가(늘 그렇듯 헤드리스 환경의 `document.hidden` 제약).
  - **다음에 이어서 볼 사람에게**: 사용자가 다시 페이지를 넘기고 콘솔 로그를 캡처해주면, 이번엔 `overlay base footer @ removal`의 `baseFooterY`(임시 카드가 마지막으로 보여준 footer 위치, 절대좌표)와 `logFooterDiagnostics`의 `footerY`+`stageTop`(진짜 페이지 footer 위치, 절대좌표로 환산: `stageTop + (footerY 상대값이 아니라 이미 getBoundingClientRect 기준이라 그 자체로 절대좌표)`)를 직접 비교할 수 있다 — 이 두 값이 다르면 임시 카드 자체의 위치/크기 계산이 실제로 진짜 페이지와 어긋난다는 최초의 직접 증거가 되고, 이것마저 같다면 DOM 측정으로는 못 잡는 순수 페인트/컴포지팅 레벨 문제(이 프로젝트가 clip-path 커얼 사가 내내 겪었던 것과 같은 종류)일 가능성이 높아진다 — 그땐 rotateY+backface-visibility 기법 자체를 재고해야 할 수 있다.
  - **이 항목의 진단 로그로 실제 원인이 확정됨 — 아래 항목 참고.**
- ✅ **완료·커밋됨 (2026-08-25, 같은 날 5차 라운드 — 📌 footer 위치 점프 버그 원인 확정 + 수정)**: 4차 라운드의 진단 로그로 사용자가 다시 캡처해준 결과, **드디어 명확한 39px 차이를 직접 잡았다** — 진짜 페이지 footer: `footerY:999`(트리거~2초 폴링 내내 고정), 임시 카드 footer: `baseFooterY:1038`(시작~제거 직전까지 고정). `myBookOffsetTop/Left`도 `deltaW/H`도 전부 0(크기·위치 입력값은 완전히 동일)인데도 **`.page` 내부에서 footer가 그려지는 실제 위치가 39px 다르다** — 영상에서 실측한 39px와 정확히 일치.
  - **근본 원인 발견**: `js/vendor/page-flip.browser.js`가 자체 주입하는 CSS를 직접 읽어보니 진짜 오타 버그가 있었다 — `.stf__wrapper`를 `.sft__wrapper`(철자 순서 오타)로 잘못 써서, 그 규칙(`position: relative` 포함)이 실제 DOM의 `.stf__wrapper` 요소에 전혀 적용이 안 된다. 그 결과 자식 `.stf__block`(`position: absolute`)의 containing block이 의도한 `.stf__wrapper`가 아니라 그 위의 다른 조상으로 튀면서, 실제 페이지가 우리가 단순히 재현한 위치와 미묘하게 어긋난 곳에 그려진다 — 이게 정확히 39px로 나타난 것.
  - **수정 방향**: 이 업스트림 CSS 오타 자체를 고치는 건 라이브러리 내부에 더 깊이 손대는 위험한 선택이라(다른 부작용 가능성, 이미 8라운드 넘게 이 파일을 패치해온 부담도 고려) 배제 — 대신 **원인을 몰라도 되는 방식**을 택했다: 임시 카드의 크기뿐 아니라 화면상 정확한 위치(top/left)까지 진짜 페이지에서 그대로 재서 맞춘다.
  - `js/reader.js`의 `getActiveRealPageSize()`를 `getActiveRealPageRect()`로 확장 — width/height뿐 아니라 `#book-stage` 기준 `offsetTop`/`offsetLeft`도 함께 반환(측정 실패 시 기존처럼 안전하게 폴백). `jumpToPrevPage`/`goToNextPage`가 이 값을 `playPortraitPageTurn()`에 새로 추가된 `offsetTop`/`offsetLeft` 파라미터로 전달.
  - `js/portrait-flip.js`의 `.portrait-flip-stage`를 `position:absolute; inset:0`(#book-stage 전체를 그냥 덮음)에서 `top`/`left`/`width`/`height`를 전부 명시적으로 지정하는 방식으로 바꿔서, 오버레이가 이제 "#book-stage 전체"가 아니라 "진짜 페이지가 실제로 그려지는 그 자리"에 정확히 겹친다.
  - **검증**: dev 세션에서 (1) 세로 모드 — `activeRect vs frozen render size` 로그가 정상 작동(이 환경은 실제 페이지가 안 보여서 폴백값 0/0을 쓰지만 코드 경로 자체는 예외 없이 정상 실행됨), `overlay base footer @ start`/`@ removal`이 동일한 위치를 안정적으로 유지, (2) 가로(데스크톱) 모드 — `.portrait-flip-stage`가 전혀 안 생기고 정상 동작, 회귀 없음. 콘솔 에러 전 구간 없음.
  - `sw.js`의 `CACHE_VERSION`을 `v56`→`v57`로 올림.
  - **남은 임시 디버그 로깅**: `[FOOTERDEBUG]` 계열 전부(`logFooterDiagnostics`/`pollFooterDiagnostics`/`scheduleFlipbookRebuild`/`activeRect vs frozen render size`/`overlay base footer @ start`/`@ removal`) — 이 수정이 실기기에서 최종 확인되면 전부 제거할 것.
  - **여전히 남은 한계**: 이번에도 이 환경에서 실제 화면을 보는 최종 확인은 불가능하다 — 하지만 이번엔 지금까지 이 버그를 쫓아온 모든 라운드 중 가장 강한 근거가 있다: 실제 사용자 로그에서 시작 → 정확한 39px 불일치를 두 요소 모두에서 직접 실측 → 라이브러리 소스에서 그 불일치를 설명하는 진짜 CSS 버그(오타)를 확인 → 원인 불문 구조적으로 견고한 수정(실제 위치를 그대로 복사)까지 갔다. 사용자가 실기기에서 최종 확인해야 한다.
  - **이 항목은 5차 수정으로도 부족했던 것으로 반증됨 — 아래 6차(진짜 근본 원인) 항목 참고.**
- ✅ **완료·커밋됨 (2026-08-25, 같은 날 6차 라운드 — 📌 진짜 근본 원인 발견 + 수정)**: 5차 수정(오프셋 top/left 보정)을 사용자가 PC 웹 브라우저(데스크톱 앱 아니었음 — "pc웹이야")에서 재검증했는데 **여전히 위로 튀었다.** 이번엔 사용자가 "다음장" 3번을 연달아 넘기며 캡처해준 로그를 페이지별로 낱낱이 대조해서 결정적인 사실을 찾았다.
  - **실측 결과**: `offsetTop`/`offsetLeft`는 이번에도 매번 정확히 0 — 즉 5차의 "위치 보정" 자체가 애초에 고칠 게 없었던 것(그래서 효과가 없었다). 대신 **연속된 페이지들의 진짜 footer 위치 자체가 서로 달랐다**: 472페이지는 `footerY:999`, 473페이지는 `footerY:973`(999와 26px 차이), 474페이지는 다시 `footerY:999`. **같은 책, 같은 세션, 같은 창(window) 안에서도 진짜 PageFlip `.page` 요소끼리 footer 위치가 들쭉날쭉했다** — 이게 진짜 근본 원인이었다. 임시 카드는 늘 고정된 값(1038) 하나로 그려지니, 어느 페이지에 맞추든 다른 페이지에서는 어긋날 수밖에 없는 구조였다.
  - **수정**: 크기/위치를 "미리 재서 맞추는" 접근 자체를 버리고, **순서를 뒤집었다** — 애니메이션을 시작하기 *전에*, 오버레이 뒤에 완전히 숨겨진 채로 진짜 페이지 전환(`pageFlip.turnToPage()` + `maybeShiftPageWindow()`)을 먼저 끝내버리고, 그 직후 "지금 막 진짜로 바뀐 바로 그 페이지"를 실측해서 임시 카드 크기를 맞춘다. 이러면 어떤 페이지가 어떤 높이로 렌더링되든(원인 불문) 항상 같은 페이지를 재는 것이므로 100% 일치한다.
  - `js/reader.js`의 `finishManualPageTurn`을 둘로 쪼갰다: 새 함수 `swapRealPageForFlip(targetGlobal)`(진짜 DOM 전환만, 애니메이션 *시작 전* 호출 — 오버레이가 바로 화면 전체를 덮을 것이므로 사용자 눈엔 아무 것도 안 보임, 전부 동기 실행이라 중간 프레임이 그려질 틈도 없음)와, 살아남은 `finishManualPageTurn`(하단 페이지 인디케이터/진행상황 저장/화면 항상 켜짐 등 *사용자 눈에 보이는* 것들만, 애니메이션이 끝난 뒤 여전히 호출 — 이 타이밍은 앞서 여러 라운드에 걸쳐 따로 튜닝했던 부분이라 안 건드림).
  - `jumpToPrevPage`/`goToNextPage`가 이제 `swapRealPageForFlip(targetGlobal)`을 먼저 호출한 다음 `getActiveRealPageRect()`로 재고, 그 값을 `playPortraitPageTurn()`에 넘긴다.
  - `maybeShiftPageWindow`의 `isShiftingWindow` 재진입 가드(`if (isShiftingWindow || !pageFlip) return`)와 순서가 꼬이지 않도록 — `swapRealPageForFlip` 안에서 `turnToPage()`를 `isShiftingWindow=true`로 감싼 뒤 `finally`에서 다시 `false`로 되돌리고 나서야 `maybeShiftPageWindow()`를 호출하도록 기존 순서를 그대로 보존했다(이전 `finishManualPageTurn`이 하던 순서와 동일, 옮기기만 함).
  - `sw.js`의 `CACHE_VERSION`을 `v57`→`v58`로 올림.
  - **검증**: dev 세션에서 (1) 세로 모드 next/prev 각각 트리거 — 콘솔 에러 없음, 애니메이션 정상 완료(오버레이 생성→제거, 페이지 인디케이터 갱신), (2) 가로(데스크톱) 모드 — 회귀 없음. 이 환경은 여전히 실제 PageFlip 페이지의 `display:none↔block` 전환이 안 되므로(문서 hidden으로 인한 rAF 정지 추정) `getActiveRealPageRect()`가 매번 폴백 경로를 타는 것까지는 동일 — 이 환경에서 "페이지마다 다른 실제 높이"라는 이번 발견의 핵심 값 자체를 재검증하지는 못했다.
  - **여전히 남은 한계**: 왜 진짜 PageFlip 페이지들끼리 높이가 들쭉날쭉한지(창 재구성 시점의 미세한 측정 차이? `.sft__wrapper` 오타로 인한 containing-block 혼선이 페이지마다 다르게 작용하는 것? 아직 확정 못함) 근본 원인까지는 못 밝혔다 — 다만 이번 수정은 그 원인을 몰라도 되게 설계했다("항상 방금 실제로 보여준 그 페이지를 그대로 잰다"는 방식이라 원인이 무엇이든 구조적으로 무력화됨). 사용자가 실기기(PC 웹)에서 다시 확인해야 최종 결론이 난다.
  - **이 항목도 부족했던 것으로 확인됨 — 아래 7차(leaf/base 크기 분리) 항목 참고.**
- ✅ **완료·커밋됨 (2026-08-25, 같은 날 7차 라운드)**: 사용자가 6차 수정을 재확인 — "똑같이 튀고 있어"(PC 웹 브라우저, 데스크톱 앱 아님). 새 녹화를 요청했으나 사용자가 "예전에 보낸 것과 같으니 그걸 다시 보라"고 해서, 기존 영상(`Bookify 2026-08-25 08-04-23.mp4`)의 점프 전/후 프레임(f_017/f_018)을 다시 나란히 비교 — **본문 텍스트는 완전히 그대로, footer 줄만 정확히 위로 이동**하는 것을 재확인(이전 분석과 일치, 새로운 단서는 아니었음).
  - **6차 수정 자체의 진짜 결함 발견**: `swapRealPageForFlip()`을 먼저 호출한 *뒤에* `getActiveRealPageRect()`를 딱 한 번만 불러서 그 값을 leaf(넘어가는 옛 페이지)와 base(새로 드러날 페이지) **둘 다에 똑같이** 썼다. 그런데 스왑 *이후*의 값은 TARGET(새) 페이지의 크기다 — 6차 라운드에서 이미 "같은 책 안에서도 진짜 페이지끼리 footer 위치가 다르다"(999 vs 973, 26px)는 걸 실측으로 확인해놓고도, 정작 leaf에는 (원래 자기 자신의 크기가 아니라) TARGET 페이지의 크기를 잘못 물려주고 있었다 — base는 맞았지만 leaf는 틀렸던 것.
  - **수정**: `getActiveRealPageRect()`를 **스왑 전/후 두 번** 부른다 — 스왑 *전* 값(`fromRect`, 지금 넘어가는 leaving 페이지 자신의 실측 크기)과 스왑 *후* 값(`toRect`, 새로 드러날 revealing 페이지의 실측 크기)을 따로 잰다. `js/portrait-flip.js`의 `playPortraitPageTurn()`에 `leavingWidth`/`leavingHeight` 파라미터를 새로 추가해서 leaf가 base와 다른(자기 자신의) 크기를 쓸 수 있게 했다(생략 시 기존처럼 `width`/`height`와 같다고 가정, 하위호환 유지). `jumpToPrevPage`/`goToNextPage`가 `fromRect`를 leaf에, `toRect`를 base/오버레이 컨테이너 자체에 각각 전달.
  - **부가 진단 로그**: `[FOOTERDEBUG] fromRect vs toRect`(reader.js, 트리거 시점 — 두 실측값과 `deltaH` 비교)와 `[FOOTERDEBUG] overlay base+leaf footer @ start`(portrait-flip.js, 기존 base만 재던 것에 `leafFooterY`/`leafFooterText`도 추가)로 이번 수정이 실제로 효과가 있는지 다음 캡처에서 바로 확인 가능하게 했다.
  - `sw.js`의 `CACHE_VERSION`을 `v58`→`v59`로 올림.
  - **검증**: dev 세션에서 새 로그(`fromRect vs toRect`, `overlay base+leaf footer @ start`의 `leafFooterY`/`leafFooterText`)가 정상 작동, 콘솔 에러 없음, 가로 모드 회귀 없음까지 확인 — 이 환경은 여전히 실제 페이지 크기가 항상 폴백값(고정 렌더 크기)으로 잡히므로(`fromRect`/`toRect`가 같은 값) 이번 수정의 진짜 효과(FROM/TO가 다를 때 leaf가 올바른 자기 크기를 쓰는지)까지는 검증 불가.
  - **여전히 남은 한계**: 이번에도 이 환경에서 실기기 재현은 불가능하다. 만약 이 수정 이후에도 "똑같이" 튄다면, leaf/base 크기 분리로는 설명 안 되는 종류의 문제(예: 진짜로 페인트/컴포지팅 레벨 버그, 혹은 footer 위치 문제가 아닌 완전히 다른 무언가)일 가능성이 커진다 — 그땐 DOM 측정 기반 접근을 접고 새 화면 녹화로 처음부터 다시 분석하는 쪽으로 전환해야 한다.
  - **이 항목의 수정도 사용자 재확인에서 반증됨("여전히 튀는데") — 아래 8차(근본 원인 자체를 제거) 항목 참고.**
- ✅ **완료·커밋됨 (2026-08-25, 같은 날 8차 라운드 — 📌 근본 원인 제거)**: 7차 수정도 사용자가 재확인 — 여전히 똑같이 튐. 사용자가 **직접 아키텍처 방향을 제안**했다: "페이지 영역을 absolute나 fixed로 디스플레이를 바꿔서 하는 방식은 어때?" — 지금까지 6~7라운드는 전부 "제멋대로 렌더링되는 진짜 페이지 크기를 실측해서 임시 카드가 쫓아가게" 만드는 방식이었는데, 이 제안은 반대 방향이었다: **진짜 페이지 자체를 처음부터 고정된 크기/좌표로 못박아서, 애초에 크기가 들쭉날쭉할 수 없게 만들자**는 것.
  - 돌이켜보니 이게 훨씬 근본적인 해법이었다 — 지금까지 계속 "진짜 페이지가 왜 인스턴스마다 다른 높이로 렌더링되는지"(라이브러리의 `.sft__wrapper` CSS 오타로 인한 containing-block 혼선 추정, 확정은 못 함)를 추적하면서 그 결과값을 실시간으로 베끼려고만 했지, **원인 자체를 원천 차단하는 건 시도한 적이 없었다.**
  - **수정**: `js/reader.js`의 `createPageElements()` — 세로(한 페이지) 모드일 때 새로 만드는 모든 `.page` 요소에 `position:absolute; top:0; left:0; width:${currentRenderWidth}px; height:${currentRenderHeight}px;`를 인라인 스타일로 직접 못박는다. 기존엔 `.page`의 `height:100%`가 조상 요소 체인(`.stf__wrapper`의 퍼센트 기반 종횡비 등, 오타 버그가 있는 그 체인)에 의존해서 인스턴스마다 결과가 흔들렸는데, 인라인 픽셀 값(inline style은 캐스케이드 최우선)으로 직접 지정하면 조상 체인이 뭘 하든(오타 버그 포함) 상관없이 모든 `.page` 인스턴스가 항상 정확히 같은 크기가 된다 — 그리고 그 값 자체가 `currentRenderWidth`/`currentRenderHeight`, 즉 **페이지네이션 알고리즘이 애초에 전제하고 있던 바로 그 크기**라 텍스트가 잘리거나 여백이 남을 위험도 없다.
  - **가로(2페이지 스프레드) 모드는 전혀 안 건드림** — `isSinglePageMode`일 때만 적용되는 분기라, PageFlip 자체 커얼 애니메이션이 이 요소들의 크기/위치를 직접 다루는 가로 모드는 원래도 이 버그가 없었고 이번에도 영향 없다.
  - 6~7차의 `getActiveRealPageRect()`/`fromRect`/`toRect` 측정 코드는 그대로 남겨뒀다 — 이제 진짜 페이지 크기가 항상 결정론적(`currentRenderWidth`/`currentRenderHeight`와 항상 일치)이라 사실상 무해한 안전망이 됐을 뿐, 제거할 필요는 없다고 판단(이미 있는 코드를 굳이 되돌려서 리스크를 추가하지 않음).
  - `sw.js`의 `CACHE_VERSION`을 `v59`→`v60`으로 올림.
  - **검증**: dev 세션에서 (1) 세로 모드로 책을 열어 실제 `.page` 요소의 `style` 속성을 직접 확인 — `position: absolute; top: 0px; left: 0px; width: 393.6px; height: 700px;`가 정확히 박혀있음(뷰포트 크기와 일치), 페이지 넘김 정상 작동(콘솔 에러 없음, `fromRect`/`toRect`가 이제 항상 동일한 값), (2) 가로(데스크톱) 모드 — 실제 `.page` 요소에 `style` 속성 자체가 아예 안 붙는 것 확인(`isSinglePageMode` 분기 밖이라 전혀 안 건드림), 정상 동작·회귀 없음.
  - **여전히 남은 한계**: 이 환경은 여전히 실제 화면 재생/실기기 검증이 불가능하다. 다만 이번엔 "진짜 페이지 인스턴스마다 크기가 다르다"는 실측된 근본 원인 자체를 구조적으로 제거하는 수정이라(추측이 아니라 인라인 스타일로 강제 고정), 지금까지 라운드 중 가장 직접적인 해법이다. 사용자가 실기기(PC 웹)에서 최종 확인해야 한다 — 이번에도 튄다면 정말로 DOM/CSS 레벨이 아닌 문제(페인트/컴포지팅, 혹은 애초에 다른 원인)로 봐야 한다.
  - **이 수정은 실제로는 전혀 적용되지 않았던 것으로 판명됨 — 아래 9차(진짜 진짜 근본 원인) 항목 참고. 실기기 콘솔 확인 한 줄이 8라운드 만에 처음으로 "내 수정이 실제로 코드에 반영됐는지" 자체를 직접 검증하게 해줬다.**
- ✅ **완료·커밋됨 (2026-08-25, 같은 날 9차 라운드 — 📌📌 진짜 진짜 근본 원인 발견 + 라이브러리 패치)**: 8차 수정("이전과 다를게 없는데")도 사용자가 재확인 — 정말 아무 변화도 없었다. 이번엔 처음으로 **"내가 설정한 값이 실제로 DOM에 적용됐는지"를 사용자가 직접 콘솔에서 확인**하게 했다 — `document.querySelector('#my-book .page')?.getAttribute('style')`를 실행하게 하니 결과가 **`'display: none;'`뿐**이었다. 8차에서 분명히 `position:absolute; width:...px; height:...px;`를 인라인으로 넣었는데, 실제 DOM에는 그게 하나도 안 남아있었던 것 — **8차 수정 자체가 처음부터 적용된 적이 없었다**(캐시 문제가 아니라, 코드는 돌았지만 결과가 지워졌다).
  - **왜 지워졌는지**: `js/vendor/page-flip.browser.js` 소스를 다시 뒤져서 `HTMLPage`/`HTMLRender`의 그리기 코드가 `element.style.cssText = "display: none"`처럼 **`cssText` 전체를 통째로 대입**하는 방식으로 페이지를 그린다는 걸 확인했다 — 개별 프로퍼티 설정(`el.style.display = 'none'`)이 아니라 `style` 속성 자체를 완전히 교체하는 방식이라, 우리가 `createPageElements()`에서 미리 넣어둔 인라인 스타일은 라이브러리가 페이지를 렌더링하는 순간(매 렌더 사이클마다) 통째로 사라질 수밖에 없었다.
  - **더 위로 올라가서 찾은 진짜 원인**: 그 렌더링이 사용하는 페이지 위치/크기 계산(`Render.calculateBoundsRect()`)이 `getBlockWidth()`/`getBlockHeight()`를 호출하는데, 이 둘은 `this.app.getUI().getDistElement().offsetWidth`/`offsetHeight`(=`#my-book`의 실측 크기)를 **호출될 때마다 매번 실시간으로 다시 측정**하고 있었다. `calculateBoundsRect()`는 렌더 사이클마다(사실상 페이지가 그려질 때마다) 호출되므로, 이 실시간 측정값이 세션 도중 조금이라도 흔들리면(정확한 원인은 미확인 — 서브픽셀 반올림, 다른 곳의 미세한 리플로우 등으로 추정) 그 이후 그려지는 모든 페이지가 그 새 값을 그대로 물려받는다 — **8라운드 내내 우리가 뭘 해도 라이브러리가 매번 자기 실측값으로 다시 덮어썼던 것**이 이제야 설명된다.
  - **수정**: `js/vendor/page-flip.browser.js`의 `getBlockWidth()`/`getBlockHeight()`를 패치 — DOM을 매번 다시 재는 대신, **PageFlip 인스턴스당 딱 한 번만 측정해서 캐싱**하도록 바꿨다(`this._cachedBlockWidth`/`this._cachedBlockHeight`에 저장, 이후 호출은 캐시된 값 반환). 진짜 리사이즈/재빌드가 일어나면 `buildFlipBook()`(js/reader.js)이 항상 **새 PageFlip 인스턴스**를 만드므로, 캐시는 정확히 그 시점에만 자연스럽게 초기화된다 — 별도 무효화 로직이 필요 없다. `js/vendor/page-flip.browser.js` 상단 히스토리 주석에 (j) 항목으로 자세히 기록(패치 개수 "8개"→"10개"로 갱신, (i) 항목이 이미 있었던 걸 이번에 알아챔).
  - `js/reader.js`의 8차 수정(`createPageElements()`의 인라인 스타일 설정)은 **무효했던 걸로 확인됐으니 제거** — 대신 왜 거기가 아니라 vendor 파일 쪽이 진짜 원인이었는지 설명하는 주석만 남겨뒀다.
  - **이 패치는 (a)~(i)와 성격이 다르다**: 지금까지의 8라운드+@ 패치는 전부 라이브러리 자체 애니메이션(portrait의 clip-path 커얼) 관련이라 지금은 무의미해졌지만(portrait이 이제 이 라이브러리의 애니메이션을 아예 안 씀), 이번 (j) 패치는 **portrait/landscape, 애니메이션 유무와 무관하게 모든 렌더링 경로에 영향**을 준다 — `turnToPage()`(우리가 쓰는 무애니메이션 점프)도 내부적으로 `update()`를 거치므로 똑같이 이 버그의 영향을 받는다.
  - `sw.js`의 `CACHE_VERSION`을 `v60`→`v61`로 올림.
  - **검증**: 실제 커밋되는 `js/vendor/page-flip.browser.js`를 로컬 정적 서버로 서빙 — (1) 패치된 스크립트가 파싱 에러 없이 정상 로드(콘솔 에러 0건), (2) dev 로그인으로 세로 모드 책 열기 → 정상 표시 → 페이지 넘김 정상 작동(콘솔 에러 없음, 페이지 인디케이터 정확히 갱신), (3) 가로(데스크톱) 모드 — `#book-spine` 정상 표시, 페이지 넘김 정상 작동, 콘솔 에러 없음, 회귀 없음. Python으로 패치 대상 문자열이 파일 안에 정확히 1번만 존재하는 것도 사전에 확인해서 의도치 않은 다른 곳을 건드리지 않았음을 확인.
  - **여전히 남은 한계**: (1) 실시간 측정값이 정확히 "왜" 세션 도중 흔들리는지(무엇이 그 몇 px 차이를 만드는지)는 여전히 미확인이다 — 이번 패치는 그 원인을 몰라도 되게 만드는(캐싱으로 아예 재측정을 안 하는) 방어적 수정이다. (2) 이 환경은 여전히 실제 페인트/실기기 검증이 불가능하다 — 특히 이번 발견 자체가 "8라운드 동안 실제로 적용된 적 없는 수정을 검증됐다고 착각했다"는 사실에서 나온 것이므로, **이번에도 실기기에서 최종 확인 전에는 절대 "해결됐다"고 단정하지 말 것.** 다음에 확인할 사람도 반드시 사용자가 브라우저 콘솔에서 직접 값을 찍어보게 해서(예: `pageFlip`이 모듈 내부 변수라 직접 접근은 안 되지만, `getBoundingClientRect()`류 실측은 항상 가능) "코드가 실제로 반영됐는지"부터 검증하는 습관을 들일 것 — 이번 라운드의 가장 큰 교훈이다.
  - **9차 패치도 사용자 재확인에서 반증됨("아 똑같아") — 하지만 이번엔 사용자가 결정적인 관찰을 직접 제공해서 진짜 원인이 밝혀졌다. 아래 10차 항목 참고.**
- ✅ **완료·커밋됨 (2026-08-25, 같은 날 10차 라운드 — 📌📌📌 진짜 근본 원인 확정 + CSS 구조 변경)**: 9차 패치(`getBlockWidth`/`getBlockHeight` 캐싱) 후에도 사용자가 "아 똑같아"라고 재확인 — 캐싱까지 했는데도 안 바뀌었다는 건 크기/위치 계산의 "흔들림" 자체가 원인이 아닐 수 있다는 뜻이라, 사용자에게 시크릿 창으로 캐시 문제부터 배제해달라고 요청(시크릿 창에서도 동일 — 캐시 문제 아님 확정). 그리고 사용자가 직접 관찰한 핵심 단서를 줬다: **"`.page-content` 클래스의 div 바로 밑에 붙어서 세로 크기에 따라 위치가 달라지는 것처럼 보임."**
  - **이게 결정적이었다**: `.page-content` 바로 아래 붙어서 "내용물 세로 크기"에 따라 위치가 바뀐다는 건, `.page-footer`가 `justify-content:space-between`으로 바닥에 고정되는 게 아니라 **일반 문서 흐름(block flow)으로 `.page-content`를 뒤따라가고 있다**는 뜻 — 즉 `.page`의 `display:flex`가 어디선가 깨지고 있다는 신호였다.
  - **소스에서 확인**: `js/vendor/page-flip.browser.js`의 `HTMLPage.simpleDraw()`(실제 페이지가 애니메이션 없이 "그냥 보여지는" 상태 — 우리가 쓰는 `turnToPage()`가 정확히 이 경로)가 `this.element.style.cssText` 를 **통째로** 다음 문자열로 교체한다: `` `position: absolute; display: block; height: ${...}px; ...` `` — **`display: block`을 인라인으로 명시적으로 박아넣는다.** 인라인 스타일은 우리 CSS 클래스의 `display: flex`를 그대로 덮어쓰므로, 실제 페이지에서는 `.page`가 **항상 일반 block 컨테이너**로 렌더링되고 있었다. flex가 깨지니 `.page-content{flex:1}`도 무효가 되어 자기 텍스트 분량만큼만 차지하는 일반 블록으로 되돌아갔고, `.page-footer`는 그 바로 아래 붙어서 페이지마다(텍스트 분량마다) 다른 위치에 나타났다 — 지금까지 실측했던 모든 수치(999 vs 973 등)가 사실은 "페이지마다 텍스트 분량이 달라서 생기는 자연스러운 차이"였을 가능성이 크다(우리가 계속 "흔들림/버그"로 여기고 쫓아다녔던 것).
  - **왜 8~9차 수정이 전부 무효했는지 이제 설명된다**: 8차(`.page`에 인라인으로 크기 고정)는 `simpleDraw()`의 `cssText` 전체 교체로 지워졌고, 9차(`getBlockWidth/Height` 캐싱)는애초에 **`.page`의 크기 자체(전체 높이)** 문제가 아니라 **`.page` *안에서* footer가 고정되는 방식** 문제라 애초에 대상이 달랐다 — `.page`의 전체 높이(`boundsRect.height`)는 사실 계속 일정했을 가능성이 높고(우리 로그에서도 `pageH`는 항상 1069로 고정이었다), 진짜 문제는 그 안에서 `.page-content`/`.page-footer`가 flex로 안 나뉘고 있었다는 것.
  - **수정**: `.page-footer`를 flex(`justify-content:space-between` + `flex-shrink:0`)가 아니라 **`position:absolute; left:0; right:0; bottom:0;`**로 바닥에 직접 고정하도록 바꿨다 — `position:absolute` 자식은 부모(`.page`)가 `position`만 static이 아니면(`.page`는 항상 `position:relative`이거나 라이브러리가 덮어써도 `position:absolute`, 어느 쪽이든 "positioned") 되므로, 부모의 `display`가 `flex`든 `block`이든 **완전히 무관하게 항상 똑같이 동작한다** — 라이브러리가 무슨 값으로 덮어쓰든 다시는 안 깨진다. `.page-content`도 `position:absolute; top/left/right:0; bottom:25px;`(footer 높이 20px + 위 여백 5px 예약)로 맞춰 바꿨다. `js/styles.css`의 `.page`/`.page-content`/`.page-footer` 세 규칙에 이 경위를 상세히 주석으로 남겼다.
  - `sw.js`의 `CACHE_VERSION`을 `v61`→`v62`로 올림.
  - **검증**: ⚠️ 이번엔 이 환경에서 CSS 변경이 실제로 반영됐는지 확인하는 것 자체가 한 번 더 이 환경의 캐싱 문제에 걸렸다 — 서비스워커 해제·Cache Storage 삭제·새 탭·강제 새로고침을 다 해도 `document.styleSheets`가 계속 예전 규칙을 보여줬다. `fetch('/styles.css', {cache:'no-store'})`로 직접 받아보면 새 내용이 맞는데(네트워크 레이어는 정상), 이미 파싱되어 적용된 `<link>`의 스타일시트 자체는 안 바뀌었다 — `<link>`의 `href`를 `styles.css?bust=<timestamp>`로 코드에서 직접 바꿔치기하고 나서야 `document.styleSheets`에 새 규칙(`.page-content{position:absolute; inset:0px 0px 25px}`, `.page-footer{position:absolute; left:0; right:0; bottom:0}`)이 정확히 반영된 것을 확인했다. 이 상태로 (1) 세로 모드 — 애니메이션 오버레이의 실제 렌더링 결과를 실측: `content bottom=675px`, `footer top=680px`(정확히 5px 간격, 의도한 대로), `footer bottom=700px`(page bottom과 일치) — 절대배치가 수치상 정확히 의도대로 동작. 콘솔 에러 없음. (2) 가로(데스크톱) 모드 — `#book-spine` 정상 표시, 페이지 넘김 정상, 콘솔 에러 없음, 회귀 없음.
  - **여전히 남은 한계**: 이 환경은 여전히 실기기 최종 확인이 불가능하고, 이번에도 "코드가 실제로 반영됐는지"부터 우회 방법을 찾아야 했을 만큼 캐싱 문제가 심했다. 다만 이번 원인 규명은 **사용자가 실제 화면에서 직접 관찰한 시각적 단서**("`.page-content` 바로 밑에 붙어서 세로 크기 따라 위치가 바뀜")로 정확히 좁혀졌고, 그 단서가 소스 코드의 정확한 한 줄(`simpleDraw()`의 `display:block` 인라인 대입)과 완벽히 들어맞았다는 점에서 지금까지 라운드 중 가장 신뢰도 높은 진단이다 — `position:absolute` 기반 수정은 `.page`의 `display` 값이 무엇이든 구조적으로 절대 안 깨지므로, 이번에도 안 되면 정말 CSS/DOM 레벨이 아닌 문제로 봐야 한다.
  - **📌 실기기(PC 웹) 확인 완료 — "위로 튀는 현상은 없어졌다"** — 10차에 걸친 "footer 위치 점프" 버그 사가의 실질적 종착점. 다만 부작용 하나가 같이 발견됨: 아래 11차 항목 참고.
- ✅ **완료·커밋됨 (2026-08-25, 같은 날 11차 라운드 — padding 복원)**: 10차 수정을 실기기에서 확인한 사용자가 "위로 튀는 현상은 없어졌는데 여백도 없어졌네"라고 스크린샷과 함께 피드백 — footer가 화면 맨 아래, 본문이 맨 위/좌우 가장자리에 거의 붙어서 렌더링되고 있었다.
  - **원인**: `position:absolute` 자식의 좌표 기준(containing block)은 부모의 "패딩 박스"다 — 즉 top/left/right/bottom을 단순히 `0`으로 주면 부모(`.page`)의 `padding` 자체를 건너뛰고 패딩까지 포함한 바깥쪽 가장자리에 딱 붙어버린다. 10차에서 `.page-content`/`.page-footer`를 절대배치로 바꾸면서 `top:0; left:0; right:0;` / `left:0; right:0; bottom:0;`으로 뒀던 게 정확히 이 함정에 걸렸다 — `.page`의 반응형 padding(`clamp(14px,2.5vw,24px)` 위, `clamp(10px,1.5vw,15px)` 아래, `--reading-padding-x` 좌우)이 절대배치 자식들에게는 전혀 안 먹히고 있었다.
  - **수정**: `.page`의 위/아래 padding 값을 `--page-pad-top`/`--page-pad-bottom` CSS 변수로 뽑아서 `padding` 선언 자체에도 쓰고, `.page-content`/`.page-footer`의 절대배치 오프셋에도 그대로 재사용한다(좌우는 이미 있던 `--reading-padding-x` 변수를 그대로 재사용). `.page-content`는 `top:var(--page-pad-top); left/right:var(--reading-padding-x); bottom:calc(var(--page-pad-bottom) + 25px);`(25px는 footer 자체 예약 공간, 아래쪽 padding과는 별개로 더해짐), `.page-footer`는 `left/right:var(--reading-padding-x); bottom:var(--page-pad-bottom);`로 값을 명시했다.
  - **부수 발견·수정**: `@media (max-height: 480px)`(가로가 짧은 모바일 화면)에서 `.page { padding-top: 10px; padding-bottom: 8px; }`로 longhand를 직접 오버라이드하던 규칙이 있었는데, 이제 `.page-content`/`.page-footer`가 longhand가 아니라 변수를 직접 참조하므로 이 규칙만으로는 절대배치 자식에 반영이 안 되는 것을 미리 발견해서 같이 고쳤다 — `padding-top`/`padding-bottom` 대신 `--page-pad-top`/`--page-pad-bottom` 변수 자체를 오버라이드하도록 변경(실제 재현 전에 코드 검토만으로 미리 잡은 잠재 버그).
  - `sw.js`의 `CACHE_VERSION`을 `v62`→`v63`으로 올림.
  - **검증**: dev 세션(phone 뷰포트, 393px)에서 애니메이션 오버레이의 실제 렌더링 좌표를 실측 — `contentRect: top=14, left=20, right=373.6(=393.6-20)`, `footerRect: top=670, bottom=690(=700-10)` — `--page-pad-top`(14px @ 393.6px 너비), `--reading-padding-x`(20px), `--page-pad-bottom`(10px)의 clamp() 계산값과 정확히 일치. 콘솔 에러 없음, 가로 모드 회귀 없음.
  - **남은 한계**: 이번에도 실기기 최종 확인 전까지는 확정 아님 — 다만 10차의 "튐" 문제 자체는 이미 실기기에서 해결 확인됐고, 이번 11차는 순수하게 그 수정이 만든 시각적 부작용(여백 소실)을 복원하는 것이라 리스크가 낮다.
  - **📌 이 항목으로 "footer 위치 점프" 사가(6~11차, 이 문서에서만 총 6라운드) 마무리.** 실기기 확인 완료(튐 해결) + 여백 복원까지 커밋됐다.
- ✅ **완료·커밋됨 (2026-08-25, 같은 날 12차 라운드 — 가로 모드도 세로 모드와 통일)**: footer 버그가 해결된 직후, 사용자가 "한 페이지 넘어가는 방식으로 양쪽 페이지 넘어가는 것도 수정해줘 통일성 있게 하고싶어"라고 요청 — 가로(2페이지 스프레드) 모드도 세로 모드와 **완전히 같은 rotateY 카드 뒤집기 엔진**을 쓰도록 확장했다. ⚠️ 가로 모드는 이 사가 전체를 통틀어 단 한 번도 버그가 없었던 코드였다 — 이 변경은 순수히 사용자가 요청한 "통일성"을 위한 것이지, 가로 모드에 문제가 있어서가 아니다. 즉 **안정적으로 잘 작동하던 코드를 검증 안 된 새 코드로 교체하는** 리스크를 새로 지는 작업이라는 점을 분명히 인지하고 진행했다.
  - **설계 확인**: 구현 전에 AskUserQuestion으로 "오른쪽만 뒤집히는 실제 책 UX"와 "양쪽이 대칭으로 동시에 회전"두 옵션을 물었더니, 사용자가 "현재 한 페이지에 구현된 것과 동일하게 보이면 된다"고 답함 — 가장 문자 그대로의 해석(좌/우 패널이 각자 독립적으로, 세로 모드와 완전히 같은 로직으로 동시에 회전)으로 진행.
  - **아키텍처**: `js/portrait-flip.js`를 리팩터링 — 기존 단일 패널 로직(leaf/base 카드 생성+애니메이션)을 `buildPanelAnimation()`으로 뽑아내고, 기존 `playPortraitPageTurn()`(세로, 패널 1개)은 이 헬퍼를 한 번만 부르는 얇은 래퍼가 됐다(시그니처·동작 100% 그대로 유지, 세로 모드 회귀 없음). 새 `playSpreadPageTurn({stage, direction, panels})`가 패널 1~2개를 동시에 애니메이션하고, **모든 패널이 다 끝나야** 공유 `onDone`을 부른다. `activeAnimation`(겹쳐 눌림 방지 가드)을 단일 핸들에서 **핸들 배열**로 바꿔서 세로/가로 양쪽에서 재사용 — `cancelPortraitFlip()`도 배열을 순회하도록 수정.
  - **가로 모드 흐름**(`js/reader.js`의 `jumpToPrevPage`/`goToNextPage`): 기존 `pageFlip.flipPrev()`/`flipNext()` 호출을 전부 없애고, 세로 모드와 똑같은 패턴을 좌/우 두 페이지에 적용했다 — `getActiveRealSpreadRects()`(신설, `getActiveRealPageRect()`의 2패널 버전 — StPageFlip이 실제 페이지에 붙여주는 `--left`/`--right` 클래스로 좌/우를 구분)로 스왑 전(FROM) 실측 → `swapRealPageForFlip(targetLeftGlobal)`로 진짜 페이지 전환(숨겨진 채로, 왼쪽=짝수 로컬 인덱스만 지정하면 오른쪽은 라이브러리가 자동으로 딸려온다) → 스왑 후(TO) 실측 → `buildSpreadPanels()`(신설)로 좌/우 패널 데이터 조립 → `playSpreadPageTurn()` 호출. "다음"은 `globalIndex+2`, "이전"은 `globalIndex-2`(스프레드가 항상 짝수=왼쪽으로 정렬되므로 한 번에 2페이지씩 이동 — 라이브러리의 기존 `e.data`가 항상 스프레드의 왼쪽 인덱스였다는 것에서 역산해 확인).
  - **경계 처리**: 마지막 스프레드가 홀수 페이지라 한쪽이 없는 경우(orphan) 등 `getActiveRealSpreadRects()`가 그 쪽을 못 찾으면(`--left`/`--right` 요소 자체가 없음) `buildSpreadPanels()`가 그 패널을 그냥 뺀다 — 패널이 하나도 안 남으면(극단적) 애니메이션 없이 `finishManualPageTurn()`만 호출(진짜 페이지 전환 자체는 이미 끝나 있으므로 기능적으로는 문제없고 시각 효과만 생략).
  - **의도적으로 미해결로 남긴 것**: `#book-spine`(스프레드 중앙의 책등 그림자, z-index:5)이 우리 애니메이션 오버레이(z-index:20, 좌우 패널이 스테이지 전체를 덮음)보다 아래에 있어서, 애니메이션 진행 중(~1초) 동안 책등 그림자가 안 보이다가 끝나면 다시 나타난다 — 기능에는 영향 없는 사소한 코스메틱 디테일이라 이번엔 손 안 댔다. 사용자가 실기기에서 신경 쓰이면 다음에 처리.
  - `sw.js`의 `CACHE_VERSION`을 `v63`→`v64`로 올림.
  - **검증**: dev 세션(데스크톱 1280px, 진짜 가로/2페이지 스프레드 모드)에서 — (1) 실제 `.page` 요소 중 `--left`/`--right` 클래스가 붙은 걸 강제로 `display:block`시켜서(이 환경은 PageFlip 자체의 `display:none↔block` 전환이 rAF 정지로 안 되는 기존 한계) `getActiveRealSpreadRects()`가 정상적으로 둘 다 찾아내는 것 확인, (2) next 트리거 — `.portrait-flip-stage`가 **정확히 2개**(좌/우 각각) 생성됨, `[FOOTERDEBUG]` 로그로 좌측 패널이 leaving="15/41"→revealing="17/41", 우측 패널이 leaving="16/41"→revealing="18/41"로 **정확히 페어링**되는 것 확인, 두 패널이 정확히 동시에(오차 없이 같은 타임스탬프) 완료되는 것 확인, 카운터가 정확히 15→17로 갱신(2페이지 단위 이동 확인), (3) prev 트리거 — 17→15로 정확히 복귀, (4) 3연타 스트레스 테스트 — `isPortraitFlipAnimating()` 가드가 정상 작동해 스테이지가 2개로만 유지되고 딱 한 번만 진행(15→17), (5) 세로(모바일 393px) 모드 재검증 — 리팩터링 후에도 회귀 없음, 정상 작동. 콘솔 에러 전 구간 없음.
  - **여전히 남은 한계**: 이 환경은 실제 PageFlip 페이지 렌더링 자체가 동작하지 않아(문서 hidden으로 인한 rAF 정지) `getActiveRealSpreadRects()`가 자연 상태에서는 항상 폴백 경로를 탄다 — 이번엔 특별히 `--left`/`--right` 요소를 수동으로 강제 표시해서 실제 코드 경로(2패널 동시 애니메이션, 정확한 좌우 페어링, 동시 완료)까지 검증했지만, 강제로 보이게 한 요소들의 정확한 좌표값 자체는 라이브러리의 진짜 포지셔닝 로직을 안 거쳐서 신뢰할 수 없다(둘 다 `left:0px`로 나온 것은 테스트 편법의 부작용이지 실제 버그 아님). **가로 모드는 원래 안정적이던 코드를 완전히 새 코드로 교체한 것이라, 실기기에서 특히 꼼꼼히 확인이 필요하다** — next/prev 양방향, 여러 번 연속으로, 책 앞/뒤 경계 근처(마지막 홀수 페이지 스프레드 등)까지.
  - **이 항목은 사용자에게 보여준 직후 바로 반려됨 — 아래 13차(한쪽만 넘어가게 수정) 항목 참고.**
- ✅ **완료·커밋됨 (2026-08-25, 같은 날 13차 라운드 — 가로 모드, 양쪽 동시 회전 → 한쪽만 회전)**: 12차 결과물을 보여주자마자 사용자가 즉시 반려 — "아니... ㅋㅋ 양쪽 페이지가 같이 넘어가는데 이게 무슨 책넘기는 효과야... 한쪽만 넘어가야지." 12차의 AskUserQuestion 답변("현재 한 페이지에 구현된 것과 동일하게 보이면 된다")을 "양쪽 다 독립적으로 동시에 회전"으로 해석한 게 틀렸던 것 — 실제 책처럼 "다음"은 오른쪽 페이지만, "이전"은 왼쪽 페이지만 넘어가고, 반대쪽은 (이미 애니메이션 시작 전에 숨겨진 채로 실제 DOM 전환이 끝나 있으므로) 그냥 그 자리에서 바로 새 내용으로 바뀌어 있어야 한다는 요구.
  - **수정**: 12차에서 만든 `buildSpreadPanels()`(좌/우 패널 데이터를 둘 다 조립하는 함수) 자체는 그대로 두고, `js/reader.js`의 `jumpToPrevPage()`/`goToNextPage()` 호출부에서 그 결과에 `.filter((panel) => panel.side === 'left')`(prev) / `.filter((panel) => panel.side === 'right')`(next)를 추가해서 실제로 애니메이션시키는 패널을 한쪽만 남겼다. 반대쪽 페이지는 `swapRealPageForFlip()`이 애니메이션 시작 전에 이미 실제 DOM을 조용히 바꿔놨으므로(12차부터 있던 구조) 별도 처리 없이 그 자리에서 즉시 바뀐 것처럼 보인다. 필터 결과가 빈 배열이면(해당 쪽 페이지가 애초에 없는 경계 케이스) 애니메이션 없이 `finishManualPageTurn()`만 호출.
  - `js/portrait-flip.js`의 `playSpreadPageTurn()`/`buildPanelAnimation()` 자체는 안 건드림 — 여전히 1~2개 패널을 다 지원하니, 나중에 다시 필요해지면(예: 사용자가 마음을 바꾸면) 필터만 없애면 원래대로 돌아간다. 상단 히스토리 주석에 이번 정정 내용 추가.
  - `sw.js`의 `CACHE_VERSION`을 `v64`→`v65`로 올림.
  - **검증**: dev 세션에서 (1) 가로(1280px) 모드 — `--left`/`--right` 강제 표시 후 next 트리거 → `.portrait-flip-stage`가 **정확히 1개**(12차의 2개에서 감소)만 생성, `[FOOTERDEBUG]` 로그에 `spread-right` 항목만 찍히고 `spread-left`는 전혀 없음(leaving="- 18 / 41 -"→revealing="- 20 / 41 -"), 카운터가 19("19 / 41 (46%)")로 정확히 갱신(target-left=19, revealing-right=20과 일치), 완료 후 스테이지 0개로 정리. prev 트리거도 대칭 확인 — 스테이지 1개, `spread-left`만 로그(leaving="- 19 / 41 -"→revealing="- 17 / 41 -"), 카운터 17로 갱신. (2) 세로(393px) 모드 재검증 — 리팩터링 영향 없이 정상 작동(넘기기 후 카운터 정확히 갱신, 스테이지 정리됨), 콘솔 에러 없음.
  - **여전히 남은 한계**: 12차와 동일한 환경 제약(실제 PageFlip 렌더링이 이 환경에서 자연 발생하지 않아 `--left`/`--right` 강제 표시로 우회) — 이번엔 두 번째 가로 모드 구현이고, 사용자가 이미 한 번 "말했던 것과 다르게 구현됨"을 직접 눈으로 확인하고 지적한 전례가 있으므로, 실기기 확인 전까지 "해결됐다"고 단정하지 않는다. 사용자가 실기기에서 다음/이전 페이지 넘김이 각각 한쪽 페이지만 넘어가는 느낌으로 보이는지 확인 필요.
- ✅ **완료·커밋됨 (2026-08-25, 세션 재개 — 목표각 (B) 180deg 채택 + 번호 갱신 재설계)**: 위 결정 대기 항목에 사용자가 "b로 가고, 번호 갱신은 다시 잡아보자"고 응답 — 목표각을 180deg로 늘리되, 이번엔 그 부작용(번호 갱신 지연 재발)을 근본적으로 막는 설계로 다시 잡았다.
  - **핵심 아이디어**: 단순히 100% 키프레임의 각도만 91→180으로 바꿨다면, 같은 `ease-out` 곡선이 목표(180deg)의 절반(90deg)에 도달하는 시점이 훨씬 앞당겨져서 "페이지가 이미 넘어간 것처럼 보이는 시점"과 "번호 갱신(`animationend`) 시점" 사이에 다시 눈에 띄는 간극이 생겼을 것 — 정확히 6~7차에서 고생해서 잡았던 그 버그의 재발이다. 대신, **CSS 키프레임의 % 지점은 그 구간의 보간 곡선 모양과 무관하게 항상 duration의 정확한 시간 비율**이라는 스펙 사실을 이용해, "화면에서 사라지는 지점"을 곡선에서 실측으로 찾는 대신 키프레임 자체에 **정확한 % 값으로 못박았다.**
  - **구현**: `styles.css`의 `@keyframes portrait-flip-leaf-next/-prev`를 0%/100% 2단에서 **0%/94%/100% 3단**으로 바꿈 — 0%(0deg)→94%(±91deg, 기존과 동일한 "사라지는 지점", 여기까지 `ease-out` 적용)→100%(±180deg, scaleX/translateZ/box-shadow는 94% 값 그대로 얼리고 rotateY만 계속 돎, `linear` 적용 — 어차피 안 보이니 곡선 모양 무의미). 타이밍 함수를 키프레임 자체(`animation-timing-function` per-keyframe-stop)에 박아서, `js/portrait-flip.js`의 `leaf.style.animation`은 이제 duration만 지정(`... ${duration}ms linear`, 이 최상위 값은 키프레임이 전부 자체 함수를 가지므로 사실상 무시됨).
  - **효과**: `animationend`(→`finishManualPageTurn`, 번호 갱신)는 여전히 정확히 duration(100% = 1000ms)에 발동 — 목표각을 180으로 늘렸어도 번호 갱신 타이밍은 **조금도 나빠지지 않는다.** 오히려 이전엔 "94%"가 특정 곡선+각도 조합에서 실측(Web Animations API 스크러빙)으로 찾아낸 근사치였던 반면, 이번엔 94%를 키프레임에 직접 선언했으니 브라우저/기기가 곡선을 어떻게 계산하든 스펙상 정확히 그 시점이 보장된다 — 근사치 의존을 없애 이전보다 더 견고해졌다.
  - **적용 범위**: `portrait-flip-leaf-next/-prev` 키프레임은 세로(단일 페이지)/가로(스프레드) 모드가 `buildPanelAnimation()`을 통해 공유하므로, 이번 변경은 양쪽 모드 모두에 동일하게 적용된다("통일성" 원칙과 일치).
  - `sw.js`의 `CACHE_VERSION`을 `v65`→`v66`으로 올림.
  - **검증**: 이번 세션은 (드물게) 이 환경에서 실제 뷰어를 열어 진짜 페이지 넘김을 트리거하는 것까지 가능했다(과거 라운드들의 CDN 차단이 이번엔 없었음 — 정확한 이유 불명, 원래도 세션마다 편차가 있었음). dev 로그인 + phone(393×700)/desktop(1280×800, 진짜 landscape) 뷰포트에서 — (1) `document.styleSheets`로 새 3단 키프레임(0%/94%/100%, 각 세그먼트 타이밍 함수 포함)이 의도한 그대로 파싱됨을 확인, (2) `pointerdown`/`pointerup`을 `#book-stage`에 직접 디스패치해 실제 다음/이전 페이지 넘김을 트리거(세로 모드 next/prev 각각, 가로 모드는 `--left`/`--right` 강제 표시 후 next/prev 각각) — 트리거 시점부터 `performance.now()`로 오버레이 제거·페이지 카운터 갱신 시점을 실측한 결과 세로/가로 전부 **~1090~1160ms**(duration 1000ms + safetyTimer 여유 80ms + 폴링 오차, 이 환경은 여전히 `document.hidden`이라 매번 safetyTimer 경로를 탐 — 과거 라운드와 동일)로 **변경 전과 동일한 타이밍**임을 확인, (3) 오버레이 제거와 카운터 갱신이 항상 같은 순간에 함께 일어남(간극 없음), (4) 세로 모드 3연타(애니메이션 도중 겹쳐 누르기) — `isPortraitFlipAnimating()` 가드 정상 동작, 딱 1페이지만 진행, 잔여 오버레이 없음, (5) 가로 모드 next/prev 각각 — 패널 정확히 1개(방향에 맞는 쪽만), 카운터가 2페이지 단위로 정확히 이동(3→5, 5→3), 잔여 오버레이 없음. 콘솔에 이번 변경으로 인한 새 에러 없음(남은 에러 2건은 검증 스크립트 자체의 실수로 인한 것, 앱 코드와 무관).
  - **여전히 남은 한계**: 이 환경은 `document.hidden`이라 `animationend`가 실제로 뜨는지, 그리고 rotateY가 180도까지 실제로 매끄럽게(안 보이는 구간이라도 끊김 없이) 도는지는 여전히 눈으로 확인할 방법이 없다 — 다만 이번 수정은 애초에 "안 보이는 구간의 곡선 모양"을 다루는 것이라 리스크가 낮고, 핵심 검증 대상(번호 갱신 타이밍이 변경 전과 동일한지)은 실측으로 확인됐다. 최종 판단(180도까지 도는 것 자체가 원하던 "책장 넘김" 룩인지)은 사용자가 실기기에서 확인 필요.
- ✅ **완료·커밋됨 (2026-08-25)**: 사용자 요청 — "캐시 버전을 실사용할 때도 책 볼 때 방해되지 않게 뷰어 화면 UI 상단에 작게 넣어줘." `sw.js`의 `CACHE_VERSION`을 실사용 중에도(예: 배포했는데 옛날 버전이 계속 뜨는 캐싱 문제) 스크린샷 한 장으로 바로 확인할 수 있게 아주 작은 배지를 추가했다.
  - **값의 출처**: `CACHE_VERSION` 상수를 페이지 쪽에 따로 복붙해서 중복 선언하지 않았다 — Cache Storage API(`caches.keys()`)는 페이지(윈도우) 컨텍스트에서도 서비스워커가 연 캐시 이름을 그대로 읽을 수 있고(`js/storage-stats.js`가 이미 같은 방법을 씀), 이 앱은 캐시 네임스페이스를 하나만 쓰므로(`sw.js`) `caches.keys()`의 첫 항목이 곧 `CACHE_VERSION`이다 — 값이 두 곳에 있으면 하나만 고치고 잊어버릴 위험이 생기므로 항상 원본에서 읽어오게 했다.
  - **구현**: `index.html`의 `#sidebar`(뷰어 상단 바, `position:fixed`) 안에 `<span id="app-version-badge">`를 추가하고, `js/main.js`가 로드 시 `caches.keys()`로 읽어와 `bookify-shell-` 접두어를 뗀 값(예: `v67`)을 텍스트로 채운다(첫 방문 등 캐시가 아직 없으면 조용히 비워둠). `styles.css`에서 `#sidebar`를 기준으로 `position:absolute; top:0; right:6px`로 배치 — `.sidebar-header`의 flex 레이아웃(뒤로가기/제목/책갈피 3분할, 좌우 대칭이라 제목이 가운데로 보임)을 전혀 건드리지 않고, 아이콘 버튼들이 시작되는 y=10px보다 위(0~9px)에만 있어서 겹치지 않는다. `font-size:9px; opacity:0.3; pointer-events:none`으로 평소엔 거의 안 보이고 클릭도 안 먹는다.
  - **읽기 중 안 보이는 이유**: 별도 코드 없이 `#sidebar`의 자식이라는 것만으로 자동 해결된다 — 몰입모드(`body.immersive #sidebar { transform: translateY(-100%) }`)로 상단 바 자체가 슬라이드돼 숨을 때 배지도 같이 딸려서 숨는다.
  - `sw.js`의 `CACHE_VERSION`을 `v66`→`v67`로 올림(새 파일을 추가한 건 아니지만, 이 프로젝트 관례대로 코드가 바뀌면 항상 올린다).
  - **검증**: dev 로그인 + 데스크톱(1280×800) 뷰포트에서 실제 뷰어를 열어 (1) 배지 텍스트가 실제 Cache Storage 값과 정확히 일치(`v67`)하는 것 확인, (2) 배지와 책갈피 아이콘 버튼(`#page-bookmark-toggle-btn`)의 `getBoundingClientRect()`를 직접 비교해 겹치지 않음(배지 bottom=9px, 버튼 top=10px, 1px 여유) 확인 — 처음엔 top:3px로 뒀다가 5px 겹치는 걸 발견해서 top:0px로 조정, (3) 콘솔 에러 없음. ⚠️ 몰입모드로 숨는지는 `document.body.classList.add('immersive')`를 직접 실행해 `#sidebar`의 computed `transform`을 확인하려 했으나, **이 세션의 브라우저 자동화 환경 자체가 이번엔 화면을 전혀 컴포지팅하지 않는 상태**(스크린샷 요청도 "the Browser pane is not displayed" 에러로 실패)라 클래스를 추가해도 `getComputedStyle().transform`이 갱신되지 않는 것까지 확인됨 — `element.matches('body.immersive #sidebar')`는 정상적으로 `true`를 반환해서 CSS 선택자 매칭 자체는 문제없이 되고 있는데도 그랬다. 이건 과거부터 이 프로젝트가 반복적으로 겪어온 "이 환경은 `document.hidden`이라 rAF/애니메이션/스타일 재계산이 멈춘다"는 것과 같은 종류의 환경 제약으로 판단된다(코드 자체의 문제가 아니라 — 배지는 `#sidebar`의 평범한 절대배치 자식일 뿐이라 상위 요소의 transform을 그대로 물려받는 건 CSS의 기본 동작이라 별도로 깨질 이유가 없다). 실기기에서 몰입모드 진입 시 배지도 상단 바와 함께 사라지는지는 사용자가 확인하면 좋음.
  - **이 항목은 아래 2026-08-25 후속으로 대체(보완)됨** — 사용자가 실기기에서 "너무 작아서 아예 안 보인다"고 재확인해줘서 위치·크기를 다시 잡았다. 아래 항목 참고.
- ✅ **완료·커밋됨 (2026-08-25, 후속 — 배지 위치/크기 재설계)**: 사용자가 "너무 작아서 아예 안 보이는데 좀 키워줄래?"라고 요청.
  - **문제**: 이전 배치(`#sidebar` 코너에 절대배치)는 책갈피 아이콘 버튼과 안 겹치려면 상단 바의 얇은 여백(아이콘이 시작되는 y=10px보다 위, 즉 세로 10px 미만)에 욱여넣어야 했다 — 그 제약 때문에 `font-size:9px`까지 줄여야 했고, 결국 실기기에서 거의 안 보였다.
  - **수정**: 배지를 코너에서 **제목(`#current-title`) 바로 옆(`.sidebar-title-row` 안)** 으로 옮겼다 — 그 자리는 아이콘 버튼과 무관한 별도 flex 영역이라 높이 제약이 없고, `font-size:12px; font-weight:600; opacity:0.55`로 훨씬 크고 진하게 키울 수 있었다(이전 9px/0.3 대비). 제목이 배지 때문에 밀려나거나 안 잘리는 일이 없도록 `.sidebar-title-row strong`에 `min-width:0; flex-shrink:1`을 명시해서, 제목이 길면 여전히 줄임표로 줄어들고 배지는 항상 온전히 보이게 했다.
  - **몰입모드 숨김 동작은 그대로 유지**: 여전히 `#sidebar`의 (더 깊이 중첩된) 자손일 뿐이라 별도 코드 없이 상단 바와 함께 슬라이드돼 숨는다.
  - `sw.js`의 `CACHE_VERSION`을 `v67`→`v68`로 올림.
  - **검증**: dev 로그인 + 데스크톱(1280×800) 뷰포트에서 실제 뷰어를 열어 (1) 배지 텍스트가 `v68`로 정확히 표시됨, 실측 크기가 19.5×16px(이전 9px 폰트 대비 뚜렷이 커짐)로 확인, (2) 책갈피 버튼과 전혀 안 겹침(`overlapsBookmark: false`), (3) 제목을 일부러 아주 긴 텍스트로 바꿔봐도 제목은 `text-overflow:ellipsis`로 정상 줄어들고 배지는 밀려나지 않고 그 옆에 완전히 보이는 상태 유지, (4) 콘솔 에러 없음. 이번엔 크기/겹침처럼 정적으로 실측 가능한 부분만 검증했고(이 환경의 렌더링 제약은 이전과 동일), 실기기에서 실제로 눈에 잘 띄는지는 사용자가 다시 확인 필요.
- ✅ **완료·커밋됨 (2026-08-25, 📌 목표각 180deg의 진짜 원인 발견 + 수정)**: 위에서 목표각을 91deg→180deg로 바꿨던 항목을 사용자가 실기기에서 재확인한 뒤 "180도 가지 않고 여전히 90도 돌다가 없어져"라고 재신고 — 원인을 다시 짚어보니 **1차 시도 자체가 애초에 헛수고였다**는 걸 뒤늦게 깨달았다.
  - **진짜 원인**: `leaf` 자신에 `backface-visibility:hidden`을 걸고 그 leaf 하나를 통째로 0→180도 돌리는 구조였는데, **backface-visibility:hidden의 정의 자체가 "이 요소가 90도를 넘게 돌아 뒷면이 보이는 각도가 되면 무조건 안 보이게 한다"는 것**이라, 키프레임의 목표각을 91이든 180이든 뭐로 잡든 leaf가 90도를 넘는 순간 안 보이는 건 100% 동일한 결과였다 — 애초에 목표각과 무관한 문제였던 것. 지난 라운드에서 "번호 갱신 타이밍이 안 나빠지게" 94% 키프레임을 정교하게 못박는 데만 신경 쓰다가, 정작 "180도까지 뭔가 보이게 하려면 leaf 자체에 뒷면이 있어야 한다"는 더 근본적인 요구사항을 놓쳤다.
  - **수정 — 앞/뒷면 분리("플립 카드" 기법)**: `js/portrait-flip.js`의 `buildPanelAnimation()`을 다시 설계 — 회전하는 래퍼 `leafCard`(`transform-style:preserve-3d`, 애니메이션은 이제 여기 하나에만 건다) 안에, **앞면(`front`, 넘어가는 페이지 내용, 로컬 rotateY 0)** 과 **뒷면(`back`, 종이 뒷면 느낌의 빈 패널, 로컬 rotateY 180deg 미리 걸어둠)** 두 장을 자식으로 겹쳐 넣었다. 카드가 90도를 넘는 순간 front의 유효 각도(카드+0)와 back의 유효 각도(카드+180)가 정확히 그 프레임에 뒤바뀌어서, 하나가 사라지는 것과 동시에 다른 하나가 나타나는 이음매 없는 전환이 된다 — 카드가 180도에 다다르면 back이 정면으로 딱 보이며 끝난다(실제 책장이 뒤집혀 뒷면이 평평하게 자리잡는 모습).
  - **덤으로 키프레임도 단순화**: duration 전체가 다시 "뭔가 보이는 구간"이 되므로(이전처럼 안 보이는 꼬리 구간이 없다), 지난 라운드의 94%/91deg 못박기 트릭이 필요 없어져서 대칭적인 `0%/50%/100%` 구조로 되돌렸다 — 0%(평평, front)→50%(rotateY 90도, scaleX를 0.06까지 눌러 "실처럼 얇아지는" 순간, 그림자 최대)→100%(rotateY 180도, 다시 평평, 이번엔 back). 이징도 `linear`→`ease-in-out`(카드 뒤집기 표준 곡선, 양 끝에서 느리고 중간에서 빠름)으로 바꿨다. `animationend`(→번호 갱신)는 여전히 duration(100%)에 발동하고, 이 시점엔 back이 이미 정면으로 다 보인 뒤라 "화면이 실제로 끝난 뒤에" 번호가 바뀌는 것과 자연스럽게 일치한다 — 별도 % 지점을 못박아 맞출 필요 자체가 없어졌다.
  - **뒷면 디자인**: `.portrait-flip-leaf-back`는 `.page`(배경색/패딩 등)를 그대로 물려받되(class="portrait-flip-leaf-back page") 실제 텍스트 내용은 없음(뒤집혀 보이면 이상하므로) — `box-shadow: inset 0 0 48px rgba(0,0,0,0.14)`로 살짝 그늘진 "빈 종이 뒷면" 느낌만 얹었다.
  - `sw.js`의 `CACHE_VERSION`을 `v68`→`v69`로 올림.
  - **검증(이번엔 특히 강한 근거)**: dev 로그인 + phone(393×700)/desktop(1280×800, 진짜 landscape) 뷰포트에서 — (1) `document.styleSheets`로 새 0%/50%/100% 대칭 키프레임이 의도한 그대로 파싱됨을 확인. (2) **Web Animations API로 애니메이션을 일시정지하고 `currentTime`을 20ms(=duration의 0.1%) 단위로 스크러빙하면서, 매 지점에서 `leafCard`의 실제 컴퓨티드 `transform` 행렬을 `DOMMatrix`로 파싱해 front/back 각각의 유효 법선(normal) z-성분 부호를 수학적으로 계산** — front는 49.9%까지 보이다(z: 1→0.001) 49.9~50.1% 사이에서 정확히 0을 지나 50.1%부터는 back이 보이기 시작(z: 0.001→1)하는 것을 **각도 실측으로 직접 증명**했다(이전 라운드들의 "논리적으로 맞다"보다 훨씬 강한 근거 — 브라우저가 실제로 계산한 행렬 값 자체를 읽은 것). (3) 트리거→오버레이 제거/카운터 갱신까지 실제 경과시간 실측 — 세로/가로 전부 ~1120~1150ms로 이전과 동일한 타이밍(번호 갱신 지연 재발 없음). (4) 세로 모드 3연타 스트레스 테스트 — 가드 정상, 정확히 1페이지만 진행, 잔여 오버레이 없음. (5) 세로 prev, 가로(스프레드) next/prev 각각 — `[FOOTERDEBUG]` 로그의 `spread-right`/`spread-left` 라벨과 페이지 번호 페어링까지 정상, 카운터 2페이지 단위 이동 확인. 콘솔 에러 없음.
  - ⚠️ **검증 중 겪은 환경 함정(코드 문제 아님)**: 가로 모드를 처음 테스트할 때 이 브라우저 탭이 실제로는 여전히 좁은 뷰포트(과거 세션에서 쓰던 리사이즈 값이 새 탭에 남아있었음)였는데도 `window.innerWidth`가 1280으로 보고돼서 진짜 desktop인 줄 알고 진행했다가, 카운터가 스프레드(2페이지)가 아니라 단일 페이지(1페이지)씩만 움직이는 걸 보고서야 실제로는 `isSinglePageMode`가 여전히 true로 빌드돼 있었다는 걸 알아챘다 — `resize_window` 직후 `navigate`(재빌드 유도)가 너무 빨리 연달아 일어나면 이 환경의 뷰포트 반영과 페이지 로드 사이에 경쟁이 생기는 것으로 보인다(CLAUDE.md에 이미 기록된 "이 환경에서 ResizeObserver가 안 미덥게 돈다"는 것과 같은 종류). **해결**: 리사이즈 후 `window.innerWidth`를 실제로 읽어 값이 반영된 걸 확인하고 나서야 `navigate`(재빌드)를 호출하니 정상적으로 진짜 landscape/스프레드 모드로 빌드됐다 — 다음에 이 환경에서 가로 모드를 검증할 사람은 이 순서(리사이즈 → 확인 → 그 다음 navigate)를 지킬 것.
  - **여전히 남은 한계**: 이번엔 실제 브라우저가 계산한 3D 변환 행렬을 직접 읽어 front/back 교대 시점을 수학적으로 증명했다는 점에서 지금까지 이 항목에 대한 검증 중 가장 강한 근거지만, 실제 화면에서 이 전환이 자연스럽게 "종이가 뒤집히는 느낌"으로 보이는지(회전 속도/이징/그림자 등 감성적 튜닝)는 여전히 실기기에서 사용자가 눈으로 봐야 한다.
- ✅ **완료·커밋됨 (2026-08-25, 3차 시도 — back에 진짜 내용 넣기)**: 위 항목을 실기기에서 확인한 사용자가 "넘어가는 페이지 기준으로 보이던 면(front)의 글자는 페이지와 함께 자연스럽게 넘어가는데, 안 보이던 면(back)의 글자는 페이지에 자연스럽게 붙어있지 않고 애니메이션이 끝나고 나서 뿅 하고 나타나는 느낌이라 부자연스럽다"고 지적.
  - **원인**: 직전 라운드의 `back`은 실제 내용이 없는 **빈 패널**(장식용 `box-shadow`만)이었다 — 그래서 카드가 50%(90도)를 넘어 back이 보이기 시작하는 순간부터 100%(180도, 애니메이션 종료)까지는 화면에 아무 글자 없는 빈 판만 보이다가, 애니메이션이 끝나 오버레이(leafCard+base 전체)가 걷히는 바로 그 순간에야 진짜 새 페이지 내용(base, 오버레이 밑에 계속 깔려 있던 것)이 갑자기 나타났다 — 이게 "뿅" 하고 튀어나오는 느낌의 정체였다. front는 처음부터 진짜 내용(leavingText)이었으니 자연스러웠던 것과 정확히 대비된다.
  - **수정**: `js/portrait-flip.js`의 `back`을 빈 `<div>`에서 `base`와 **완전히 동일한 진짜 내용**(`buildPageElement(revealingText, revealingFooter, width, height)`)으로 바꿨다 — "플립 카드" 기법에서 뒷면을 미리 `rotateY(180deg)` 돌려두는 이유가 정확히 이 상황을 위한 것이었다: 카드 자신도 180도까지 돌면 180+180=360(=0)이 되어 **거울처럼 뒤집히지 않고 정상 방향 그대로** 보인다. 그래서 back에 실제 텍스트를 얹어도 좌우反전 없이 안전하다 — 별도 미러링 보정이 필요 없다.
  - **효과**: 이제 50%~100% 구간 동안 실제로 "새 페이지가 회전하며 펼쳐지는" 모습이 계속 보이고, 100%(애니메이션 완료) 시점의 back 최종 상태(완전히 정면, 진짜 내용)가 바로 밑에 깔린 base와 내용·위치·크기가 전부 동일해서, 오버레이가 걷혀도 화면상 아무 변화가 없다(이음매 없음) — front가 처음부터 자연스러웠던 것과 대칭적으로 back도 끝까지 자연스럽다.
  - `styles.css`의 `.portrait-flip-leaf-back` 규칙(빈 패널용 장식 그림자)은 이제 안 쓰여서 제거했다.
  - `sw.js`의 `CACHE_VERSION`을 `v69`→`v70`으로 올림.
  - **검증**: dev 로그인 + phone(393×700) 뷰포트에서 실제 뷰어를 열어 (1) 트리거 직후 `back`의 footer 텍스트가 `base`와 정확히 동일(둘 다 "- 9 / 53 -")한 것 확인(이전엔 back에 텍스트 자체가 없었음), (2) Web Animations API로 다시 스크러빙해서 **back에 진짜 콘텐츠를 넣은 뒤에도** front/back 교대 시점(49.9%→50.1%)이 그대로 정확한 것 재확인, (3) **추가로 back이 거울처럼 뒤집혀 보이는지도 직접 검증** — 로컬 오른쪽 방향 벡터(1,0,0)를 back의 유효 변환 행렬로 변환해 x 부호를 확인한 결과, back이 보이기 시작하는 구간(75%~99.9%) 내내 x가 계속 양수(0.375→0.916→1.0, front의 시작 상태와 같은 부호)로 나와 **미러링 없이 정상 방향**임을 수학적으로 확인, (4) 트리거→완료 실측 타이밍 ~1113ms로 이전과 동일(번호 갱신 지연 재발 없음), (5) 콘솔 에러 없음.
  - **여전히 남은 한계**: 이번에도 실제 화면에서 "새 페이지가 회전하며 자연스럽게 펼쳐지는" 느낌으로 보이는지는 사용자가 실기기에서 최종 확인해야 한다 — 다만 이번엔 front/back 교대 시점(수학적 증명)과 미러링 여부(방향 벡터 부호 확인)까지 실측했으니, 남은 건 순수하게 "느낌"의 영역이다.
- ⚠️ **중요한 소통 오류(2026-08-25) — 기록해둠**: 위 back-face 수정을 보여준 뒤 사용자가 "이번엔 또 90도 돌고 사라지는데.. 넌 시각적으로 확인이 안 돼?"라고 물어서, **이 세션은 스크린샷/화면 렌더링 확인이 구조적으로 불가능하다는 걸(브라우저 자동화가 "화면을 컴포지팅하지 않는 상태"라 `computer{action:screenshot}` 자체가 실패함) 처음으로 명시적으로 밝혔다** — 지금까지 "검증"이라고 부른 것들은 전부 computed style/DOM 행렬 계산/타이밍 실측이었지, 실제 픽셀을 본 게 아니었다(과거 라운드의 진짜 시각적 확인은 전부 **사용자가 보내준 실기기 화면 녹화**를 ffmpeg로 분석한 것이었다). 확인해보니 **사용자가 캐시 버전이 안 맞는 채로 확인하고 있었던 것**(구버전 서비스워커 캐시)이 원인이었다 — 방금 만든 버전 배지로 이걸 바로 확인시켰고, 실제로 그게 원인이었다. **버전을 맞추고 나니 back-face 수정 자체는 정상 작동 확인됨.**
  - **교훈**: 이 환경에서 "검증됨"이라고 말할 때는 항상 그게 수학적/논리적 검증인지 실제 화면 확인인지 구분해서 전달할 것 — 이번처럼 사용자가 다른 원인(캐시)으로 오인해서 혼란이 생기지 않도록.
- 🔄 **작업 중 — 다음 세션(다른 컴퓨터)에서 이어할 것 (2026-08-25, 세션 일시중단)**: 캐시 버전을 맞춘 뒤 사용자가 재확인 — back-face 문제는 해결됐지만, **새로 발견된(또는 이제야 눈에 띈) 문제 2가지**가 남아있다:
  1. **"넘어가는 축이 화면 가운데가 아니고 좀 틀어져있다"** — 페이지가 회전하는 힌지(fold axis)가 곧은 수직선이 아니라 비스듬히 틀어져 보인다는 지적. 아직 코드 수정 안 함(원인 미확정) — **미확인 가설 두 가지**:
     - (a) `leafCard`의 `transform-origin`(`left center`/`right center`)이 카드 자신의 박스 기준인데, 그 박스 위치/크기(`offsetTop`/`offsetLeft`/`leavingWidth`/`leavingHeight`, `js/reader.js`의 `getActiveRealPageRect()`가 실측)가 실제 화면상 진짜 페이지 가장자리와 아주 살짝이라도 어긋나면 힌지 선 자체가 눈에 띄게 비뚤어 보일 수 있다 — 지금까지 "footer 위치 점프" 사가에서 겪은 것과 같은 종류의 미세한 위치 불일치 가능성.
     - (b) `perspectiveStage`의 `perspective-origin`이 기본값(자기 자신의 중앙, 50% 50%)인데, 힌지는 가장자리(edge)에 있고 원근 소실점은 페이지 중앙에 있는 이 조합 자체가 — 특히 세로로 긴 화면비에서 — 회전하는 가장자리가 원근 때문에 대각선으로 기울어 보이는 시각적 착시를 만들 수 있다. 실제 책장 넘김이라면 원래 이런 원근 왜곡이 자연스러울 수도 있지만, 사용자가 "틀어져 보인다"고 부정적으로 느꼈다면 `perspective-origin`을 힌지 쪽으로 옮기거나 `perspective` 값 자체(현재 1600px)를 조정해볼 필요가 있을 수 있다.
     - 다음에 이어볼 사람은 **실기기 화면 녹화를 다시 요청**해서(과거 라운드들이 성공적으로 그렇게 원인을 찾았던 방식) 힌지 선이 정확히 어느 방향/각도로 틀어져 보이는지 프레임 단위로 확인하는 게 가장 확실하다.
  2. **"90도에서 잠깐 멈췄다 넘어가는 느낌이 있다"** — **이건 원인을 상당히 확신 있게 짚어낼 수 있다(단, 아직 수정은 안 함 — 사용자가 오늘 세션을 여기서 멈추고 싶어해서 검증 없이 코드만 바꾸는 걸 피했다):** `js/portrait-flip.js`가 `leafCard.style.animation`에 **단일** `ease-in-out`을 지정하는데, CSS 스펙상 이 타이밍 함수는 키프레임 전체 구간(0%→100%)에 걸쳐 한 번 적용되는 게 아니라 **각 키프레임 구간(0%→50%, 50%→100%)마다 독립적으로 다시 적용**된다 — 즉 지금 코드는 사실상 "ease-in-out을 0→50%에 한 번, 50%→100%에 또 한 번" 건 것과 같다. `ease-in-out`은 "느리게 시작 → 중간에 빠르게 → 느리게 끝"인데, 이 모양이 각 구간에서 독립적으로 반복되면 **50% 지점(=90도)에 다다르며 첫 구간이 감속하고, 곧바로 이어지는 둘째 구간이 다시 저속으로 시작**해서 정확히 "90도 근처에서 잠깐 멈췄다 다시 움직이는" 느낌을 만든다 — 사용자가 묘사한 증상과 정확히 일치하는, CSS 애니메이션의 잘 알려진 함정이다.
     - **다음에 시도해볼 유력한 수정**(아직 적용 안 함): `styles.css`의 키프레임에서 전역 `ease-in-out` 대신, **0% 키프레임엔 `ease-in`, 50% 키프레임엔 `ease-out`을 각각 지정**하는 것 — 그러면 0→50% 구간은 "느리게 시작해서 50% 지점에 최고 속도로 도착"하고, 50→100% 구간은 "최고 속도로 시작해서 100%에 느리게 도착"하게 되어, 두 구간을 이으면 결과적으로 **하나의 매끄러운 ease-in-out 곡선을 전체 구간에 정확히 재현**하면서 50% 지점에서의 속도 급감(=느껴지는 "멈춤")이 사라진다. `js/portrait-flip.js`의 `leafCard.style.animation`에서는 그냥 `${duration}ms`만 지정하고 타이밍 함수는 키프레임 쪽에 맡기면 된다(과거 91deg 버전에서 이미 한 번 써봤던 "키프레임별 `animation-timing-function`" 패턴 — `git log`의 `94%/91deg` 라운드 참고).
  - **CACHE_VERSION**: 현재 `v70`(이 문서 기준 최신, `sw.js`에서 직접 확인할 것). **코드는 3차 시도(back에 진짜 내용) 상태 그대로 — 위 두 가지 다 아직 수정 안 함.**
  - **다음에 이어서 볼 사람에게(중요)**: (1) 먼저 사용자에게 실기기 화면 녹화를 다시 요청해서 축 틀어짐(1번) 문제의 정확한 방향/정도를 프레임 단위로 확인할 것 — 추측만으로 코드를 고치지 말 것. (2) 90도 멈춤(2번)은 위에서 설명한 `ease-in`/`ease-out` 분리 수정을 먼저 시도해볼 가치가 높다(원인이 CSS 스펙 동작으로 상당히 명확하게 설명됨) — 다만 이것도 실제로 고쳐지는지 실기기에서 확인 후 커밋할 것. (3) 이 세션은 스크린샷을 못 찍는 환경이었다(위 "소통 오류" 항목 참고) — 다음 세션(다른 컴퓨터)에서도 브라우저 자동화 환경이 같은 제약일 수 있으니, 코드를 고친 뒤 "검증됐다"고 말할 땐 항상 그게 수학적 검증인지 실제 화면 확인인지 명확히 구분해서 전달할 것.
- ✅ **완료·커밋됨 (2026-08-26, 세션 재개 — 위 두 항목 실제 조사·수정)**: 사용자가 실기기 화면 녹화(`Bookify 2026-08-26 06-58-27.mp4`, PC 웹 브라우저, 가로/2페이지 스프레드 모드)를 보내줘서 `ffmpeg`+Python(Pillow/numpy)로 프레임 단위 분석 — **두 문제의 성격이 서로 다르다는 게 이번에 명확해졌다.**
  - **"90도에서 멈췄다" — ✅ 실측으로 원인 확정, 수정 완료.** 30fps 프레임별 픽셀 diff를 재봤더니, 실제로 화면에 보이는 회전은 `duration`(1000ms)의 30~40%(약 300~400ms) 만에 다 끝나고, 나머지 60~70%는 연속 16프레임 이상(500ms+) diff=0인 완전 정지 상태로 남아있었다 — 정확히 지난 세션에서 진단만 해두고 못 고쳤던 그 문제(위 항목 참고: 단일 `ease-in-out`이 CSS 스펙상 키프레임 구간(0→50%, 50→100%)마다 독립 재적용되어 50%/90도 지점에서 감속→재가속이 겹침)를 실측으로 재확인한 것. **수정**: `styles.css`의 `portrait-flip-leaf-next/-prev` 키프레임에 `animation-timing-function`을 스톱별로 직접 지정(0%: `ease-in`, 50%: `ease-out`) — 두 구간을 이으면 50% 지점에서 속도가 끊기지 않는 하나의 연속된 ease-in-out 곡선이 된다(지난 세션이 이미 제안해뒀던 그 수정을 그대로 적용). `js/portrait-flip.js`의 `leafCard.style.animation`은 이제 전역 타이밍 함수 없이(무의미해져서) `linear` + duration만 지정.
  - **"축이 틀어져있다" — 재조사 결과, 문자 그대로 "대각선으로 굽은 선"은 아닌 것으로 결론.** `rotateY`/`scaleX`/`translateZ`는 전부 Y좌표(세로 위치)에 의존하지 않는 변환이라, 회전축(hinge) 자체가 대각선으로 휘는 건 좌표 수학상 불가능하다(직접 좌표 계산으로 재확인 — 임의의 점 (X0,Y0)에서 이 세 변환을 거치면 결과 (X',Z')은 X0에만 의존하고 Y0엔 전혀 의존하지 않으므로, 투영된 화면 X좌표도 Y0과 무관하게 항상 같다). 영상 프레임을 직접 뜯어봐도(텍스트 압축 구간의 시각적 착시는 있었지만) 일관된 "대각선 기울기"를 픽셀로 확정하지는 못했다. **대신 진짜 원인으로 지목**: `perspectiveStage`에 `perspective-origin`을 따로 안 정해줘서 기본값 50% 50%(=패널 자기 자신의 한가운데)를 쓰고 있었는데, 이건 회전축(스파인 쪽 가장자리)에서 패널 폭의 절반만큼 떨어진, 화면 전체 기준으로는 책의 진짜 가운데(스파인)와 거리가 먼 지점이다(예: 오른쪽 패널 자기중심 ≈ 화면 전체의 75% 지점) — 실제 책을 볼 때 눈은 펼쳐진 책 전체의 가운데를 향하지 낱장 페이지 하나의 중심을 향하지 않으므로, 소실점이 회전축과 멀리 떨어져 있으면 "화면 중앙이 아니라 패널 옆 어딘가를 중심으로 도는" 인상을 줄 수 있다 — 사용자가 "가운데 축만 맞추면 되는데"라고 표현한 것과 맞아떨어진다. **수정**: `perspective-origin`을 `transform-origin`과 정확히 같은 가장자리(방향에 따라 `0% 50%` 또는 `100% 50%`)로 맞춰서, 소실점이 항상 회전축(=책의 진짜 중앙)에 오도록 했다.
  - **검증**: 로컬 정적 서버 + dev 로그인으로 실제 뷰어를 열어 — (1) `document.styleSheets`로 새 키프레임의 스톱별 `animation-timing-function`(0%: ease-in, 50%: ease-out)이 의도대로 파싱됨을 확인, (2) `MutationObserver`로 오버레이가 DOM에 붙는 순간을 낚아채서 실측 — 가로(스프레드) 모드 next/prev 각각, 세로(모바일) 모드 next 전부 `perspectiveOrigin`이 `cardTransformOrigin`(transform-origin의 computed 값)과 **정확히 일치**하는 것 확인(예: next는 `"0px 360px"`↔`"0px 360px"`, prev는 `"112px 360px"`↔`"112px 360px"`), `cardAnimation`이 `linear` 베이스로 걸리는 것도 확인. (3) 트리거→오버레이 제거까지 실측 타이밍 ~1100ms로 기존과 동일(이 환경은 여전히 `document.hidden`이라 매번 `safetyTimer`(duration+80ms) 경로를 탐 — duration 자체는 안 건드렸으니 이 수치 자체는 회귀가 아님). (4) 4연타 스트레스 테스트 — 겹쳐 눌림 없이 정상 가드, 잔여 오버레이 0개. (5) 콘솔 에러 없음, 가로/세로 모드 둘 다 회귀 없음.
  - `sw.js`의 `CACHE_VERSION`을 `v70`→`v71`로 올림.
  - **여전히 남은 한계**: "90도 멈춤" 수정은 실측(프레임별 diff)으로 강하게 뒷받침되지만, "축 틀어짐" 수정은 "소실점 위치가 원인일 가능성이 가장 크다"는 추론에 기반한다 — 이 환경은 실기기 렌더링을 눈으로 볼 방법이 없어서(과거 세션들과 동일한 제약), perspective-origin 변경 후 실제로 "더 가운데처럼 보이는지"는 검증 못 했다. 사용자가 실기기에서 다시 확인 필요 — 그래도 여전히 이상하게 보인다면, "소실점 위치" 가설 자체가 틀렸다는 뜻이니 다시 화면 녹화를 받아 처음부터 재조사할 것.
- 🔄 **작업 중 (2026-08-26, 같은 세션 — 축 정렬 재신고 + 임시 진단 로깅 추가)**: 위 perspective-origin 수정을 사용자가 확인한 뒤 정확한 기준을 재확인해줬다 — "넘어가는 기준 축이 오른쪽 페이지일 경우 맨왼쪽, 왼쪽페이지일 경우 가장 오른쪽이고 페이지 기준으로 보면 정가운데야 근데 그게 지금 안맞아." 이건 이미 코드가 의도한 그대로다(오른쪽 패널의 `transform-origin`='left center', 왼쪽 패널은 'right center' — 둘 다 스파인 쪽 가장자리) — 즉 **"논리 자체는 맞는데 실제 화면에서 그 자리에 안 온다"**는 뜻이라, perspective-origin 수정만으로는 부족했고 뭔가 다른(아마도 실제 위치 측정값 자체의) 문제가 더 있다는 신호다.
  - **추측 대신 실측으로 전환**: 지금까지 이 축 문제만 두 라운드째 "논리적으로 맞다"는 추론(회전축 자체는 수학적으로 안 굽는다 / 소실점을 스파인으로 옮기면 나아질 것)에 기반한 수정을 냈는데 둘 다 사용자가 재확인 안 됐거나 부족했다 — footer 위치 점프 사가(6~11차)가 실기기 콘솔 로그로 정확한 39px 차이를 실측해서야 진짜 원인(라이브러리 CSS 오타)을 찾았던 것과 같은 패턴으로 전환했다.
  - **추가한 진단**: `js/portrait-flip.js`의 `buildPanelAnimation()`에 `[AXISDEBUG]` 로그를 추가 — 매 플립 트리거마다 (1) 우리 오버레이가 실제로 계산한 회전축의 절대 뷰포트 X좌표(`hingeAbsX`, `perspectiveStage`의 절대 위치 + offsetLeft/width로 계산), (2) `#book-spine`(스파인 그림자 데칼) 요소의 실측 중심 X좌표(`spineCenterX`, 세로/단일페이지 모드에선 스파인이 없으니 `null`), (3) 그 차이(`deltaFromSpine`)를 함께 찍는다. 세로 모드에서는 스파인 자체가 없으니 `spineCenterX`/`deltaFromSpine`이 항상 `null`로 나오는 것도 확인(정상 동작, 크래시 없음).
  - **가설**: `#book-spine`은(styles.css) `#my-book` 기준 `left:50%`에 순수 CSS로 고정된 장식 그림자일 뿐, 실제 `.page` 요소들의 위치를 측정해서 그리는 게 아니다 — 만약 실제 두 `.page` 요소의 경계가 (라이브러리의 내부 레이아웃 계산 어딘가의 오차로) `#my-book`의 진짜 50% 지점과 조금이라도 어긋나 있다면, 우리 오버레이 힌지(실제 `.page` rect를 정확히 재서 계산 — 이론상 정확함)는 그 어긋난 진짜 경계에 위치하는데, 사용자 눈에 "중앙"으로 보이는 스파인 데칼은 여전히 `#my-book`의 기하학적 50%에 고정되어 있어 — **둘이 서로 다른 기준이라 어긋나 보일 수 있다.** 다만 이건 아직 미검증 가설이고, `[AXISDEBUG]` 로그의 `deltaFromSpine` 값이 실기기에서 실제로 유의미한 크기(예: 수십 px)로 나오는지 봐야 확정된다.
  - `sw.js`의 `CACHE_VERSION`을 `v71`→`v72`로 올림.
  - **검증(이 환경 한정)**: 로컬 프리뷰에서 가로/세로 모드 둘 다 `[AXISDEBUG]` 로그가 에러 없이 정상 출력되는 것만 확인 — 이 환경은 실제 PageFlip 페이지 렌더링이 정상 동작하지 않아(문서 hidden으로 인한 rAF 정지, 기존 한계) `panelOffsetLeft`/`panelWidth` 등이 전부 폴백/이상값(예: `panelWidth:112`)이라 **이 환경에서 얻은 숫자 자체는 무의미하다** — 오직 로깅 코드가 안 깨진다는 것만 확인됐다.
  - **⚠️ 임시 디버그 로깅 — 원인 확인되면 제거할 것**: 대상은 `js/portrait-flip.js`의 `buildPanelAnimation()` 안 `[AXISDEBUG]` 콘솔 로그 블록 하나.
  - **다음에 이어서 볼 사람에게**: 사용자가 실기기(PC 웹)에서 페이지를 한 번 넘기고 콘솔에서 `[AXISDEBUG]` 로그 한 줄을 복사해서 주면(개발자도구 F12 → Console 탭), `deltaFromSpine` 값(스파인 그림자 중심과 우리 힌지 사이의 실제 픽셀 차이)으로 가설을 바로 확정하거나 기각할 수 있다 — 값이 크면(수십 px대) 위 가설이 맞고 스파인 데칼 위치 자체를 실제 페이지 경계에 맞춰 재계산하는 방향으로 고쳐야 하고, 값이 거의 0이면 이 가설도 틀린 것이니 완전히 다른 원인(예: 사용자가 보는 "정가운데"가 사실 스파인이 아니라 다른 시각적 기준점일 가능성 등)을 처음부터 다시 찾아야 한다.
- ✅ **완료·커밋됨 (2026-08-26, 같은 세션 — 📌 축 문제 진짜 원인 확정 + 수정, [AXISDEBUG] 제거)**: 사용자가 실기기 콘솔에서 `[AXISDEBUG]` 로그를 캡처해줬다 — 결과는 `deltaFromSpine: 0`(!), 즉 **우리가 계산한 회전축 좌표 자체는 스파인 정중앙과 완벽히 일치**했다. 위 가설(소실점/스파인 위치 어긋남)이 반증된 것 — 그런데도 사용자는 "안 맞아 보이는 게 정확히 언제냐"는 후속 질문에 **"회전 도중(접히는 순간)"**이라고 명확히 답해줘서, 문제가 "축의 정적 좌표"가 아니라 "축이 회전하는 동안 화면에 실제로 그려지는 위치"에 있다는 게 확정됐다.
  - **진짜 원인(좌표 수학으로 도출 + 실측으로 확정)**: 키프레임의 `transform: rotateY(θ) translateZ(28px) scaleX(k)`에서 **`translateZ`가 `rotateY`보다 안쪽(=먼저 적용)에 있다** — CSS는 리스트된 함수를 오른쪽(안쪽)부터 순서대로 점에 적용하므로, 실제 적용 순서는 scaleX → translateZ → rotateY다. 문제는 **`transform-origin` 지점(회전축 자기 자신)조차 이 순서의 영향을 받는다는 것** — 로컬 좌표 (0,0,0)(=축 자신)에 translateZ(28px)를 먼저 적용하면 (0,0,28)이 되고, 그 다음 rotateY(θ)가 이 z-오프셋을 x축으로 섞어버려서 축 자신이 `28×sin(θ)`만큼 화면에서 옆으로 밀려버린다 — 90도(50% 키프레임)에서 최대(±28px), 0도/180도에서는 0(사라짐). **정확히 "회전 도중에만, 그것도 그림자가 가장 짙어지는 90도 근처에서 가장 크게" 어긋나 보인다는 사용자 증상과 좌표 수학이 정확히 일치**했다. 직접 손으로 유도한 뒤 Python으로 수치 검산까지 마쳤다(구 순서: x=-28,z=0 / 새 순서: x=0,z=28).
  - **수정**: `styles.css`의 `portrait-flip-leaf-next`/`-prev` 키프레임에서 `translateZ`를 `rotateY`보다 앞(=바깥, 나중에 적용)으로 옮겼다 — `translateZ(28px) rotateY(θ) scaleX(k)`. 이러면 translateZ가 회전이 다 끝난 *뒤*(항상 화면을 향하는 진짜 z축)에 적용되므로 축 자신은 전혀 안 밀리고 순수하게 카메라 쪽으로만 들어올려진다 — 오히려 "페이지가 화면 쪽으로 들어올려짐"이라는 원래 연출 의도에도 이 순서가 더 물리적으로 맞다(회전된 로컬 z축이 아니라 실제 카메라를 향한 z축으로 들어올려야 자연스러움).
  - `js/portrait-flip.js`의 `[AXISDEBUG]` 임시 로깅 블록 제거(목적 — 축의 정적 좌표가 맞는지 확인 — 을 이미 달성했으므로) — 대신 이번 재조사 결과를 요약한 주석으로 교체.
  - **검증(이번엔 특히 강한 근거 — 논리 검증이 아니라 실제 브라우저가 계산한 변환 행렬 그 자체를 읽었다)**: 로컬 프리뷰에서 dev 로그인 + 가로(1280×800)/세로(375×812) 모드 각각 next/prev 트리거 → `card.getAnimations()[0]`을 실제로 pause하고 `currentTime`을 duration의 정확히 50%(=90도)로 맞춘 뒤, `getComputedStyle(card).transform`으로 나온 **실제 합성 행렬**(`matrix3d(...)`)을 `DOMMatrix`로 파싱해서 `m41`(로컬 X 드리프트)을 직접 읽음 — 가로 next: `m41=0, m43=28`, 가로 prev: `m41=0, m43=28`, 세로(모바일) next: `m41=0, m43=28` — **네 경우 다 X 드리프트가 정확히 0, Z만 28**로 확인(수정 전이었다면 next는 -28, prev는 +28이 나왔어야 함 — git 이력에서 구 코드로 재현 가능). 콘솔 에러 없음, 잔여 오버레이 없음.
  - `sw.js`의 `CACHE_VERSION`을 `v72`→`v73`으로 올림.
  - **여전히 남은 한계**: 이번엔 실제 브라우저가 계산한 3D 변환 행렬(추론이 아니라 진짜 렌더링 파이프라인이 만든 숫자)을 직접 읽어 "회전축이 회전 중에도 전혀 안 밀린다"는 걸 수학적으로 증명했다는 점에서 지금까지 이 축 문제에 대한 근거 중 가장 강력하지만, 이 환경은 여전히 실제 화면을 눈으로 보는 최종 확인이 불가능하다 — 사용자가 실기기에서 "회전 도중 접히는 순간에도 이제 축이 정가운데로 보이는지" 최종 확인 필요.
- ✅ **완료·커밋됨 (2026-08-26, 같은 세션 — 그림자를 회전체에서 분리)**: 위 축 수정을 보여준 뒤 사용자가 "페이지가 넘어갈 때 중심축 기준으로 그림자가 생기는 부분이 어색해 보이는데 너 생각은 어때?"라고 의견을 물었다 — 코드 분석 후 원인과 방향을 먼저 제시하고("어떻게 생각하냐"는 질문이라 바로 고치지 않고 의견부터 답함), 사용자가 "진행시켜"라고 승인한 뒤 구현했다.
  - **원인**: 그림자(`box-shadow`)가 회전하는 `leafCard` 자신에게 붙어있어서, 카드와 똑같이 `rotateY`+`scaleX`의 영향을 그대로 받고 있었다 — 실제 종이 그림자는 "밑에 깔린 고정된 표면 위에 드리워지는 것"이라 카드가 회전하든 말든 그 표면에서 자연스럽게 자라났다 옅어져야 하는데, 지금 구현은 그림자가 카드에 얹혀서 같이 돌기 때문에 90도 근처(`scaleX(0.06)`로 카드 폭이 짜부라지는 지점, 정작 그림자가 가장 짙어야 할 그 순간)에 그림자도 같이 눌려서 부자연스러웠다 — 표면에 드리워진 그림자가 아니라 회전체에 그려진 무늬처럼 보이는 것.
  - **수정**: `js/portrait-flip.js`의 `buildPanelAnimation()`에 새 레이어 `shadowLayer`를 추가 — `base`(z-index 1)와 `leafCard`(z-index 2→3으로 올림) 사이(z-index 2)에 위치한, **회전 변환을 전혀 안 받는 평평한 레이어**다. 스파인 쪽 가장자리가 짙고 바깥으로 옅어지는 `linear-gradient`(방향은 next/prev에 따라 인라인으로 반대로)를 배경으로 깔고, `opacity`만 `styles.css`의 새 키프레임 `portrait-flip-shadow-fade`(0%: 0, 50%: 1, 100%: 0 — leaf 키프레임과 같은 방식으로 0%엔 `ease-in`, 50%엔 `ease-out`을 걸어 50% 지점에서 속도가 안 끊기게 맞춤)로 움직인다. `leafCard`의 `box-shadow`는 완전히 제거했다.
  - **검증**: 로컬 프리뷰에서 dev 로그인 + 가로(1280px)/세로(375px) 모드 각각 — (1) `shadowLayer`가 의도한 그라디언트/z-index로 생성되고 `leafCard.boxShadow`가 `'none'`인 것 확인, (2) `shadowLayer.getAnimations()[0]`을 pause하고 `currentTime`을 0/250/500/750/999ms로 스크러빙해서 실제 `opacity` 값을 직접 읽음 — `0 → 0.315 → 1 → 0.315 → ~0`으로 정확히 0→1→0 대칭 페이드, 정점(1.0)이 leaf 카드의 90도 지점(50%)과 정확히 일치하는 것 확인(카드가 아무리 눌려도 그림자 자체는 별도 레이어라 안 눌림). (3) 콘솔 에러 없음, 잔여 오버레이 없음, 세로 모드도 동일 구조로 정상 동작.
  - `sw.js`의 `CACHE_VERSION`을 `v73`→`v74`로 올림.
  - **여전히 남은 한계**: opacity 페이드 자체는 실제 Web Animations API로 정확히 검증했지만, "그림자가 이제 자연스러워 보이는지"(그라디언트 폭 140px, 최대 불투명도 0.45 등 수치가 적절한지)는 감성적 판단이라 이 환경에서 확인 불가 — 사용자가 실기기에서 확인 필요.
- ✅ **완료·커밋됨 (2026-08-26, 같은 세션 — 📌 스파인 "뿅" 등장 버그, z-index 함정 두 겹 확인 + 수정)**: 사용자가 "페이지 넘어가서 애니메이션이 끝나는 순간 가운데 축 그림자가 뿅하고 나타나서 어색하다"고 신고 — `#book-spine`(스프레드 중앙 책등 그림자)이 z-index:5로 고정돼 있는데, 애니메이션 오버레이(`perspectiveStage`, z-index:20)가 화면을 그보다 위에서 덮어서 애니메이션 도중엔 안 보이다가, 끝나서 오버레이가 사라지는 순간 원래 계속 거기 있던 스파인이 갑자기 나타나는 것처럼 보이는 문제였다 — 12차 항목(2026-08-25)에서 이미 "의도적으로 미해결로 남긴" 걸로 기록해뒀던 그 코스메틱 디테일.
  - **1차 시도(z-index만 21로 올림) — 부분적으로만 맞았다**: `#book-spine`의 `z-index`를 오버레이보다 높은 21로 올렸는데, 합성 테스트(실제 스파인 좌표에 z-index:20 더미 요소를 겹쳐두고 `elementFromPoint`로 승자 확인)로 검증해보니 **더미가 여전히 이겼다** — 진짜 원인을 더 파보니, 스파인이 `#my-book`(=`bookElement`) *안에* 들어있는데, StPageFlip 라이브러리가 `#my-book`에 항등행렬이라도 `transform`을 인라인으로 걸어둔다는 걸 발견했다. CSS 스펙상 `transform: none`이 아니면(설령 시각적으로 아무 효과 없는 `matrix(1,0,0,1,0,0)`이라도) 그 요소가 **새 스태킹 컨텍스트를 만든다** — 즉 `#my-book` 전체가 하나의 봉인된 상자가 되어, 그 안의 스파인이 z-index를 아무리 올려도 `#my-book` 자신의 스태킹 레벨(z-index:auto, "positioned/auto" 계층) 밖으로 못 나간다. `#my-book`의 형제인 애니메이션 오버레이(명시적 양수 z-index:20, 항상 "positioned/auto" 계층 위에 그려짐)를 절대 못 이길 수밖에 없는 구조였다.
  - **2차 시도(진짜 수정) — 스파인을 `#my-book` 밖으로 이동**: `js/reader.js`의 `buildFlipBook()`에서 `spineElement`를 `bookElement.appendChild(...)` 대신 `stageContainer.appendChild(...)`로 바꿔서, 오버레이와 **같은 부모(`#book-stage`)의 직계 형제**가 되게 했다 — 이러면 스태킹 컨텍스트에 안 갇히고 z-index 21 vs 20을 직접 겨룬다. 가로 위치(`left:50%`)는 CSS 그대로 둬도 된다(`#my-book`이 `#book-stage` 안에서 flex로 가운데 정렬되므로 둘의 가로 중심은 대칭 오프셋이 상쇄돼 항상 일치) — 다만 세로는 `#my-book`이 `#book-stage`보다 짧게 레터박스될 수 있어서, `#my-book`의 실측 `top`/`height`를 인라인으로 못박았다(CSS 기본값이던 `top:0;bottom:0`을 오버라이드).
  - **검증 중 잡은 테스트 방법론 자체의 함정**: 처음 합성 테스트에서 "z-index 21인데도 더미가 이긴다"는 결과가 한 번 더 나와서 당황했는데, 원인은 `#book-spine`의 `pointer-events:none` — 이 속성이 있으면 `elementFromPoint()`가 그 요소를 **아예 히트테스트 대상에서 제외**하고 그 뒤(=페인트 순서상 아래)의 것을 반환한다. 즉 z-index/페인트 순서가 맞아도, `pointer-events:none`인 요소는 `elementFromPoint`로 "이겼는지" 확인할 수 없다 — 테스트 중에만 `pointer-events`를 `auto`로 임시로 바꿔서(진짜 앱 동작에는 영향 없음, 테스트 후 원복) 재확인하니 스파인이 정확히 승리했다.
  - **검증**: `127.0.0.1`(이 환경의 `localhost` 캐싱-프록시 문제를 우회하는 이미 알려진 방법 — 새 탭을 열어도 `localhost`는 여전히 구버전을 서빙했지만 `127.0.0.1`은 최신 코드를 정확히 서빙했다)로 새 탭에서 dev 로그인 — (1) `spine.parentElement.id === 'book-stage'`(더 이상 `my-book`이 아님) 확인, (2) `spine.style.top`/`height`가 `#my-book`의 실측값으로 정확히 채워짐 확인, (3) 합성 더미(z-index 20) 테스트를 스파인의 `pointer-events`를 임시로 `auto`로 바꾼 채 재실행 — 정적 상태와 **실제 살아있는 플립 애니메이션 오버레이가 떠 있는 도중** 둘 다에서 스파인이 승리하는 것 확인, (4) 세로(모바일 375px) 모드 — `spine.parentElement.id === 'book-stage'`(정상), `display:none`(정상, 크래시 없음), 플립 트리거도 정상 동작. 콘솔 에러 없음, 잔여 오버레이 없음.
  - `sw.js`의 `CACHE_VERSION`을 `v74`→`v76`으로 올림(1차 시도에서 v75, 2차 시도에서 v76).
  - **여전히 남은 한계**: 이번엔 실제 살아있는 애니메이션 오버레이를 상대로 한 히트테스트(단순 논리 검증이 아니라 진짜 페인트 순서를 반영하는 테스트)까지 마쳐서 이전 라운드들보다 근거가 강하지만, 이 환경은 여전히 실제 화면을 눈으로 보는 최종 확인이 불가능하다 — 사용자가 실기기에서 "애니메이션이 끝나는 순간 스파인이 더 이상 갑자기 나타나지 않는지" 최종 확인 필요.
- 🔄 **작업 중 (2026-08-26, 새 문제 — "next로 넘길 때 왼쪽 페이지가 오른쪽 내용으로 다시 바뀐다")**: 위 스파인 수정을 확인한 사용자가 애니메이션 문제 자체는 해결됐다고 확인해줬지만, 새로 발생한(또는 이제야 눈에 띈) 문제를 신고: "오른쪽을 클릭해 다음 페이지로 넘기면, 클릭 즉시 왼쪽 페이지가 다음 내용으로 깜박이며 바뀌고, 애니메이션이 진행되는 동안 넘어오는(오른쪽) 페이지에 왼쪽 페이지의 바뀌기 전 내용이 보이다가, 애니메이션이 끝나면 다시 깜박이며 정상적인 다음 페이지로 바뀐다."
  - **사용자가 보내준 화면 녹화(`Bookify 2026-08-26 10-08-24.mp4`)를 ffmpeg+Python으로 프레임 단위 분석해서 실제 버그를 확정했다**: "next" 클릭 시 LEFT는 클릭 즉시 정확히 373(예시 페이지 번호)으로 바뀌는 게 맞다(설계대로, 애니메이션 없이 즉시). 그런데 애니메이션이 시각적으로 끝나갈 무렵(클릭 후 약 800ms, RIGHT의 회전 애니메이션이 settle되는 시점과 거의 정확히 일치) **LEFT가 다시 한번, 이번엔 잘못된 값(374 — RIGHT의 번호와 동일!)으로 바뀌는 것을 확인했다.** 이후 완전히 정지된 화면(f183)에서는 좌우 페이지 본문 텍스트까지 글자 하나 안 틀리고 완전히 동일 — 명백한 콘텐츠 중복 버그(단순 애니메이션 잔상/렌더링 착시가 아니다).
  - **코드 추적으로 좁힌 유력 후보**: `js/reader.js`의 `maybeShiftPageWindow()` — 페이지 창(윈도우) 가장자리 근처에서 `pageFlip.updateFromHtml(newElements)`로 창을 재구성하는데, 이 벤더 라이브러리의 `updateFromHtml()` 내부가 `const e=this.pages.getCurrentPageIndex(); this.pages.destroy(); this.pages=new PageCollection(...); ...; this.pages.show(e);`처럼 **옛(재구성 전) 창 기준의 로컬 인덱스 `e`를, 방금 새로 만든(재구성 후) PageCollection에 그대로 재사용**한다는 걸 소스에서 확인했다 — `e`가 새 창에서는 완전히 다른 전역 페이지를 가리키게 된다. 다만 그 직후 우리 코드가 `pageFlip.turnToPage(globalIndex - windowStartIndex)`로 바로 정정하는 호출을 하므로, 순수 논리상으론 같은 동기 실행 안에서 바로 덮어써져야 하는데, 실제로 관찰된 딜레이(약 800ms, RIGHT 애니메이션이 끝나는 시점과 일치)와 시간적으로 맞아떨어지는지는 확정하지 못했다 — `pageFlip.on('flip', ...)` 핸들러(같은 파일, `isShiftingWindow` 가드로 우리가 직접 트리거한 flip은 무시하게 되어 있음)나 라이브러리의 다른 내부 상태와 상호작용해서 딜레이가 생기는 것일 수 있다.
  - **추측 대신 실측으로 전환**: 확신 없이 코드를 고치는 대신(이 사가 내내 반복됐던 실수), 실제로 무엇이 언제 일어나는지 실기기 콘솔로 직접 잡기로 했다. 추가한 진단(전부 `[WINDOWDEBUG]` 태그):
    - `maybeShiftPageWindow()` 진입 시("triggered", 창 재구성이 실제로 발동하는지 여부 자체부터 확인) + 완료 시("done", 새 windowStart/End·최종 로컬 인덱스·`pageFlip.getCurrentPageIndex()` 실측값).
    - `pageFlip.on('flip', ...)` 핸들러 맨 앞 — 이 핸들러가 실제로 몇 번, 언제, `isShiftingWindow`가 무슨 값일 때 발동하는지 전부(무시되는 경우 포함) 기록.
    - 새 함수 `pollSpreadFooterDiagnostics(label)` — `goToNextPage`/`jumpToPrevPage`의 스프레드 분기에서 `swapRealPageForFlip()` 직후부터 1.5초간 50ms 간격으로 **좌/우 real `.page`의 footer 텍스트를 각각 따로** 반복 기록한다(기존 `logFooterDiagnostics`는 "보이는 페이지 하나"만 잡아서 좌/우를 구분 못 했다) — 왼쪽 footer가 정확히 몇 ms 지점에 "다시" 바뀌는지 놓치지 않기 위함.
  - `sw.js`의 `CACHE_VERSION`을 `v76`→`v77`로 올림.
  - **검증(이 환경 한정)**: 로컬 프리뷰(`127.0.0.1`)에서 dev 로그인 후 next 트리거 — `[WINDOWDEBUG] flip event fired`(`isShiftingWindow:true`로 정상 무시됨)와 `[WINDOWDEBUG] spread footer poll goToNextPage` 시리즈가 에러 없이 정상 출력됨을 확인. 다만 이 환경은 여전히 PageFlip의 실제 페이지 전환이 안 일어나는 것으로 보여(footer 값이 1.5초 내내 "1/41"·"2/41"로 고정) **이 환경에서는 실제 버그(창 재구성 타이밍) 자체를 재현/검증할 수 없다** — 로깅 코드가 안 깨진다는 것만 확인됐다.
  - **⚠️ 임시 디버그 로깅 — 원인 확인되면 제거할 것**: `js/reader.js`의 `maybeShiftPageWindow()` 안 진입/완료 로그 2줄, `pageFlip.on('flip', ...)` 핸들러 맨 앞 로그 1줄, 새 함수 `pollSpreadFooterDiagnostics()` 전체, 그리고 `goToNextPage`/`jumpToPrevPage`의 호출 2줄.
  - **다음에 이어서 볼 사람에게**: 사용자가 실기기(PC 웹)에서 다시 "next"로 페이지를 넘겨서 왼쪽이 다시 바뀌는 증상을 재현하고, 그 순간의 콘솔 로그(`[WINDOWDEBUG]`로 시작하는 줄 전부)를 캡처해서 주면 — (1) `maybeShiftPageWindow`가 실제로 트리거됐는지(즉 창 가장자리 근처였는지), (2) 만약 트리거됐다면 `done` 로그의 `finalLocalIndex`/`finalCurrentPageIndex`가 기대값과 일치하는지, (3) `flip event fired` 로그가 예상보다 많이/이상한 시점에 찍히는지, (4) `spread footer poll`에서 정확히 몇 ms 지점에 leftFooter가 "다시" 바뀌는지를 보고 정확한 트리거를 특정할 수 있다. `maybeShiftPageWindow`가 이번 케이스에서 아예 안 트리거됐다면(창 가장자리와 무관), 이 가설 전체가 틀린 것이니 처음부터 다른 원인(예: StPageFlip 자체의 내부 렌더 상태 vs 우리가 만드는 DOM 사이의 또 다른 경쟁 상태)을 찾아야 한다.
- 🔄 **작업 중 (2026-08-26, 같은 세션 — 사용자 로그 1차 분석, 진단 버그 발견 + 수정)**: 사용자가 실기기 콘솔 로그를 캡처해서 보내줬다. 분석 결과:
  - **창 재구성(`maybeShiftPageWindow`) 가설은 반증됨**: 로그에 `[WINDOWDEBUG] maybeShiftPageWindow triggered/done`가 단 한 줄도 없었다 — 즉 이번 재현에서는 그 경로가 아예 안 탔다. `flip event fired`도 딱 한 번(우리가 직접 건 `swapRealPageForFlip`의 turnToPage, `isShiftingWindow:true`로 정상 무시)만 찍혔다. 이전 세션의 "창 재구성 중 라이브러리가 옛 로컬 인덱스를 재사용한다" 가설은 적어도 이번 케이스의 원인은 아니다.
  - **내 진단 코드 자체의 버그 발견**: `pollSpreadFooterDiagnostics()`가 `display:none` 필터를 빼먹어서, 화면에 실제로 안 보이는 **낡은** `--left`/`--right` 요소를 잘못 찾아 애니메이션 내내 "365/366"이라는 (당시 실제 페이지인 385 근방과 전혀 다른) 값을 고정으로 찍었다 — 이 데이터는 못 쓴다. `logFooterDiagnostics`/`portrait-flip.js`의 FOOTERDEBUG 쪽은 이 문제가 없어서(둘 다 `display!=='none'` 필터가 이미 있었음), 그쪽 로그(leaving=384/revealing=386, 최종 footerText=385)는 신뢰할 수 있는데 — **이번 캡처 자체에서는 눈에 띄는 이상이 안 보였다**(정상적인 384→386 전환처럼 읽힘). 사용자가 이 특정 캡처 순간에 실제로 글리치를 "봤는지"는 아직 확인 못 함 — 다음 요청 때 반드시 확인 필요.
  - **수정**: `pollSpreadFooterDiagnostics()`에 `display!=='none'` 필터를 추가하고, 추가로 **DOM에 `--left`/`--right` 클래스가 붙은 요소의 총 개수**(`allLeftCount`/`allRightCount`, 보이는 것+숨겨진 것 전부)도 같이 찍도록 확장 — "낡은 요소가 안 지워지고 계속 쌓이는지"를 직접 확인하기 위함.
  - **이 환경에서 재확인**: 수정한 진단을 재배포하고 트리거해보니 `{"leftFooter":null,"rightFooter":null,"totalPageEls":22,"visiblePageEls":0,"allLeftCount":3,"allRightCount":3}` — 이 환경은 여전히 아무것도 안 보이지만(기존 한계, `visiblePageEls:0`), **`--left`/`--right` 클래스가 붙은 요소가 각각 3개씩 동시에 DOM에 존재한다는 구조적 사실 자체는 확인됐다** — 매 `updateFromHtml`/재빌드마다 옛 요소가 완전히 안 지워지고 남는 것으로 보인다. 콘솔 에러 없음.
  - `sw.js`의 `CACHE_VERSION`을 `v77`→`v78`로 올림.
  - **다음에 이어서 볼 사람에게**: 사용자에게 (1) 이 진단 로그를 다시 요청하되, **이번엔 화면에서 실제로 글리치(왼쪽이 다시 바뀌는 것)를 직접 본 바로 그 순간의 로그인지 확인**해달라고 명시할 것 — 지난 캡처는 어쩌면 운 좋게 정상이었던 케이스였을 수 있다. (2) `allLeftCount`/`allRightCount`가 실기기에서도 1보다 크게 나오는지(낡은 요소가 실제로 쌓이는지) 확인 — 크다면 다음 조사 방향은 "그 낡은 요소들이 어떻게 다시 보이게 되는지"가 된다.
- ✅ **완료·커밋됨 (2026-08-26, 같은 세션 — 📌 진짜 원인 확정: `syncProgressFromServer`와 수동 애니메이션의 경쟁 상태)**: 사용자가 "직접 로그 확인이 안 되냐, 승인하면 가능하냐"고 물어서 **Claude in Chrome으로 사용자의 실제 프로덕션 계정(`https://txt-viewer-murex.vercel.app/`)에 직접 접속해서 재현·콘솔 로그를 직접 읽었다** — 지금까지 두 번의 사용자 로그 캡처가 우연히 깨끗했던 것과 달리, 직접 조작하며 지켜보다가 **결정적인 로그를 잡았다**: 내가 수동으로 클릭한 "다음"(예: 17→19페이지, 화면에 정상적으로 보임)과 완전히 동시에, `[FOOTERDEBUG] overlay base+leaf footer @ start spread-right`가 **전혀 다른, 훨씬 먼 페이지(436/438, 전체 1529)** 를 대상으로 찍힌 걸 발견했다.
  - **원인**: `js/reader.js`의 `syncProgressFromServer()` — "다른 기기에서 더 최근에 읽은 기록이 있으면 자동으로 그 위치로 이동"하는 기능으로, **60초 주기 `setInterval`**로 사용자 조작과 전혀 무관하게 발동한다. 오늘 하루 이 책으로 여러 세션(로컬 dev 테스트, 이번 실제 계정 테스트 등)을 반복하면서 Firestore의 `reading_progress` 문서에 "더 최신" 타임스탬프가 계속 쌓였고, 이 함수가 그걸 발견할 때마다 `jumpToGlobalPage(targetPage)`를 호출한다. 그런데 `jumpToGlobalPage()`는 `pageFlip.updateFromHtml()`로 **창 전체를 애니메이션 없이 즉시 재구성**하는 함수라서, 사용자가 방금 클릭한 "다음" 수동 애니메이션(`js/portrait-flip.js`의 rotateY 오버레이)이 **아직 화면에 떠 있는 도중** 이 함수가 끼어들면 — 오버레이가 덮고 있는 오른쪽은 오버레이 자신이 클릭 시점에 미리 캡처해둔 텍스트로 계속 애니메이션하다 끝나는 반면, **오버레이가 안 덮고 있는 왼쪽(진짜 DOM)만 느닷없이 동기화로 넘어온 완전히 다른 페이지로 바뀐다** — 정확히 신고된 "다음 페이지 클릭 후 한참 뒤 왼쪽이 다시 바뀐다" 증상이다. (처음 보내준 영상에서 좌우가 우연히 같은 번호로 "374/374"가 됐던 건, 그 순간 동기화 대상 페이지가 마침 내가 로컬에서 클릭해 넘어가려던 페이지와 가까웠기 때문으로 보인다.)
  - **수정**: (1) `syncProgressFromServer()` 맨 앞과, Firestore `getDoc` 완료 직후(await 사이에 사용자가 클릭을 시작했을 수 있으므로 한 번 더) 둘 다에 `isPortraitFlipAnimating()` 체크를 추가 — 애니메이션 진행 중이면 **`lastKnownProgressUpdatedAt`을 갱신하지 않고** 그냥 건너뛴다(동기화를 "완료"로 잘못 기록하면 다음 60초 주기가 재시도를 안 하므로, 반드시 "아직 못 따라잡음" 상태로 남겨서 다음 체크가 같은 서버 값을 다시 보고 정상 재시도하게 함). (2) `jumpToGlobalPage()` 자체에도 방어적으로 같은 가드를 추가(다른 호출부가 실수로 애니메이션과 겹치는 경우까지 대비).
  - **왜 지난 두 번의 사용자 로그 캡처는 깨끗했는지**: 이 버그는 애초에 "다음 클릭"의 로직 문제가 아니라, "60초 주기 타이머가 우연히 그 클릭과 겹치는 타이밍에 도는가"에 달린 **순수한 타이밍 경쟁**이라, 재현이 간헐적일 수밖에 없었다 — 지난 두 캡처는 단지 운 좋게 타이머가 안 겹친 순간이었을 뿐이다.
  - `sw.js`의 `CACHE_VERSION`을 `v78`→`v79`로 올림.
  - **검증**: 로컬 프리뷰(`127.0.0.1`)에서 캐시 초기화 후 재로드 — 콘솔 에러 없이 정상 로드되는 것까지 확인(문법 오류 없음). 실제 수정 효과(충돌 재현 안 되는지)는 프로덕션 배포 후 같은 방식(Claude in Chrome으로 실제 계정 접속)으로 재확인 예정.
- ✅ **완료·커밋됨 (2026-08-26, 같은 세션 — 📌 프로덕션에서 직접 검증 완료 + 임시 디버그 로깅 전부 제거)**: 위 `syncProgressFromServer`/`jumpToGlobalPage` 경쟁 상태 수정을 배포한 뒤, 사용자가 "확인해줘"라고 요청 — 사용자가 이미 승인한(1: 조작 허용, 2: 프로덕션 URL) Claude in Chrome으로 `https://txt-viewer-murex.vercel.app/`에 실제 로그인 세션으로 접속해 **직접 여러 번 "다음" 페이지를 클릭하며 화면과 콘솔을 직접 지켜봤다** — 왼쪽/오른쪽 패널이 항상 짝을 맞춰(예: 1045/1046 → 1047/1048 → 1049/1050) 깨끗하게 순차 이동하고, 클릭 후 한참 뒤에 왼쪽만 다시 바뀌는 증상이 재현되지 않는 것을 스크린샷+DOM 조회로 확인 — **버그가 실제로 해결됐음을 프로덕션에서 직접 확인.**
  - 검증 도중 강한 연타 스트레스 테스트(약 70초간 5회 연속 클릭, 60초 동기화 타이머와 고의로 충돌시키려는 시도) 중 브라우저 탭이 일시적으로 응답 없음 상태(`Page.captureScreenshot` 30초 타임아웃)가 됐다가 자동 새로고침된 것으로 보이는 현상을 겪었다 — 이후 진행상황이 일시적으로 데스크된(오래된 "15/31" 내용에 "1036/3946" 카운터가 붙는) 상태를 봤지만, 완전히 새로고침하니 실제 사용자의 진짜 진행상황(1045/1046, 총 3946페이지)이 온전히 복원된 것을 확인 — **사용자의 실제 데이터 손상은 없었다**(테스트 클릭이 낮은 페이지 번호로 진행상황을 덮어쓰지 않았음). 이 탭 멈춤 현상 자체의 근본 원인(연타 자체의 부하? `--left`/`--right` 낡은 DOM 요소가 쌓이는 문제, 위 항목에서 `allLeftCount`/`allRightCount`로 이미 관찰됐던 것과 관련 있을 가능성)은 이번에 확정하지 못했다 — 별도로 조사할 가치가 있으나 이번 세션에서는 더 파고들지 않았다.
  - **임시 디버그 로깅 전부 제거**: 이번 라운드(및 직전 2026-08-25 라운드)에서 조사용으로 추가했던 `[WINDOWDEBUG]`/`[FOOTERDEBUG]` 로깅을 전부 걷어냈다 — `js/reader.js`의 `logFooterDiagnostics()`/`pollFooterDiagnostics()`/`pollSpreadFooterDiagnostics()` 헬퍼 함수 3개를 통째로 삭제하고, `maybeShiftPageWindow()`/`finishManualPageTurn()`/`jumpToPrevPage()`/`goToNextPage()`/`pageFlip.on('flip', ...)` 핸들러/`scheduleFlipbookRebuild()`에 남아있던 호출부·콘솔로그 블록을 전부 제거했다. 그 과정에서 상단에 남아있던 "임시 디버그 로깅, 원인 확인되면 제거할 것"이라는 안내 주석(`getActiveRealPageRect()` 위)도 이제 로깅이 실제로 사라졌으니 과거형("~를 잡기 위해 추가했다", "실기기 로그로 확인된 진짜 원인")으로 다시 정리했다 — 나머지 기술적 설명(원인 규명 경위)은 여전히 유효한 기록이라 그대로 남겨뒀다.
  - `sw.js`의 `CACHE_VERSION`을 `v79`→`v80`으로 올림.
  - **검증**: 로컬 프리뷰(`127.0.0.1`)에서 정리 후 새 탭으로 재확인 — 페이지가 콘솔 에러 없이 정상 로드됨(모듈 그래프 전체가 문제없이 파싱·실행됐다는 뜻, `reader.js`에 문법 오류가 있었다면 이 시점에 전체가 깨졌을 것), dev 로그인 후 뷰어 화면이 정상 표시되고 `[FOOTERDEBUG]`/`[WINDOWDEBUG]` 로그가 완전히 사라진 것, `.portrait-flip-stage` 잔여 오버레이 없음을 확인.
  - **최종 상태**: "next 클릭 후 왼쪽 패널이 뒤늦게 오른쪽 내용으로 다시 바뀐다"는 버그는 근본 원인(진행상황 동기화 타이머와 수동 플립 애니메이션의 경쟁 상태) 확정 + 수정 + **프로덕션에서 직접 재현 안 됨을 확인**까지 마쳤다. 관련 임시 로깅도 전부 제거된 깨끗한 상태로 커밋됨.
  - **이 항목은 틀렸던 것으로 판명됨 — 사용자가 "문제가 그대로"라고 재확인, 게다가 "매번 재현된다"고 확인해줘서(60초 동기화 타이머로는 설명 안 되는 빈도) 위 진단 자체가 다른 문제를 고친 것이었다는 게 드러났다. 아래 항목이 진짜 원인 규명 + 수정.**
- ✅ **완료·커밋됨 (2026-08-26, 같은 세션 — 📌 진짜 원인: `--left`/`--right` 클래스 기반 "지금 보이는 페이지 찾기"의 구조적 결함, 수정: 위치 기반 직접 조회로 교체)**: 사용자가 "매번 재현된다"고 확인해준 뒤, `js/vendor/page-flip.browser.js` 소스를 다시 파고들어 이번엔 라이브러리 내부 렌더링 파이프라인 자체를 끝까지 추적해서 진짜 구조적 결함을 찾았다.
  - **확인된 사실 두 가지(소스 코드로 직접 검증)**:
    1. `HTMLPage.setOrientation()`(라이브러리 내부)은 **새로 배정되는** 페이지에만 `--left`/`--right` 클래스를 붙인다 — **교체되어 밀려나는 이전 페이지의 클래스는 절대 지우지 않는다.** 그 이전 페이지가 다시 `display:none`으로 가려지는 것도 라이브러리 자신의 렌더 루프(`Render.start()`가 구성 시 한 번 걸어두는, 영원히 도는 `requestAnimationFrame` 루프 — 매 프레임 `drawFrame()`을 불러 `clear()`로 현재 left/right/flipping/bottom이 아닌 페이지를 전부 숨긴다)가 **다음 프레임에** 처리하는 일이다.
    2. `pageFlip.turnToPage()`(우리 `swapRealPageForFlip()`가 애니메이션 시작 *전*에 동기 호출하는 바로 그 함수)는 `render.leftPage`/`rightPage`의 내부 참조와 방향 클래스만 동기적으로 바꾼다 — 실제 `display:block`/좌표 스타일은 여전히 `simpleDraw()`가 맡고, 그건 위 1번의 rAF 루프에서만 불린다.
    - **결론**: `swapRealPageForFlip()` 직후(다음 rAF 프레임이 오기 전, 우리 코드가 동기적으로 바로 이어서 실행하는 그 시점) `js/reader.js`의 `getActiveRealPageRect()`/`getActiveRealSpreadRects()`가 "`--left`/`--right` 클래스 + `display!=='none'`인 요소를 찾아서 그게 지금 보이는 페이지"라고 가정하던 방식은, 방금 교체된 **옛 페이지**를 계속 찾아낼 수 있다 — 새 페이지는 클래스는 이미 붙었어도 아직 `display:none`이라 필터에서 걸러진다. 페이지를 여러 번 넘기면 이런 식으로 스쳐 지나간 옛 페이지들에 `--left`/`--right` 잔여 클래스가 계속 쌓일 수도 있다(과거 세션에서 관찰했던 `allLeftCount:3` 같은 값이 이걸로 설명된다).
  - **수정**: 클래스/`display` 값으로 "지금 보이는 페이지가 누구인지 추측"하는 방식 자체를 버렸다. 대신 우리가 이미 정확히 알고 있는 값 — 그 페이지의 전역 인덱스와 `windowStartIndex` — 로 DOM 순서상 정확한 위치를 직접 찾는다. `#my-book .page`의 DOM 순서가 항상 `createPageElements(start, end, ...)`가 만든 순서(= `windowStartIndex`부터 연속)와 정확히 같다는 것(`buildFlipBook()`의 초기 `.forEach(el => bookElement.appendChild(el))`과 라이브러리의 `updateItems()`가 둘 다 배열 순서 그대로 DOM에 appendChild하는 것, 그리고 `PageCollection.load()`가 그 순서 그대로 `pages` 배열에 담는 것까지 소스로 재확인)를 이용해, `getRealPageElementByGlobalIndex(globalIndex)`(= `document.querySelectorAll('#my-book .page')[globalIndex - windowStartIndex]`) 하나로 항상 정확한 요소를 집는다. `getActiveRealPageRect()`/`getActiveRealSpreadRects()`를 이 조회 위에 재구현하고, 둘 다 이제 명시적으로 어떤 전역 페이지를 재야 하는지 인자로 받는다(예전엔 인자 없이 "지금 보이는 걸 추측"했었다) — `jumpToPrevPage`/`goToNextPage`의 스왑 전/후 호출 6곳 전부 그 시점에 정확히 어떤 전역 인덱스를 재야 하는지 이미 알고 있으므로(`globalIndex`/`targetGlobal`/`targetLeftGlobal`) 그대로 넘기도록 고쳤다.
  - 덤으로 `js/portrait-flip.js`에 남아있던 (이전 라운드의 조사에서 쓰고 지우는 걸 깜빡했던) `[FOOTERDEBUG] overlay base+leaf footer` 임시 콘솔 로그 2곳과, 그것만을 위해 있던 `debugLabel` 매개변수도 같이 제거했다.
  - `sw.js`의 `CACHE_VERSION`을 `v80`→`v81`로 올림.
  - **검증**: 로컬 프리뷰(dev 로그인, 데스크톱 1280px = 가로 스프레드 모드)에서 next/prev 각각 트리거 — 콘솔 에러 없음, 카운터가 정확히 2페이지 단위로 이동(1→3→1), 애니메이션 오버레이 정리됨(`.portrait-flip-stage` 잔여 0개). 조사 과정에서 실제 `.page` 요소들의 DOM 순서·개수를 직접 찍어봐서 "인덱스 기반 조회가 항상 정확한 요소를 가리킨다"는 핵심 가정 자체를 확인했다(footer 텍스트가 window 시작점부터 연속으로 어긋남 없이 매핑됨) — 다만 이 확인 과정에서 **완전히 별개의 사소한 버그도 하나 우연히 발견**: 페이지네이션이 점진적으로(양방향 배경 계산으로) totalPages를 키워가는 동안 이미 만들어둔 `.page-footer`의 "총 페이지 수" 표시(`- N / total -`)가 그 뒤로 안 바뀌는 것으로 보임(카운터 UI는 정확한 반면, 개별 페이지 footer의 total 숫자는 생성 시점 값에 고정) — 이건 이번 버그와 무관한 순수 코스메틱 이슈로 보이고 이번 수정 범위 밖이라 손대지 않았다, 다음에 볼 사람 참고용으로만 남겨둠.
  - **모바일(세로/단일페이지) 모드는 이 세션의 자동화 환경이 로그인 흐름에서 반복적으로 멈춰(30초 타임아웃) 직접 재확인은 못 했다** — 다만 세로 모드가 쓰는 `getActiveRealPageRect(globalIndex)`는 가로 모드가 쓰는 것과 완전히 같은 함수이고 모드별 분기가 전혀 없어서, 가로 모드에서의 검증이 그대로 적용된다고 판단.
  - **여전히 남은 한계**: 이번 수정은 실제 라이브러리 소스를 끝까지 추적해서 찾은, 논리적으로 확실한 구조적 결함을 고친 것이지만, 이 환경은 여전히 실제 화면에서 "왼쪽이 더 이상 잘못 바뀌지 않는지" 눈으로 보는 최종 확인이 불가능하다 — 사용자가 실기기(PC 웹)에서 다음 페이지를 여러 번 넘겨보고 재확인해줘야 한다.
  - (이 항목을 쓴 시점엔 "남은 디버그 로깅 있음"이라고 적었지만, 같은 턴에서 실제로 518번 줄 항목대로 이미 다 지웠다 — 뒤늦게 정정.)
  - **이 "stale --left/--right class matching" 진단·수정 자체는 틀리지 않았지만, 사용자가 실기기에서 재확인한 결과 신고했던 증상은 그대로였다** — 즉 진짜 원인이 아니었거나, 원인의 일부에 불과했다. 아래 항목이 사용자가 직접 문답으로 짚어준 진짜 원인이다.
- ✅ **완료·커밋됨 (2026-08-26, 같은 세션 — 📌📌 사용자가 직접 문답으로 짚어준 진짜 원인 + 수정: leafCard 뒷면 콘텐츠가 반대쪽 패널용이었다)**: 위 수정으로도 "다음 클릭 시 왼쪽이 애니메이션 끝날 무렵 잘못 바뀐다" 신고가 그대로였다. 이번엔 사용자가 보내준 영상(`Bookify 2026-08-26 10-08-24.mp4`)을 ffmpeg로 프레임 단위 재분석했는데 — **이번엔 4번의 "다음" 전환 전부에서 신고된 증상이 재현되지 않았다**(왼쪽 페이지 번호가 단 한 프레임도 어긋나지 않고 깨끗하게 유지됨). 이 사실을 사용자에게 있는 그대로 보고하고 "제가 뭘 놓쳤는지 정확한 시점을 짚어달라"고 요청했더니, 사용자가 **AskUserQuestion 답변이 아니라 직접 대화로, 한 문장씩 "여기까지 이해했어?"로 확인해가며** 정확한 증상을 처음부터 다시 설명해줬다.
  - **사용자가 정정해준 것 2가지**(내가 처음 잘못 짚었던 부분): (1) 클릭 즉시 왼쪽이 애니메이션 없이 바로 바뀌는 것 자체가 "정상"이 아니다(나는 이걸 의도된 설계로 잘못 판단했었다). (2) 넘어가는 카드에 담겨야 할 내용은 오른쪽 목표 페이지가 아니라 **왼쪽 목표 페이지**여야 한다.
  - **정확한 증상**(사용자가 확정): 왼쪽 33/오른쪽 34로 시작 → "다음" 클릭 → 왼쪽이 즉시(애니메이션 없이) 35로 바뀜(이 자체가 문제) → 카드가 회전하며 넘어감(카드엔 36이 들어있음, 문제) → 애니메이션이 끝나는 순간 왼쪽 자리에 36이 잠깐 안착했다가 35로 다시 바뀜.
  - **원인(코드 추적으로 확인)**: "다음" 페이지 넘김의 카드는 오른쪽 패널 자신의 왼쪽 가장자리(=책등/스파인)를 축으로 180도 회전한다. 이건 경첩 달린 문이 180도 열리면 반대편에 가 있는 것과 똑같은 3D 회전 수학이라(`transform-origin`이 있는 가장자리는 고정된 채, 반대쪽 먼 가장자리는 로컬 x가 +W→0→-W로 진행), leafCard의 **뒷면**(back, 90도를 넘어서부터 보이는 면)은 화면상 최종적으로 **왼쪽 자리** 쪽에 걸치게 된다. 그런데 `js/reader.js`의 `buildSpreadPanels()`는 이 뒷면에 여태 "자기 자신과 같은 쪽"의 목표 페이지(오른쪽 패널이면 targetRightGlobal, 즉 36)를 넣고 있었다 — 뒷면이 실제로 걸치는 자리(왼쪽)와 내용(오른쪽 목표)이 서로 안 맞았던 것. 그 결과 회전 후반부~완료 시점에 왼쪽 자리 쪽에 36이 잘못 보이다가, 오버레이가 사라지면 그 아래 이미 조용히 35로 바뀌어 있던 진짜 왼쪽 DOM(`swapRealPageForFlip`이 클릭 시점에 미리 바꿔둠)이 드러나며 "36→35" 정정처럼 보였다.
  - **base와 back을 분리한 이유**: leafCard의 "앞면"(front)과 별개로 `base`라는 정적인 배경 레이어가 있는데, 이건 자기 패널의 자기 자리에 고정된 채 안 움직인다(회전 없음) — 즉 오른쪽 패널의 `base`는 항상 오른쪽 자리에 있으므로, leafCard가 회전해서 그 자리를 비켜줄 때(회전 후반부, leafCard의 뒷면이 왼쪽 자리 쪽으로 넘어가버린 뒤) 오른쪽 자리엔 `base`가 그대로 드러나야 하고, 그건 "자기 자신과 같은 쪽"의 목표(36)를 계속 보여줘야 한다 — 즉 `base`는 원래대로 두는 게 맞다. 그래서 `revealingText`/`revealingFooter`(base용, 안 바꿈)와 새로 추가한 `landingText`/`landingFooter`(leafCard 뒷면 전용, 반대쪽 목표)를 분리했다.
  - **수정 범위**: `js/reader.js`의 `buildSpreadPanels()` — 각 패널(`side:'left'`/`side:'right'`)에 `landingText`/`landingFooter`를 새로 추가, 값은 **반대쪽**의 target(왼쪽 패널이면 targetRightGlobal, 오른쪽 패널이면 targetLeftGlobal). `js/portrait-flip.js`의 `buildPanelAnimation()` — `landingText = revealingText, landingFooter = revealingFooter` 기본값으로 새 매개변수 추가(세로/단일 페이지 모드는 "반대쪽 패널" 개념이 없으므로 안 넘기면 기존과 동일하게 동작), `back` 요소 생성을 `revealingText`→`landingText`로 교체. `playSpreadPageTurn()`이 `panel.landingText`/`panel.landingFooter`를 `buildPanelAnimation()`으로 그대로 전달하도록 배선.
  - `sw.js`의 `CACHE_VERSION`을 `v81`→`v82`로 올림.
  - ⚠️ **검증 중 이 환경의 캐싱-프록시 문제를 다시 세게 겪었다**: `CACHE_VERSION`을 안 올린 채로 한참 테스트했더니, 서비스워커가 재설치 자체를 안 해서(자기 파일 바이트가 안 바뀌었다고 판단) 새 탭·시크릿 수준으로 캐시를 지워도 `js/portrait-flip.js`가 계속 예전 코드로 서빙됐다 — `caches.open()`으로 Cache Storage 안의 실제 캐시된 파일 내용을 직접 열어봐서 확정했다(`fetch(..., {cache:'no-store'})`로 받은 응답은 최신인데, SW가 가로채 돌려주는 응답은 구버전 — 두 경로가 다르다는 걸 이번에 처음 명확히 구분해서 확인). `CACHE_VERSION`을 올리고 정적 서버 프로세스 자체를 재시작(`preview_stop`+`preview_start`)한 뒤에야 SW가 재설치되며 새 코드가 실제로 반영됐다. **교훈**: 이 프로젝트에서 JS 파일을 고친 뒤 로컬에서 검증할 때는 `CACHE_VERSION`을 반드시 먼저 올릴 것 — 안 올리면 아무리 탭을 새로 열고 캐시를 지워도(서비스워커의 `install` 이벤트 자체가 재발동을 안 해서) 예전 코드를 계속 테스트하게 된다.
  - **검증(수정 반영 확정 후)**: dev 로그인 + 데스크톱(1280px, 가로 스프레드) — next 트리거 시 `base`(자기 자리 고정, 오른쪽 목표)=14, `front`(leaving)=12, `back`(반대쪽 목표, 왼쪽 자리로 넘어감)=13으로 셋 다 정확히 기대값과 일치(직전 실패했던 캐시 문제 상태에서는 `back`이 `base`와 똑같이 나왔었음 — 캐시 재설치 후 정확히 갈라짐 확인). 애니메이션 완료 후 카운터 13/46로 정확히 정착, 잔여 오버레이 0개. `prev` 방향도 대칭 확인(`base`=11, `front`=13, `back`=12, 정착 후 11/46). 세로(모바일 390px, 새로고침으로 재빌드 유도) 모드 재검증 — `base`/`back` 둘 다 12로 동일(반대쪽 패널 개념이 없으니 `landingText` 기본값이 `revealingText`와 같아서 정상), 정착 후 12/45, 회귀 없음. 콘솔 에러 전 구간 없음.
  - **여전히 남은 한계**: 이번엔 처음으로 "사용자가 정확히 짚어준 증상"과 "코드에서 확인한 원인"이 논리적으로 정확히 맞아떨어졌고(3D 회전 수학으로 왜 뒷면이 반대쪽 자리에 걸치는지, 그 자리에 왜 반대쪽 목표가 들어가야 하는지까지 설명됨), `base`/`back`이 실제로 서로 다른 값을 갖는 것까지 실측 확인했다 — 지금까지 이 버그에 대한 근거 중 가장 강력하다. 그래도 이 환경은 실제 3D 회전 애니메이션이 화면에 어떻게 보이는지 눈으로 확인할 방법이 없으므로(과거 라운드들과 동일한 제약), 최종 확인은 사용자가 실기기에서 직접 여러 번 넘겨봐야 한다.
  - **이 항목엔 사용자가 원래 짚어준 문제 3가지 중 1번(왼쪽이 클릭 즉시 애니메이션 없이 튀어 바뀌는 것 자체가 정상이 아니다)이 빠져있었다** — 2·3번만 고치고 끝냈는데, 사용자가 "2, 3번은 됐는데 1번은 그대로"라고 재확인해줘서 이어서 고쳤다. 아래 항목 참고.
- ✅ **완료·커밋됨 (2026-08-26, 같은 세션 — 사용자가 짚어준 문제 1번 수정: 반대쪽 패널의 즉시 팝을 정적 덮개로 가림)**: "다음"에서 애니메이션이 안 걸리는 왼쪽 패널(과 "이전"에서 오른쪽 패널)이 `swapRealPageForFlip()`이 스프레드 전체를 애니메이션 시작 전에 한 번에 조용히 스왑해두는 탓에, 클릭 즉시(애니메이션 시작 전부터) 새 내용으로 튀어 보이는 문제.
  - **수정**: `js/portrait-flip.js`에 `coverPanelWithLeavingContent()` 신설 — `buildPageElement()`(기존 base/front/back이 쓰던 것과 같은 헬퍼)로 "예전 내용"을 담은 정적(회전·페이드 없음) 페이지 하나를 반대쪽 패널 자리에 얹는다. `js/reader.js`의 `jumpToPrevPage`/`goToNextPage` 스프레드 분기에서, 애니메이션 대상이 아닌 쪽(각각 오른쪽/왼쪽)의 `toSpreadRects` 위치에 이 덮개를 만들어뒀다가, `playSpreadPageTurn`의 `onDone` 콜백(애니메이션 전체가 끝나는 시점)에서 `finishManualPageTurn`과 함께 제거한다 — 덮개가 사라지는 순간 그 아래 깔려 있던 진짜 DOM(이미 새 내용으로 바뀌어 있음)이 자연스럽게 드러난다. 마침 그 시점엔 반대쪽 카드(leafCard의 back)도 바로 위 항목의 수정 덕에 이미 같은 새 내용으로 안착해 있으므로 이음매 없이 이어진다.
  - `toSpreadRects.left`/`.right`가 없는 경우(마지막 스프레드가 홀수 페이지라 한쪽이 없는 edge case)는 덮개 자체를 안 만든다(가릴 게 없으므로).
  - `sw.js`의 `CACHE_VERSION`을 `v82`→`v83`으로 올림 — **이번엔 처음부터 CACHE_VERSION을 올리고 정적 서버를 재시작한 뒤 검증**해서, 바로 직전 항목에서 겪은 캐싱 삽질을 반복하지 않았다.
  - **검증**: dev 로그인 + 데스크톱(1280px) — next 트리거 시 덮개가 정확히 1개 생성되고 `offsetLeft:0`(왼쪽 슬롯)·footer가 클릭 *전* 왼쪽 값(예: "11/46")을 그대로 유지하는 것을 실측 확인 — 클릭 즉시 팝업하던 이전 동작과 달리 이제 애니메이션이 끝날 때까지 예전 값이 유지된다. 애니메이션 완료 후 카운터 13/46 정착 + 덮개·오버레이 둘 다 0개(누수 없음). `prev` 방향도 대칭 확인(오른쪽에 덮개 생성, offsetLeft:640px, footer는 클릭 전 오른쪽 값 유지, 정착 후 11/46). 4연타 스트레스 테스트 — `isPortraitFlipAnimating()` 가드가 여전히 정상 작동(스테이지·덮개 항상 최대 1개), 딱 한 번만 진행, 잔여 없음. 콘솔 에러 전 구간 없음.
  - **여전히 남은 한계**: 동일 — 실제 화면에서 "왼쪽이 더 이상 즉시 안 바뀌고 애니메이션 끝날 때까지 그대로 있는지"는 이 환경에서 눈으로 볼 수 없다. 사용자가 실기기에서 최종 확인 필요. 이걸로 사용자가 정리해준 3가지 문제(1·2·3번) 전부 코드상으로는 대응 완료.
- 🔄 **작업 중 — `remove-pageflip-library` 브랜치 (2026-08-26)**: 위 항목까지 세 차례 라운드에 걸쳐 페이지 넘김 버그를 고치고 나서, 사용자가 "지금 페이지 관리하는 용도로 라이브러리를 계속 쓰는 방향이 맞다고 봐?"라고 물어서 — 이 앱이 이미 오래전에 페이지 넘김 *애니메이션*은 라이브러리(`js/vendor/page-flip.browser.js`, StPageFlip 자체 패치본) 없이 완전히 자체 구현(`js/portrait-flip.js`)했으면서도, "지금 몇 페이지가 좌/우에 보이는지" 상태 관리 용도로는 계속 라이브러리 전체(책이 열려있는 내내 도는 자체 `requestAnimationFrame` 렌더 루프 포함)를 짊어지고 있었다는 걸 지적했다. 코드를 실제로 살펴서(라이브러리를 부르는 지점이 딱 7가지 API뿐이고, 우리 CSS가 라이브러리 전용 클래스를 단 하나도 참조 안 한다는 것까지 확인) "아니라고 본다"고 답했고, 사용자가 "걷어낼 수 있게 설계해봐" → "별도 브랜치에서 진행하자"고 요청 — `pre-page-window-rewrite` 태그로 백업하고 `remove-pageflip-library` 브랜치에서 진행.
  - **새 모듈 `js/page-window.js`**: 라이브러리를 대체하는 아주 얇은 상태 관리자. `mountWindow(elements)`(= `#my-book`의 자식을 통째로 교체, 라이브러리의 `updateItems()`와 정확히 같은 한 줄짜리 로직)와 `showPage(globalIndex, windowStartIndex, width, height)`/`showSpread(leftGlobalIndex, windowStartIndex, panelWidth, height)`(= 해당 페이지 요소 1~2개만 `display:block`+정확한 위치로 켜고 나머지는 끔) 딱 두 가지 일만 한다. 렌더 루프도 이벤트 시스템도 orientation 판별도 없다 — 전부 호출된 그 순간 동기적으로 끝난다.
  - **CSS(`styles.css`)**: `.page`의 기본 `position`을 `relative`→`absolute`(top:0,left:0)로, `display` 기본값을 명시적으로 `none`으로 바꿨다 — 예전엔 라이브러리가 렌더링 시점마다 `element.style.cssText`를 통째로 덮어써서 이 역할(display 켜기/위치 잡기)을 대신해줬는데(그리고 그 강제 `display:block` 인라인 대입이 몇 주씩 쫓았던 "footer 위치 점프" 버그의 근본 원인이기도 했다 — 위 항목들 참고), 이제 `page-window.js`의 `showAt()`이 그 역할을 명시적으로, 훨씬 단순하게 대신한다.
  - **`js/reader.js` 변경(약 12곳)**: `pageFlip`(라이브러리 인스턴스, truthy/null로 "책이 로드됐는지" 판단하던 변수)을 `bookMounted`(boolean)로 교체. `pageFlip.turnToPage()`/`updateFromHtml()` 조합 3곳(`swapRealPageForFlip`/`maybeShiftPageWindow`/`jumpToGlobalPage`)을 `mountWindow()`+`showPage()`/`showSpread()` 호출로 교체. `windowStartIndex + pageFlip.getCurrentPageIndex()`(2곳)를 이미 있던 `currentDisplayedGlobalPage` 변수로 통일(이 변수가 `updatePageIndicator()`를 통해 이미 모든 페이지 변경 지점에서 정확히 갱신되고 있었다는 것부터 먼저 확인). `pageFlip.destroy()`(2곳)를 `clearWindow()`/`bookMounted=false`로.
  - **`pageFlip.on('flip', ...)` 핸들러를 통째로 삭제** — 코드를 추적해보니 이 앱의 모든 `turnToPage()` 호출이 전부 `isShiftingWindow=true`로 감싸져 있었거나 이 리스너가 등록되기 *전에* 실행돼서, 이 핸들러의 실제 로직(진행상황 저장/인디케이터 갱신/창 재정렬)은 이미 오래전부터 한 번도 실행된 적이 없는 죽은 코드였다 — 실제 페이지 전환은 전부 `finishManualPageTurn()`(수동 애니메이션의 `onDone`)이 담당하고 있었다. 라이브러리 제거로 이벤트 시스템 자체가 없어지면서 자연스럽게 정리됨 — 잃는 동작 없음.
  - **`computeWindowRange()`의 "창 시작은 항상 짝수" 제약도 삭제** — 라이브러리가 스프레드를 *로컬* 인덱스로 짝지어서 생겼던 제약이었는데, 스프레드를 이제 *전역* 인덱스로 직접 짝지으므로(`showSpread(leftGlobalIndex, ...)`) 창 경계의 홀짝이 스프레드 정렬과 완전히 무관해졌다.
  - **삭제**: `js/vendor/page-flip.browser.js`(9~10차에 걸쳐 패치가 쌓였던 그 파일)와 `js/vendor/page-flip.LICENSE`, `index.html`의 `<script src="js/vendor/page-flip.browser.js">`, `sw.js`의 해당 프리캐시 항목.
  - `sw.js`의 `CACHE_VERSION`을 `v83`→`v84`로 올림.
  - **검증**: 서비스워커 캐시를 완전히 지우고 정적 서버를 재시작한 뒤(이번 세션에서 여러 번 겪은 캐싱 문제를 다시 반복하지 않도록) dev 로그인으로 전체 라이프사이클을 직접 확인했다 — (1) 초기 로드: 가로(1280px) 스프레드 모드에서 정확히 2개 페이지만 `display:block`(좌우 위치·크기 정확), `#my-book` 크기가 렌더링 크기와 정확히 일치. (2) next/prev 각각 — 콘솔 에러 없음, 정확히 2페이지씩 이동, 애니메이션 오버레이 정리됨. (3) **창 재정렬(`maybeShiftPageWindow`) — 진짜 라이브러리 없이 처음으로 검증**: 슬라이더로 중간(20페이지)까지 점프한 뒤 이전 페이지를 6번 실제로 눌러(매번 애니메이션 완료를 기다리며) 창 경계 근처까지 이동시켜서 재구성이 실제로 일어나는 것을 확인 — 마운트된 창이 정확히 책 시작(1페이지)부터 다시 시작하고, 그 안에서 현재 위치(6/7페이지)가 정확히 표시됨. (4) 슬라이더 점프(`jumpToGlobalPage`) — 끝 근처(41페이지)로 직접 점프, 창이 올바르게 재계산되고 정확한 페이지가 표시됨. (5) 세로(모바일 390px, 새로고침으로 재빌드 유도) 단일 페이지 모드 — 초기 로드/next 둘 다 정상, 페이지 1개만 정확한 크기로 표시. (6) "서재로 돌아가기"(`resetReaderSession`) → 같은 책 재오픈 전체 사이클 — 콘솔 에러 없이 깨끗하게 재구축됨(진행상황도 정확히 복원). 모든 단계에서 콘솔 에러 0건.
  - **여전히 남은 한계**: 이 환경에서 실제 애니메이션이 화면에 어떻게 보이는지는 여전히 확인 불가(과거 라운드들과 동일한 제약) — 다만 이번 변경은 애니메이션 자체(`js/portrait-flip.js`)를 전혀 안 건드렸고 오직 "페이지 상태 관리" 계층만 교체했으므로, 애니메이션 관련 리스크는 낮다고 판단(애니메이션 코드는 여전히 `getBoundingClientRect()`로 실제 DOM만 읽어서 동작하고, 라이브러리 유무와 무관하게 동일하게 작동해야 한다). 그래도 별도 브랜치에서 작업했고 실기기에서 전체 리딩 경험(페이지 넘김 애니메이션 느낌, 스크롤/리사이즈 등)을 사용자가 확인한 뒤에 `main`으로 머지할 것 — 아직 머지 안 함.
- ✅ **완료·커밋됨 (2026-08-26, 다른 컴퓨터 세션 — `remove-pageflip-library`를 `main`에 머지 + 회귀버그 발견·수정)**: 사용자가 실기기에서 확인했다고 확답한 뒤 "머지하자 메인으로" 요청 — `--no-ff`로 머지(`47feba1`), push까지 완료.
  - 머지 직후 사용자가 "페이지 넘김 애니메이션 자체가 없어, 근데 그림자는 생겼다 없어진다"고 재신고. `js/portrait-flip.js` 자체는 이번 라이브러리 제거 작업에서 한 줄도 안 바뀌었는데도 발생한 회귀라, 원인은 그 파일과 새 CSS 기본값의 상호작용에 있었다.
  - **원인**: `remove-pageflip-library` 작업이 `styles.css`의 `.page` 기본값에 `display: none`을 새로 추가했다(진짜 책 페이지는 `page-window.js`의 `showAt()`이 보여줄 것만 명시적으로 `display:block` 켜주는 구조라서 — 위 543/544 항목 참고). 그런데 `js/portrait-flip.js`의 `buildPageElement()`(애니메이션용 `base`/`front`/`back`, 그리고 `coverPanelWithLeavingContent()`의 덮개까지 전부 이 함수로 만들어짐)는 스타일을 그대로 물려받으려고 `className='page'`를 쓰면서도 `display`를 명시적으로 켜지 않았다 — 그래서 이 요소들이 전부 새 CSS 기본값(`display:none`)을 그대로 물려받아 처음부터 안 보이는 상태가 됐다. `leafCard`(회전하는 래퍼) 자신은 `.page`가 아니라 안 보이는 이유가 없지만, 그 자식인 `front`/`back`과 그 밑에 깔린 `base`가 전부 안 보이니 — 애니메이션이 도는 동안 화면엔 그림자(`shadowLayer`, `.page`가 아니라 영향 없음)만 페이드 인/아웃되고, 오버레이가 걷히는 순간에야 진짜 페이지(이미 스왑 완료)가 "순간이동"한 것처럼 드러난 것 — 신고 내용과 정확히 일치.
  - **수정**: `buildPageElement()`에 `el.style.display = 'block';` 한 줄 추가 — base/front/back/덮개 네 용도 전부 이 함수 하나를 공유하므로 한 곳만 고치면 전부 해결된다.
  - `sw.js`의 `CACHE_VERSION`을 `v84`→`v85`로 올림.
  - **검증**: 실제 커밋되는 `js/portrait-flip.js`를 동적 import해서 `playPortraitPageTurn()`을 직접 호출해보니 `base`/`front`/`back` 셋 다 `getComputedStyle().display === 'block'`으로 정상 계산되고 콘텐츠도 각각 정확(`base`="NEW", `front`="OLD")한 것을 확인. 별도로 아무 인라인 스타일 없는 `class="page"` 요소를 만들어 `display:none`이 실제로 새 CSS 기본값임을 재확인해서 버그 메커니즘 자체도 실증했다. ⚠️ 이번에도 이 환경은 실제 화면에서 카드가 회전하는 모습 자체를 보는 건 못 했다(document.hidden 등 과거부터 반복된 제약, dev-test-book.txt도 이 환경에서는 페이지 분할 측정이 깨져 1페이지로 잘못 나와 다중 페이지 넘김 자체를 재현 못 함) — 다만 이번 진단은 실측(computed style)으로 버그의 정확한 메커니즘(CSS 기본값 상속)까지 확인했으므로 지금까지 라운드 중 근거가 강한 편이다. 사용자가 실기기에서 최종 확인 필요.
- ✅ **완료·커밋됨 (2026-08-26, 같은 세션 — 밝기 낮추고 페이지 넘기면 애니메이션 도중 밝기가 적용 안 되는 버그)**: 사용자가 "밝기를 내리고 페이지 이동을 하면 페이지 넘기는 애니메이션 중에는 밝기 적용이 안 되고 애니메이션이 끝나고 나서야 다시 내려놓은 밝기로 돌아온다"고 신고 — `#book-spine`의 "뿅 등장" 버그(2026-08-26 앞쪽 항목)와 **완전히 같은 부류**의 z-index 문제였다.
  - **원인**: `#brightness-overlay`(밝기를 흉내내는 반투명 검은 막, `#book-stage`의 형제 요소, `z-index:6`)가 `js/portrait-flip.js`의 페이지 넘김 오버레이(`perspectiveStage`, `#book-stage` *안*에 있음, `z-index:20`)보다 한참 낮았다. `#book-stage` 자신은 `position:relative`뿐 `z-index`/`transform`/`opacity`/`filter`/`isolation` 등 스태킹 컨텍스트를 만드는 속성이 하나도 없어서(실측 확인), 그 안에 있는 `perspectiveStage`의 `z-index:20`이 `#book-stage` 바깥의 형제 요소(`#brightness-overlay`, z:6)와 직접 비교되는 같은 스태킹 컨텍스트로 "새어나갔다" — 애니메이션 카드가 불투명한 실제 페이지 내용이라, 그 밑에 깔린 어두운 막을 완전히 가려버려서 애니메이션 도중엔 밝기 낮춤이 안 보이다가, 애니메이션이 끝나 오버레이가 사라지는 순간에야 다시 드러났다.
  - **수정**: `#brightness-overlay`의 `z-index`를 `6`→`22`로 올려서(페이지 넘김 오버레이 20, book-spine 21보다 위) 애니메이션 도중에도 계속 그 위에서 어둡게 덮도록 했다. 그런데 이러면 `#brightness-drag-indicator`(밝기 조절 중 뜨는 "☀️ 밝기 NN%" 안내, 원래 z:8이라 이제 새 밝기 막(22)에 가려짐)가 정작 밝기를 조절하는 그 순간에 안 보이게 되는 새 문제가 생겨서, 같이 `8`→`23`으로 올렸다.
  - `sw.js`의 `CACHE_VERSION`을 `v85`→`v86`으로 올림.
  - **검증**: 이번엔 `elementFromPoint()`로 실제 페인트 순서를 검증하려다 첫 시도에서 함정에 걸렸다 — `#brightness-overlay`는 평소 `pointer-events:none`이 걸려있어서(밝기 막 아래로 탭이 그대로 통과해야 하므로) `elementFromPoint()`가 애초에 히트테스트에서 건너뛰어, z-index와 무관하게 항상 그 아래 요소가 잡혔다(첫 실측에서 이 함정 때문에 "여전히 애니메이션이 위에 있다"는 잘못된 결과가 나왔다가, 원인을 알아채고 재시도). `pointer-events`를 테스트 동안만 `auto`로 바꾸고 나서 재실측하니, `#book-stage` 안에 진짜 애니메이션 오버레이와 동일한 조건(z:20, 불투명 배경)의 가짜 요소를 만들어도 `#brightness-overlay`(z:22)가 정확히 그 위에서 히트되는 것을 확인 — 수정이 의도대로 동작함을 실증했다. `#brightness-drag-indicator`도 `23`으로 여전히 밝기 막 위에 있는 것 확인. ⚠️ 이번에도 실제 화면을 눈으로 보는 확인은 못 했다(이 환경의 반복된 제약) — 다만 CSS 스태킹 컨텍스트 확립 조건을 전부 실측(position/z-index/transform/opacity/filter/isolation/will-change)해서 가정 자체를 검증한 뒤 고쳤고, 수정 후 실제 페인트 순서(히트테스트 우회 보정까지 거친 정확한 방법)까지 확인했으므로 근거가 탄탄하다. 사용자가 실기기에서 최종 확인 필요.
- ✅ **완료·커밋됨 (2026-08-26, 같은 세션 — PC에서 글자 크기를 키워도 다시 열면 기본값으로 돌아오는 버그)**: 사용자가 "PC에서 글자 크기를 몇 번이나 키웠는데 다시 열면 보통으로 나온다"고 신고 — 사용자가 실제 배포 사이트(Vercel, `txt-viewer-murex.vercel.app`)에서 직접 확인해도 좋다고 승인해줘서, **Claude in Chrome으로 실제 로그인 계정에 직접 접속해 재현·진단·수정 검증까지 전부 실제 프로덕션에서 했다.**
  - **1차 진단(코드 리뷰)**: `js/reader.js`의 저장/불러오기 자체(Firestore `users/{uid}/settings/readerPrefs`, 기기별 `pc`/`mobile` 프로필, 탭 숨김/닫힘 시 flush)는 논리적으로 문제없어 보였다.
  - **실제 계정에서 재현**: 뷰어 설정을 열어보니 실제로 글자 크기가 "보통"(기본값)으로 나와 있었다 — 사용자의 신고와 일치하는 초기 상태를 직접 목격. Firestore 문서를 직접 조회(`getDoc`)해도 `pc.fontSizeStep: 2`(기본값)로 확인됐다.
  - **직접 테스트**: 글자 크기를 두 단계 키우고 "적용" → 몇 초 대기 → 페이지 전체 새로고침(실제 재로그인 플로우까지 다시 거침) → 다시 Firestore를 직접 조회하니 이번엔 `pc.fontSizeStep: 4`로 정확히 저장·복원됐다. 즉 **저장 메커니즘 자체는 정상 작동**했다 — 그런데 사용자의 실제 사용 패턴에서는 매번 실패했다는 뜻이라, "언제" 실패하는지가 관건이었다.
  - **진짜 원인**: `saveReaderPrefsDebounced()`(글꼴/글자크기/문단너비/밝기 저장에 공통으로 쓰던 500ms 디바운스 함수)의 호출부를 다시 살펴보니, 실제로는 **디바운스가 전혀 필요 없는 자리에서만 쓰이고 있었다** — "적용" 버튼 클릭(1회성 이벤트)과 밝기 드래그가 끝나는 순간(`endBrightnessDrag`, 이것도 드래그 *도중*이 아니라 손을 뗀 뒤 1회성으로만 호출 — 드래그 중엔 `applyReaderPrefs()`만 실시간으로 돌고 저장 함수는 안 건드림) 딱 두 곳뿐이라, "여러 번 연달아 불려서 마지막 것만 반영하면 되는" 디바운스의 존재 이유 자체가 코드에 없었다. 그런데도 매번 저장을 500ms 늦추기만 해서, 그 사이(탭을 닫거나 다른 페이지로 이동하는 등) `flushReaderPrefsSave()`의 `pagehide`/`visibilitychange` 안전망이 못 따라잡는 실제 사용 패턴(예: "적용" 클릭 직후 곧바로 책을 덮거나 앱을 종료)에서는 저장이 그대로 유실될 수 있는 구조였다 — 사용자가 "몇 번이나 시도해도 매번 안 된다"고 한 것과 정확히 들어맞는다.
  - **수정**: `saveReaderPrefsDebounced()`와 그 타이머(`saveReaderPrefsTimer`)를 완전히 제거하고, 두 호출부(적용 버튼, 밝기 드래그 종료) 모두 디바운스 없이 즉시 저장하는 `flushReaderPrefsSave()`로 통일했다 — 안전망(pagehide 등)에 기대는 대신,애초에 지연 자체를 없애서 그 경쟁 구간을 원천 차단.
  - `sw.js`의 `CACHE_VERSION`을 `v86`→`v87`로 올림.
  - **검증**: dev 세션(로컬)에서 "적용" 클릭 직후, **같은 동기 실행 틱 안에서** `localStorage`를 바로 읽어 `fontSizeStep`이 이미 반영돼 있는 것을 확인 — 이전엔 500ms를 기다려야 반영됐을 것이 이제 완전히 동기적으로 저장된다. 콘솔 에러 없음. 프로덕션 계정에서의 실측(위 "직접 테스트" 항목)까지 더하면, 이 항목은 실기기(정확히는 실제 배포 사이트+실제 계정) 확인까지 이미 거친 몇 안 되는 케이스 중 하나다 — 다만 그 실측은 수정 *전* 코드로 한 것이었고, 수정 자체는 로컬에서만 검증됨. 사용자가 다시 프로덕션에서 "설정 바꾸고 바로 앱을 벗어나도 저장되는지" 재확인해보면 좋다.
